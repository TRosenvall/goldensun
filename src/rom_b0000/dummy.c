/*
 * Phase 1 Task 1; empty translation unit used to prove the agbcc -> as
 * pipeline is wired into the build. No symbols emitted; contributes zero
 * bytes to the linked image. Delete once a real .c file lives here.
 */
