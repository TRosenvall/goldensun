/* Func_c628 -- ScriptOp_Continue -- opcode handler that does nothing and returns 1, keeping
 *
 * the VM running. The no-op entry in the Data_13624 dispatch table.
 *
 * STATUS: MATCHING.
 */
int Func_c628(void) { return 1; }
