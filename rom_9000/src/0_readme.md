I'm a total novice at this and don't really understand good coding practices when it comes to decomp. So what I've discovered is that when I run `make`, or in my case `gmake`, the checksum is very sensative to linker order and placement. Originally, I wanted to see if I could elevate Func_b684 in rom_074.s to be a c function. But it turns out you can't just pull out a function like that or it messes up the ordering of things. Then in order to fix the ordering, I removed each function into their own .s file. That way in the linker file, I could ensure that I was compiling each function in the needed order. 

In order to ensure that I kept the proper order, I devised my own naming scheme for the files. I wanted the exports file to remain at the top of the directory, so I started each file with the letter `f`. I noticed that I was working in the `rom_b074.s` file which was the `9`th one from the top. I'm under the impression that ordering is important so I used 9 in order to keep the final spacing correct. Then I labeled each file according to the function order. Since Func_b388 was the `3`rd function from the top, it's file name became `f9_3_rom_b388.s`.

Lastly, at the end of the `rom_b074.s` file is a small section about rodata. I'm not particularly familiar with what it is or why it's there, but it seemed important and there's a section for it in the linker file. I removed it to the file `f9_6_rom_b074_rodata.s`, and appended that to the linker file. The issue is that the associated rodata is a local symbol required in `Func_b6b8`. To that end, I made it a global symbol and updated the reference in the required function. This did pass the checksum, and it will become my standard, but I'm worried that it might be fragile in the long run. I will be very careful in doing this going forward and ensuring I test this all well.

Incidentally, I updated the Makefile to compile c files, so now we can start converting the game functions to c and test if they match or not.