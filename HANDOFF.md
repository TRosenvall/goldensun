# Handoff index

Batches of matching C ready to port into `Coaltergeist/goldensun-decomp`.

Each batch is self-contained: a table of what was elevated, what it replaces,
and anything found on the way that is worth passing upstream. Read them in
order; later ones assume the tooling from earlier ones is already in.

| Batch | Functions | Status |
|---|---|---|
| [batch-01](reports/batch-01.md) | 12 | ready to port |
| [batch-02](reports/batch-02.md) | 7 | ready to port |
| [batch-03](reports/batch-03.md) | 11 | ready to port |
| [batch-04](reports/batch-04.md) | 8 | ready to port |
| [batch-05](reports/batch-05.md) | 7 | ready to port |
| [batch-06](reports/batch-06.md) | 5 | ready to port |
| [batch-07](reports/batch-07.md) | 5 | ready to port |
| [batch-08](reports/batch-08.md) | 10 | ready to port |
| [batch-09](reports/batch-09.md) | 9 | ready to port |
| [batch-10](reports/batch-10.md) | 12 | ready to port |
| [batch-11](reports/batch-11.md) | 12 | ready to port |
| [batch-12](reports/batch-12.md) | 4 | ready to port |
| [batch-13](reports/batch-13.md) | 12 | ready to port |
| [batch-14](reports/batch-14.md) | 8 | ready to port |
| [batch-15](reports/batch-15.md) | 29 | ready to port |
| [batch-16](reports/batch-16.md) | 11 | ready to port |
| [batch-17](reports/batch-17.md) | 18 | ready to port |
| [batch-18](reports/batch-18.md) | 14 | ready to port |
| [batch-19](reports/batch-19.md) | 19 | ready to port |
| [batch-20](reports/batch-20.md) | 5 | ready to port |
| [batch-21](reports/batch-21.md) | 7 | ready to port |
| [batch-22](reports/batch-22.md) | 5 | ready to port |
| [batch-23](reports/batch-23.md) | 6 | ready to port |
| [batch-24](reports/batch-24.md) | 11 | ready to port |
| [batch-25](reports/batch-25.md) | 6 | ready to port |
| [batch-26](reports/batch-26.md) | 5 | ready to port |
| [batch-27](reports/batch-27.md) | 5 | ready to port |
| [batch-28](reports/batch-28.md) | 8 | ready to port |
| [batch-29](reports/batch-29.md) | 5 | ready to port |
| [batch-30](reports/batch-30.md) | 5 | ready to port |
| [batch-31](reports/batch-31.md) | 6 | ready to port |
| [batch-32](reports/batch-32.md) | 9 | ready to port |
| [batch-33](reports/batch-33.md) | 5 | ready to port |
| [batch-34](reports/batch-34.md) | 5 | ready to port |
| [batch-35](reports/batch-35.md) | 8 | ready to port |
| [batch-36](reports/batch-36.md) | 7 | ready to port |
| [batch-37](reports/batch-37.md) | 5 | ready to port |
| [batch-38](reports/batch-38.md) | 10 | ready to port — 7 are fakematches |
| [batch-39](reports/batch-39.md) | 7 | ready to port |
| [batch-40](reports/batch-40.md) | 8 | ready to port |
| [batch-41](reports/batch-41.md) | 6 | ready to port |
| [batch-42](reports/batch-42.md) | 6 | ready to port |
| [batch-43](reports/batch-43.md) | 8 | ready to port |
| [batch-44](reports/batch-44.md) | 8 | ready to port |
| [batch-45](reports/batch-45.md) | 5 | ready to port |
| [batch-46](reports/batch-46.md) | 7 | ready to port — 2 are unparks |
| [batch-47](reports/batch-47.md) | 6 | ready to port |
| [batch-48](reports/batch-48.md) | 7 | ready to port |
| [batch-49](reports/batch-49.md) | 9 | ready to port |
| [batch-50](reports/batch-50.md) | 7 | ready to port |
| [batch-51](reports/batch-51.md) | 11 | ready to port |
| [batch-52](reports/batch-52.md) | 7 | ready to port |
| [batch-53](reports/batch-53.md) | 8 | ready to port |
| [batch-54](reports/batch-54.md) | 6 | ready to port |
| [batch-55](reports/batch-55.md) | 6 | ready to port — 2 are unparks |
| [batch-56](reports/batch-56.md) | 6 | ready to port |
| [batch-57](reports/batch-57.md) | 6 | ready to port |
| [batch-58](reports/batch-58.md) | 6 | ready to port |
| [batch-59](reports/batch-59.md) | 6 | ready to port |
| [batch-60](reports/batch-60.md) | 5 | ready to port |
| [batch-61](reports/batch-61.md) | 5 | ready to port |
| [batch-62](reports/batch-62.md) | 5 | ready to port |
| [batch-63](reports/batch-63.md) | 5 | ready to port — 1 is an unpark |
| [batch-64](reports/batch-64.md) | 8 | ready to port — 4 are one function in four overlays |
| [batch-65](reports/batch-65.md) | 8 | ready to port — 7 first-screen, via existing .sym symbols |
| [batch-66](reports/batch-66.md) | 5 | ready to port — all via existing .sym symbols |
| [batch-67](reports/batch-67.md) | 5 | ready to port — 8 new area ids added on stated evidence |
| [batch-68](reports/batch-68.md) | 7 | ready to port — 5 new message ids; 1 park unparked in the same batch |
| [batch-69](reports/batch-69.md) | 7 | ready to port — 6 are one function in six places, via a new -fno-strict-aliasing flag group |
| [batch-70](reports/batch-70.md) | 8 | ready to port — 5 are one bitfield store in five places; 1 unpark |
| [batch-71](reports/batch-71.md) | 7 | ready to port — solves the 34-function narrow-constant blocker; 16 stale parks deleted |
| [batch-72](reports/batch-72.md) | 5 | ready to port — the struct-member read lever; a switch reproduces a ROM jump table |
| [batch-73](reports/batch-73.md) | 6 | ready to port — register class measured and narrowed by a compiler experiment |
| [batch-74](reports/batch-74.md) | 5 | ready to port — new -fno-gcse group; three size symbols; a blob that is bigger than its function |
| [batch-75](reports/batch-75.md) | 5 | ready to port — do-while wrappers are load-bearing; tryc's pool warning hole closed |
| [batch-76](reports/batch-76.md) | 5 | ready to port — address vs value; the three-operand lever and its limit |
| [batch-77](reports/batch-77.md) | 5 | ready to port — 2 solves with free twins; one short member read two ways |
| [batch-78](reports/batch-78.md) | 6 | ready to port — 3 solves, 3 free twins; a split that dropped its .data |
| [batch-79](reports/batch-79.md) | 6 | ready to port — `ldrh rD, .L` IS `ldr rD, =v`; pool ORDER reads operand modes |
| [batch-80](reports/batch-80.md) | 6 | ready to port — the pool_range table from arm.md; naming a file-local data label |
| [batch-81](reports/batch-81.md) | 6 | ready to port — tryc called clean matches failures; the 13-member family at 144/144 |
| [batch-82](reports/batch-82.md) | 5 | ready to port — asm labels beat renaming; a named local can COST the preferred register |
| [batch-83](reports/batch-83.md) | 6 | ready to port — which operand becomes the `orr` destination; const.sym |
| [batch-84](reports/batch-84.md) | 5 | ready to port — read the constant's WIDTH off the ROM; split_asm could not see .lcomm |
| [batch-85](reports/batch-85.md) | 5 | ready to port — batch 83's lever is not a rule; pick_candidates sees BUILT constants |
| [batch-86](reports/batch-86.md) | 5 | ready to port — both answers to the operand lever in one function; where the local goes |
| [batch-87](reports/batch-87.md) | 5 | ready to port — find a family by its instruction SHAPE, not by byte identity |
| [batch-88](reports/batch-88.md) | 9 | ready to port — tools/find_shape.py --clusters: 143 functions reachable by shape |
| [batch-89](reports/batch-89.md) | 7 | ready to port — a two-way choice of NEARBY constants goes branchless; scope of a named arg |
| [batch-90](reports/batch-90.md) | 7 | ready to port — a trailing `& 0xf` can be a BITFIELD; bitfield write ORDER is source order |
| [batch-91](reports/batch-91.md) | 5 | ready to port — a fourth facing test (the quadrant mask), and what a repeated `cmp` says about a switch |
| [batch-92](reports/batch-92.md) | 6 | ready to port — a missing prototype reorders argument setup; two readings that failed their own control |
| [batch-93](reports/batch-93.md) | 5 | ready to port — the prototype lever runs both ways; 52 parks are within six instructions |
| [batch-94](reports/batch-94.md) | 5 | ready to port — the gState offset must be BUILT not folded; a callee-saved register is not a naming signal |
| [batch-95](reports/batch-95.md) | 5 | ready to port — `pushal` is `push`; a load's operand order says subscript vs pointer arithmetic |
| [batch-96](reports/batch-96.md) | 5 | ready to port — the positive test stops the return-constant hoist; the r2/r3 class named |
| [batch-97](reports/batch-97.md) | 5 | ready to port — a named constant's TYPE can decide an r2/r3 pair; two pointer chains must both be live |
| [batch-98](reports/batch-98.md) | 5 | ready to port — the WIDTH of the arithmetic decides add-vs-sub; the r2/r3 live-range theory refuted |
| [batch-99](reports/batch-99.md) | 5 | ready to port — the prototype lever is really a RETURN TYPE lever; two files de-folklored |
| [batch-100](reports/batch-100.md) | 5 | ready to port — bcc/bhi in a switch means an UNSIGNED value; the exit layout follows duplicated tails |
| [batch-101](reports/batch-101.md) | 5 | ready to port — jump tables reachable (106 remain); split_s.py was blind to .incrom |
| [batch-102](reports/batch-102.md) | 5 | ready to port — the jump-table class worked systematically; READ THE TABLE for the case order |
| [batch-103](reports/batch-103.md) | 5 | ready to port — an out-of-line arm needs a goto; symbol-vs-literal split inside one switch |
| [batch-104](reports/batch-104.md) | 5 | ready to port — the 1000+ band scouted and its blocker named; a named constant that crosses a call; HImode constants have no immediate |
| [batch-105](reports/batch-105.md) | 5 | ready to port — the basic-block lever retires the r0-against-a-shift class and reaches pool-loads-first; five parks unparked, no new function attempted |
| [batch-106](reports/batch-106.md) | 5 | ready to port — CSE_CFLAGS vs the basic-block lever separated (try the flag first); the full table of argument orders; a low-numbered .L symbol cannot use the asm-label extension |
| [batch-107](reports/batch-107.md) | 5 | ready to port — rebuilt-vs-carried is one rule behind three blocker classes; the HImode rule needs the lever's placement too; one park unparked |
| [batch-108](reports/batch-108.md) | 7 | ready to port — grep every .s for a solved function's PROLOGUE; two families found that way; the three-operand add means a named pointer |
| [batch-109](reports/batch-109.md) | 7 | ready to port — tools/prologue_families.py clusters the whole tree into families (34 of 3+, three of 17-18 members left); carried values want naming only if gcc would rebuild them |
| [batch-110](reports/batch-110.md) | 5 | ready to port — the 18-member block-pushing family parked at 7 of 176; band breakdown of the remaining 2475; a constant hoist the CSE flag does NOT reach |
| [batch-111](reports/batch-111.md) | 12 | ready to port — four parallel screening agents (coordinator does all tree mutation); the symbol-address technique retires two parks; constant-CSE blocker measured to the end (49% of remaining mass at risk) |
| [batch-112](reports/batch-112.md) | 13 | ready to port — a park that was never wrong (a dirty screen opening on a LABEL is a false negative); the symbol tell fires on large values too; six TUs wanted CSE_CFLAGS |
| [batch-113](reports/batch-113.md) | 10 | ready to port — a double slash made six Makefile rules silently dead (clean overlays, failing ROM sha1); two mis-scoped -O1 wildcards; label sweep of all 228 parks is a clean negative |
| [batch-114](reports/batch-114.md) | 6 | ready to port — block layout tells you which branch is the if BODY (42 of 55 wrong way round); signed division and signed ranges are gcc's own; grep for an IDIOM as a third family axis |
| [batch-115](reports/batch-115.md) | 32 | ready to port — a .pool_aligned inside a loop is a SECOND label false negative my batch-113 sweep could not have found (4 byte-identical functions); -fno-rerun-cse-after-loop costs matches in loops; declaration order, parameter-copy and while(1)-increment levers |
| [batch-116](reports/batch-116.md) | 0 | 12 parks, six at <=5 differing — store INSIDE each arm or gcc speculates; read a field twice to get the mov; `ldr rN,=0` is NOT a symbol tell; `push {r4` greps for a file that wants -fcall-saved-r4 |
| [batch-117](reports/batch-117.md) | 5 | ready to port — -ffixed-r7 as a per-file group (ROM spends r8 where gcc spends r7); the no-prototype lever for argument order; tools/script_candidates.py avoids the straight-line constant-CSE class at selection time |
| [batch-118](reports/batch-118.md) | 31 | ready to port — tryc.py folded `mov rd,rs` = `add rd,rs,#0` (low regs only), a false negative that was costing correct answers; -ffixed-r7; the HImode-literal rule narrowed to 0 and >=0x8000; corrects the batch-116 push{r4} overstatement |
| [batch-119](reports/batch-119.md) | 34 | ready to port — RETRACTS batch 118's ".call_via is a hard wall": tryc.py was dropping the line and inline asm reaches it, 51 functions back in play; _call_via_sl/_call_via_ip added; the constant-hoist lever needs a dominating block |
| [batch-120](reports/batch-120.md) | 18 | ready to port — a blocked_cse filter that filtered NOTHING (awk column off by one, 113 copies of the word "repeats"); corrections to the .call_via helper guidance; build the constant AFTER the call; naming one level too many costs a register |
| [batch-121](reports/batch-121.md) | 18 | ready to port — tools/twin_families.py: work by FAMILY, 46 families cover 96 remaining functions; DMA call belongs in BOTH arms (retracts a batch-120 note); the ORR-destination local must be NARROW; a CSE boundary is necessary but not sufficient |
| [batch-122](reports/batch-122.md) | 21 | ready to port — `cmp #K/bge` with K>0 is UNREACHABLE (read from combine.c), decidable from the ref; goto loops disable loop optimisation entirely (95 differing -> 3 where no flag helped); three more mis-scoped O1 wildcards had three functions parked at the wrong -O level |
| [batch-123](reports/batch-123.md) | 8 | ready to port — RETRACTS the "gcc always folds symbol+offset" park: 34 of 531 already-matching functions carry the shape, and the integer-local address idiom produces it; 356 remaining functions in reach. The first control read 0 of 531 because the detector wanted the two-operand `add` — count sub-patterns. `ldrh` vs `ldrsh` is set by the DESTINATION type; a rebuilt loop bound is not always the goto tell |
| [batch-124](reports/batch-124.md) | 8 | ready to port — the batch-123 address idiom used deliberately: three of four candidates matched first or second screen. One integer local PER ADDRESS CHAIN (reusing one scratch variable was 10 of 32 and no reordering fixed it). tools/oneref.py makes the tryc size check one command -- it is skipped on any multi-function ref, which is how a fakematch shipped |
| [batch-125](reports/batch-125.md) | 8 | ready to port — tools/solved_twins.py searches the remaining functions against the SOLVED ones; seven of eight this batch were twins, six found by the tool. A twin inherits its template's FLAG GROUP too. Closes a park outright (Func_8020a60 was its already-elevated twin's source with the name changed). Corrects batch 124's named-pointer precondition: necessary, not sufficient |
| [batch-126](reports/batch-126.md) | 5 | ready to port — solved_twins queue emptied (0 hits at length 8; re-run each round, the corpus grows). TRUST THE TEMPLATE HEADER: "correcting" two documented levers on a twin cost 67 and 4 differing, the unmodified rename was exact. Named-pointer lever pinned: it works when the other operand needs its own register, folds when it is a bare constant. A branchless-idiom match is UNDONE by naming the constant |
| [batch-127](reports/batch-127.md) | 5 | ready to port — CORRECTS three parks: the interleaved argument order IS reachable (144 of 3411 solved functions have it). Lever is named locals at the top WITH A BRANCH between them and the call; the branch is load-bearing and straight-line functions get worse, not better. 248 carry the shape, 150 guarded. Covers mov+neg as well as mov+lsl. Also: a redundant copy before a loop can mean the function RETURNS that value; an added push holding a commoned constant is a CSE_CFLAGS tell, read the push list first |
| [batch-128](reports/batch-128.md) | 6 | ready to port — the interleave detector was too narrow: r0 can be ANY constant, which took the guarded pool from 97 to 194 functions and the smallest from 85 insns to 18. A call with TWO split-build arguments needs BOTH named (naming one is no win). The commoned-constant tell has two remedies -- CSE_CFLAGS and separate named locals -- that are NOT interchangeable, try both. Three distinct behaviours for a stored constant zero |
| [batch-129](reports/batch-129.md) | 6 | ready to port — DENSITY not size is the selector: filter the guarded pool by calls*4>=insns and mem*4<=insns; two rounds picking small functions produced zero matches, the density filter gave three of four. GetFlag(id)/SetFlag(id) in one function means CSE_CFLAGS (six cases to one). Consume the pointer (a += 0x62) rather than index it, worth 28 differing to 7. A detector returned 0 of 2227 twice because I compared against a space where the asm has a tab -- when the first fix does not move a zero, suspect the harness |
| [batch-130](reports/batch-130.md) | 5 | ready to port — NO-PROTOTYPE LEVER: when the ROM sets up `mov r1 / mov r0` and gcc gives `mov r0 / mov r1`, remove the callee's prototype (332 of 3428 solved functions have that order; the smallest example declares nothing at all). Recovered a park at 2 of 74 to exact. SYMBOL BASES: gcc spends a callee-saved register for a symbol address and derives from it, but never for an integer -- 73 differing to 3. A function with NO conditional branch has only the flag group; both naming levers need a dominating block |
| [batch-131](reports/batch-131.md) | 5 | ready to port — tools/pool.py replaces a candidate query rebuilt inline nine times; its flag2 column predicts CSE_CFLAGS before any C is written and got all three right. Park exclusion must be by function NAME: overlay functions share low addresses, so one park was hiding an unrelated candidate. Name a negated constant as -1, never x = 1; x = -x; -- the two-step is a computed value gcc keeps in a callee-saved register (66 of 67 differing) |
| [batch-132](reports/batch-132.md) | 6 | ready to port — tryc CANNOT SEE a Makefile wildcard: a new .c has no path, so a screen can be exact at -O2 while the build uses -O1 (18-byte overlay diff). pool.py now prints the wildcard column; 12 of 1152 files are affected. Adding a flag rule does NOT rebuild the .o -- delete it or the correct rule looks like a failed diagnosis. CORRECTS batch 130: the no-prototype lever is NOT ruled out by a callee appearing in two arms. Name cheap constants, leave POOLED ones inline (naming four cost three pushes) |
| [batch-133](reports/batch-133.md) | 6 | ready to port — run twin_families.py, not just solved_twins.py: 41 families cover 90 remaining functions and 13 have NO parked member, each a two-for-one (two of this batch's pairs were 2 and 4 differing lines). ldmia/stmia into the stack arg area = a struct passed BY VALUE. The interleave lever also moves POOLED arguments when named in the dominating block. The HImode-literal rule is not one rule -- one pointer wanted a pooled constant at one operation and a mov at the next |
| [batch-134](reports/batch-134.md) | 6 | ready to port — LEVERS COMPOSE: one function took four stacked spellings, 70 differing to 0, and the 9->8 step only looked dead until the FIRST DIFFERING LINE moved 49->65; read position, not total. A lever can be insufficient rather than inapplicable -- two namings did nothing alone and were exact together. Two spilled stack arguments want two named locals. The interleave lever's dominating-block precondition screened both ways in one function: inside the guarded arm changes nothing. CORRECTS batch 127/131: the commoned-constant tell diagnoses the cause but does NOT promise a fix -- both documented remedies fail on OvlFunc_881_2009c08, and the branch-vs-block guess is contradicted. CORRECTS batch 133: the symbol-base lever only works while the offset FITS the addressing mode (31/62/124); above that it is a park. pool.py gained a reuse column, validated against six recorded outcomes, which flagged 4 of 6 dense candidates as traps before any C was written |
| [batch-135](reports/batch-135.md) | 5 | ready to port — THE INTERLEAVE LEVER MOVES THE ARGUMENT YOU DO NOT NAME: recovered a park whose note said "NEXT: nothing"; it had tried naming the interleaved argument itself. Worth re-attacking only where pool.py shows site>0 AND unguarded==0 (not "has a branch") -- that filter picked 4 correctly. "r0 in the middle" and "r0 at the end" are DIFFERENT problems: name the other arguments vs drop the prototype, and one function can want both. The DIVISION HELPER NAMES THE SIGNEDNESS (divsi3/modsi3 signed, udivsi3/umodsi3 unsigned) -- one declaration took a screen from 55 differing to 17; in overlays __umodsi3 and _umodsi3_RAM are the same symbol via overlay.ld. CORRECTS batch 127/131: the commoned-constant tell does NOT promise a fix, two counterexamples where both remedies fail. CORRECTS batch 133: the no-prototype lever does not always trade one site for another. TOOLING: pool.py was leaking 169 of 253 parks back as fresh candidates (five header formats, one matched) and one function was re-investigated from scratch a day after being parked; five duplicate parks merged. tools/remaining.py added -- the published count was a hand-decremented counter that had drifted 46 low. REFUTED: mid-function pools are not a TU property, so the cluster experiment is dead |
| [batch-136](reports/batch-136.md) | 8 | ready to port — A BLOCKER CLASS WAS A MISSING TYPE: OvlFunc_932_200a5c0 sat at 2 differing under the "commutative register-role swap" (21 parks, seven spellings, four flag groups, two rounds) and matched instantly as p->flags |= 2 instead of pointer arithmetic; Func_80167ac fell the same way from 14 differing AND two instructions short.  73 parks reach memory raw; 2 of 3 screened so far recovered, so type-screen any park whose note says scheduling or allocation -- the diff cannot tell that apart from a wrong type. MEASURED CEILING: tools/poolblocked.py finds 312 of 2239 remaining functions (13.9%) jumping over their own literal pool, which old_agbcc cannot emit -- unreachable, and they float to the TOP of the dense queue. The interleave lever scaled to SIX sites in one pass, covering both arms of a nested branch. "r0 in the middle" and "r0 at the end" need opposite fixes and one function wanted both. A sub sp difference is the local aggregate's SIZE (ROM frame minus outgoing args), never the body. The source-order lever does not scale past two values. A twin's spelling transfers only under an equal register budget |
| [batch-137](reports/batch-137.md) | 5 | ready to port — NAME THE OFFSET, NOT THE BASE: reading a global at a fixed offset, naming the base lets gcc hoist it across a call (adds a push), leaving it inline lets gcc fold =gState+450 (three instructions SHORT), naming the OFFSET does neither and is exact.  PRECONDITION, learned by getting it wrong: the fold has to be COSTING something -- check the inline form comes out short first, which OvlFunc_955_2009424 does not.  An int intermediate for a NEGATED MASK keeps gcc from truncating it to a byte constant; worth 33 and 53 lines on two functions, and it shows in the LENGTH before the diff.  Add a umodsi3/divsi3 linker alias when it is the ONLY thing left (completed a match here) and defer it when it is not.  split_s.py refuses a .s holding one function plus data and is right to -- hand-split into _b text and _c data, both listed in both sections.  GENERAL: what transfers between functions is the REASON a lever worked, not the shape of the diff -- two overgeneralisations caught this batch |
| [batch-138](reports/batch-138.md) | 5 | ready to port — A LINKER ALIAS WAS A WHOLE CLASS: OvlFunc_970_20080b0 screened at ONE differing instruction that was a missing `__udivsi3 = _udivsi3_RAM;`, and 26 overlay scripts were missing 35 aliases between them; all added, safe because each overlay's own imports.s exports the matching stub, and the ROM is byte-identical so they provably emit no bytes. Batch 135 had the alias as a per-function fix; the change here is doing the whole tree at once. AGAINST MYSELF: three rounds re-derived the ARGUMENT PRECOMPUTE class that this file has diagnosed all along (calls.c:805 hoists any argument with rtx_cost>2; arm.c:2042 makes a Thumb shift cost 4), and I invented a rival "a callee-saved register must already be committed" theory and wrote it into two parks as a finding. OvlFunc_926_200a68c falsifies it -- it commits r5/r6 and the lever still fails. Check the diagnosed-class sections BEFORE screening. actor.h carried two elevations outright. structmap.py found Actor932/935/948/949 were four names for one object (14 files, ROM unchanged). unparked_candidates.py leaked twice -- by header format, then by matching only Func_/OvlFunc_ shapes, which missed every ROM-named function. Park integrity: only 24 of 322 parks have no C anywhere, not the 109 a first reading suggested |
| [batch-139](reports/batch-139.md) | 6 | ready to port — A DOCUMENTED FLOOR WAS NOT A FLOOR: three parks and docs/elevation.md all recorded that gcc canonicalises every signed lower bound to `cmp #(K-1) / ble` and called it a 2-line floor not worth another round. Every spelling tried had been an EXPRESSION; a SWITCH lowers through a path that emits `cmp #K / blt` directly. The counterexample was already in the tree -- 15 sites where matching C emits it, 14 of them `cmp #0` which cannot canonicalise, and one `cmp #31` from a switch. Six elevated, three parks deleted; Func_80a3ce4, the function used to STATE the floor, also screens OK and is held only by a shared .s. Scope narrowed honestly: 242 sites in 158 functions is where it CAN apply, but only 1 of 12 lower-bound parks was really on this floor and only 4 of the 158 are unparked single-function files. split_s.py freed two matching functions from a .s held by one unreachable one, verified byte-neutral BEFORE any .c. COUNTEREXAMPLE RECORDED: OvlFunc_926_2008388 meets both stated preconditions of the pool-constant CSE remedy and no flag moves it, so that rule is necessary and not sufficient, and blocked_cse.py's 585 is a population not a worklist |
| [batch-140](reports/batch-140.md) | 5 | ready to port — A CONTROL-FLOW SHAPE MOVED REGISTER ALLOCATION: OvlFunc_968_2008098 went from 26 of 36 to EXACT by moving `return 0` from an early guard to the tail. Two differences collapsed at once -- gcc materialises the early zero above the compare that selects it, AND a redundant `mov r0, r5` vanished. That copy is textbook "elided copy", one of the three shapes this file's register-pressure section attributes to TU pressure rather than source form; here it was source form. So a difference resembling a pressure-residue shape is not automatically pressure residue -- check control flow first, it is cheap. actor.h carried three first-screen matches with no pointer arithmetic; a negated mask needs an int intermediate in all of them. NEW CLASS: Func_80b0840, two DMA3 transfers sharing a source, where DMA3_SET's pinned r0 is already correct so gcc emits nothing and the stream is one SHORT -- distinct from the double-DMA functions that match, which have different sources. A stream that is N lines SHORT means a missing register class: OvlFunc_957_2008f10 was nine short until two constants were named live across the call |
| [batch-141](reports/batch-141.md) | 5 | ready to port — A 521-FUNCTION "TOOLCHAIN CEILING" WAS NOT ONE: poolblocked.py justified the class with "old_agbcc emits the pool at .func_end", but old_agbcc is not the compiler (gcc296 is) and gcc296 DOES emit mid-function pools -- 64 already-matching functions have one; it writes `.word` tables at its own labels and never the `.pool` directive, which is why searching for the directive read as proof. Five elevated. MEASURED: of 521, 198 also precompute, 43 also const-remat, 3 ARM, leaving 277 pool-only -- mostly large, 176 over 120 insns, small end now exhausted. LEVERS: pool ENTRY ORDER moves with an int intermediate; a halfword LITERAL is pooled and naming it defeats that (opposite directions, easy to confuse); CLOBBER the offset after taking the address to force an explicit address, FINISH the offset before the base to get register-offset -- visible in the store before screening; which operand has POINTER TYPE decides the addressing base, not the order of the addition. CORRECTIONS: a "dead callee-saved register" in UIDrawText was a signedness error and matched at 45/45 with unsigned params; and this file's recorded lead that gcc emits the argument interleave from f3(0xe,0x102,0x204) is FALSE -- eleven probes, all identical |
| [batch-142](reports/batch-142.md) | 6 | ready to port — NAMING A VALUE IS A LEVER THAT POINTS THREE WAYS: giving an expression its own local FIXED GetNumDjinn (stops gcc reassociating u[(0x8c<<1)+which] into (u+which)+0x118; source operand order is irrelevant, only the name moves it), took CheckEquipmentCritBoost from 2 differing to 47 (naming the mask earns it a callee-saved register, pulls r10 into the frame), and did NOTHING on Actor_SetAnimAndSpeed. Hypothesis from 3 points: naming helps when it blocks a reassociation, hurts when it promotes a value gcc would rematerialise. DUPLICATED CODE IS DELIBERATELY ASYMMETRIC: Func_8079e9c's two identical-looking loops increment p AFTER the compare in one branch and BEFORE it in the other -- writing them the same way cannot match; Actor_SetAnimAndSpeed's argument order tracks the CALLEE (every SetAnim sets r1 first, every SetAnimSpeed r0 first). READ THE IDIOM: `if (x<0) x+=0x1fffff; x>>=21` is gcc's signed DIVISION by 2^21, not a rounding fixup -- the source is v[0]/0x200000 and 0x1fffff never appears; a separate lsl#16/asr#16 after ldrh/add/strh is the signature of `(*b)++`. CONFIRMED TWICE: a halfword-stored accumulator still wants int (short forces sign-extension the ROM lacks); a global's offset must be built at RUNTIME via a local pointer or gcc folds it into one pooled ldr =sym+off. CORRECTION: rom_b0000/80b0a20.c was closed on 'zero .s files contain a mid-function pool' -- the search looked for the .pool DIRECTIVE, which gcc never writes, so it could only return zero; 64 matching functions have one. tools/park_status.py now parses the corpus properly: 25 parks at <=2 differing, 26 with no usable C |
| [batch-143](reports/batch-143.md) | 6 | ready to port — A GLOBAL POLLED WITH NO INTERVENING CALL MUST BE `volatile`: gKeyHeld read five times in a row CSEs to one load and costs NINE instructions; the declaration already existed in rom_e3958_c_c_c_b.c and had never been carried to the files that poll it. COUNTED the scope rather than guessing: 12 symbols are volatile somewhere, gKeyHeld is plain in 8 files and iwram_3001e40 in 52 -- screening the iwram_3001e40 parks unlocked OvlFunc_933_2008344 outright (its park named a different blocker) and halved Func_80b86ec. BOUND: swept all 47 parks recording they are SHORTER than the ROM, all globals volatile, NONE improved -- volatile matters only with no intervening call, since a call already forces the reload. ONE BASIC BLOCK: `if (x != 0) break; f();` is 47 differing, `if (x == 0) f(); else break;` is an exact match -- same shape as batch 142's early-return, and its LIMIT is measured here: rewriting VOID return guards that way changes nothing, there is no value to hoist. COPYING PARAMS TO LOCALS took Func_8093168 from 56 differing to 4 and is NOT general -- no change on two functions, 2 differing to 46 on a third whose parameter must stay an int. MEASURED UNREACHABLE (**WRONG -- corrected in batch 145: a named bound or a ternary both produce `cmp #K / bge`; Func_8093168 is now elevated**). ALSO: gcc allocates stack locals in REVERSE declaration order; a register-offset pair off one base needs a MUTATED offset (`off += 4`), not t[0]/t[1]; counter init wants to come first but do NOT reorder two counters against each other; the naming lever blocks reassociation only when gcc CANNOT SEE the definition |
| [batch-144](reports/batch-144.md) | 6 | ready to port — THE NAMING LEVER RESOLVED: a name asks for a REGISTER, and the three contradictory cases separate by which CLASS. NAME STACK ARGUMENTS -- they get a low register and a store, which is the ROM's shape -- but PER CALL SITE: one shared pair across three six-argument calls is 20 differing, separate pairs are 2. DO NOT NAME ZEROS -- a named zero is promoted to CALLEE-SAVED, costing push/mov/pop; gcc rematerialises `mov r3, #0` at each use and passing bare literals matched OvlFunc_926_200a5b8 exactly. NAME A MASK that would otherwise be narrowed (a byte destination lets gcc truncate -13 to 0xf3) -- the mask, not the value being masked. BOTH ARMS END THE SAME WAY: write the expression TWICE and let gcc tail-merge; hoisting it into a local used once after the join costs an instruction, because a named variable spanning the merge forces a copy out of the register the arms left it in. THREE OF SIX MATCHED ON THE FIRST SCREEN by applying batch-143 findings before writing rather than after failing. IDIOMS: x*3 then *17 then *257 is gcc's synthesis of *13107, write the multiply; `lsl #3 / sub` is *7; `if (x<0) x+=0xfffff` before asr #20 is signed division by 0x100000. NEGATIVE, so it is not repeated: the mask-narrowing sweep over the park corpus found nothing -- only two parks carry such a mask and both are int context, where naming makes one WORSE (22 to 30). PARKED: OvlFunc_948_2009ac8 is three short and they are push{r6}/mov r6,#0/pop{r6} -- the same allocator rule that DELIVERED OvlFunc_926_200a5b8, blocking instead of helping; with Func_80b0070 |
| [batch-145](reports/batch-145.md) | 21 | ready to port — CORRECTION FIRST: batch 143's "cmp #nonzero / bge is unreachable from an if" was WRONG. The corpus count was real (once in 3205 generated .s) but a count says what the tree CONTAINS, not what the compiler can EMIT -- and every function in it was written by someone who had already concluded the shape was out of reach. A nine-way probe shows `x < 8 ? 8 : x` AND a named bound (`int k = 8; if (x < k)`) both give `cmp #8 / bge`; the rewrite applies to an if-statement comparison but not a conditional expression's and not through a local. Func_8093168, parked at 4 of 57 on exactly this, is now elevated -- name the bound. THE SCHEDULER-INTERLEAVE WALL HAS A KEY: nine spellings and four flags inert at 2 differing, and declaring the object a struct so a halfword read is a TYPED FIELD fixed it, though both emit the identical ldrh -- sched2 sees the alias set, not the instruction. TOO-BROAD RULE FIXED: a pool constant rebuilt in a loop is NOT proof of goto -- reload rematerialises under register pressure and looks identical; Func_8021a18 scored 71 differing with goto loops and 4 with plain for loops. The goto lever's cheaper signature is that it defeats check_dbra_loop -- a ROM loop counting UP whose counter has no use but the exit test. ALSO: `ldrb / lsl #24 / cmp` is a volatile signed char (12 spellings probed); two separate `return -1;` ALWAYS merge, so a constant materialised at two exits means one variable; an unused local array reproduces a bare `sub sp,#N`; a struct member vs a pointer subscript flip a byte STORE's address/value order (not loads); naming is not boolean -- the statement POSITION of the assignment picks the register; declaration order and assignment order are separate levers. Six parked, two with the allocation read out of the compiler via -dg |
| *(between batches)* | 3 | THREE TARGETS CARRY FIFTY FUNCTIONS: tools/dupfuncs.py finds 101 of 2110 remaining functions are byte-identical after normalising symbols, labels and pool operands -- 27 groups, 74 free. OvlFunc_883_20080c4 (x18, parked at 7 of 176), OvlFunc_883_200834c (x17), OvlFunc_883_20088c0 (x15, length exact at 142/142). Members differ only in which data tables they name. ALSO: tools/whodoesthis.py searches GENERATED asm for a residue shape and prints the C that made it -- it corrected four wrong "unreachable" claims and closed three functions; a corpus count over REMAINING asm tells you what the authors wrote, over GENERATED asm what the compiler will do. AND: small functions are NOT the easy ones -- eleven rounds on 8-to-20 instruction functions produced no matches, because every lever here works through the register allocator and tiny functions have no pressure for it to act on. Prefer 40-120 instructions |
| [batch-146](reports/batch-146.md) | 8 | ready to port — THIRTEEN ROUNDS WITH NO ELEVATION, TURNED INTO A FILTER. Every one of those rounds parked a function 1-7 instructions out with its spellings scored; read together they are five measured conditions, now in tools/pickable.py: reject under 40 insns (every lever works through the allocator and a tiny function gives it nothing to act on -- four parks prove it), any use of r8-r11 (allocation priority, read out of gcc with -dg), a REPEATED expensive constant (cse if the uses are close, PRE HOISTING if one dominates -- probed in five variants: gcc rebuilds when every use is in a branch no other dominates and hoists to the top as soon as one does, regardless of distance or intervening calls), over 120 insns, or fewer than 8 calls. 104 of the remaining functions pass. The first candidate matched after one lever; the NEXT FOUR MATCHED ON THE FIRST SCREEN WITH NO LEVER. The levers were never the bottleneck -- picking functions that fail for reasons no lever reaches was. THE FILTER CAUGHT A BUG IN ITSELF: its first constant detector required mov and lsl ADJACENT and so admitted the very function whose hoisting motivated it; fixed, the list drops 151 -> 104. ALSO: tools/whodoesthis.py (search GENERATED asm for a residue, read the C that made it -- closed three functions, corrected four wrong "unreachable" claims) and tools/dupfuncs.py (101 remaining functions are duplicates; 3 groups carry FIFTY) |
| [batch-147](reports/batch-147.md) | 8 | ready to port — THE LEVER WAS A MISSING DECLARATION AND IT WAS ALREADY WRITTEN DOWN. `mov r0` at the END of a call's argument setup comes from calling through C's implicit `int f()` -- NO declaration, which is NOT the same as an empty parameter list (`extern void f();` screened on three functions here and moved nothing). OvlFunc_954_20095e0 recorded this in an earlier batch as a fact about itself; it is a fact about the compiler, and generalising it closed four functions in one round against 69 parks that describe some version of the residue. THE FIX IS NOT ALWAYS AT THE CALL THAT SHOWS IT: on OvlFunc_952_20085a4 the park named __ActorMessage exactly and correctly, and the lever was the prototype of the call immediately BEFORE the branch. AND THE DECLARATION IS PER-CALL-SITE: when a callee is called twice and only one site is wrong, changing the one declaration moves both -- give the odd site a `__asm__("name")` alias with a different RETURN TYPE. A void-returning alias for the result-discarded call elevated OvlFunc_971_20091bc and its twin, which the park had called unreachable for exactly the right reason. tools/protolever.py does the per-declaration greedy hill-climb; deleting all prototypes at once made 37 of 62 saved candidates worse and only 4 better. I PARKED A FUNCTION THIS ROUND AND WAS WRONG WITHIN THE HOUR: seven spellings of OvlFunc_955_20092f0's calls left its count EXACTLY unchanged and I read that as proof the residue was below the source -- invariance under call-site rewrites says WHERE the lever is, not that there is none. TWO TOOLS WERE LYING: pickable.py re-served already-parked functions (it ranks on assembly, so a park scores as well as a fresh function -- two of this round's first three picks were parks I re-derived from scratch; 102 candidates -> 64), and tryc.py took the UNION of every matching Makefile rule's flags where make gives explicit rules precedence, so three functions screened at -O1 with 40 differing when they byte-match at -O2. Its own WILDCARD hint was right both times. CORRECTION: OvlFunc_955_2009424's fourth callee-saved register is `-1` CSE'd across four uses of __Func_80933f8, not the gState base its park blamed -- that puts it in the constant_reuse class. _MSG_2352 added to message.sym, which its park had worked out and never cashed |
| [batch-148](reports/batch-148.md) | 5 | ready to port — THREE LEVERS THAT DECIDE A REGISTER. A NARROW STORE OF A LITERAL has three spellings and three answers: through a CAST gcc pools the constant sign-extended to the destination width; through a TYPED FIELD it builds it mov/lsl in a SCRATCH register; as a NAMED INT LOCAL it builds it mov/lsl but in a CALLEE-SAVED one. Prefer the typed field -- on OvlFunc_943_20097a0 the function has exactly two callee-saved registers and both are already claimed, so naming the two single-use constants would have taken registers they needed (37 differing -> 6). Reach for the named local only when the ROM's own PROLOGUE shows it spending a register there, which also refines "do not name zeros". The same lever fixed an unexplained mid-function literal pool on OvlFunc_881_200b57c: the pooled zero was one extra pool entry and one entry was enough to make gcc dump early and jump over it (31 -> 2). EXPLICIT GOTOS CONTROL BLOCK ORDER: an if/else-if chain puts each body inline behind a `bne` (99 differing on OvlFunc_927_20099b8) and a switch SORTS the cases into a balanced compare tree -- a `bgt` against a case value that is not a range bound is the tell; gotos took it to 37 in one edit. AND THE BRANCH SENSE INVERTS: `if (c) goto X; goto Y;` compiles to `b!c Y; b X`, so write `if (!c) goto Y; goto X;` to get the ROM's -- only for a conditional goto immediately followed by an unconditional one. `ldmia`/`stmia` INTO THE OUTGOING ARGUMENT AREA MEANS A STRUCT BY VALUE; six int arguments instead cost a third callee-saved register and a push the ROM lacks. The per-call-site declaration lever got three more confirmations. TOOLS: pickable.py now prints argument-interleave sites and rejects three-or-more `neg` (the -1 triple), 56 candidates -> 42. THE -1 TRIPLE NARROWED: OvlFunc_881_2009a98 has ONE such call and nothing else touching -1, so the reuse is NOT cross-site CSE as constant_reuse.c assumed. PARKS now stop at decisions made AFTER register allocation -- a reload rematerialising a zero as a POOL load (2008afc), gcc reusing `a * 60` where the ROM rebuilds `a * -60` (common1_fac), and OvlFunc_946_2009c84 at 77 of 77 lines with all eleven differing lines the same two values in swapped registers, a clean test case for the REG_ALLOC_ORDER hypothesis. PROCESS: the _TBL_L* linker aliases already existed for the `.LN` extern hazard -- grep the doc before editing shared asm |
| [batch-149](reports/batch-149.md) | 5 | ready to port — READING THE REGISTER CLASS OFF THE ASSEMBLY. The existing six-argument rule said each call site needs its own pair of stack-argument locals but not WHICH sites need one; the `str` operands answer it directly -- a `str` from a register the ROM already HOLDS means a shared local, two `mov`s into separate registers before either store mean that site wants its own fresh pair. Written as literals gcc reuses one register and interleaves the stores, one register short every time. OvlFunc_941_20080d4 needs both answers (6 differing -> exact) and OvlFunc_922_2009050 is seven sites, one shared and five pairs, matched on the first screen. AND THE STATEMENT POSITION PICKS THE REGISTER CLASS: a named stack argument lands in a callee-saved register only if its live range CROSSES A CALL. OvlFunc_927_2009454's trailing zero gets a scratch register beside its call and r5 when the assignment spans the preceding one; on the parked Func_8020198 that one move is 17 differing -> 2, because the ROM spends a THIRD callee-saved register and everything else was the displacement cascading. One crossing is enough -- moving both is worse. A PARK THAT WAS WRONG ABOUT ITSELF: OvlFunc_901_2008350 was parked on register pressure with the note asserting "the structure is believed right and is not the problem". The guard was the contrapositive of the ROM's, arms swapped, and the r8-r11 allocation followed from that; written the ROM's way it matches exactly. tools/solved_twins.py found it in seconds -- OvlFunc_898_2009674 in another overlay is the same function, already elevated. RUN solved_twins.py OVER THE PARKS, not just fresh candidates: a park naming an allocator blocker has usually had no second opinion to test its structure against. CORRECTION TO BATCH 148, from its own advice: "gotos control block order" has a boundary. Five goto arrangements on OvlFunc_971_2008e10 all lost to the plain for(;;)/break (41, 40, 79, 35 against 29), because a two-instruction block reached by `goto` is DUPLICATED INLINE unless it sits adjacent to the branch -- move the label to the ROM's position and gcc copies the body to the branch site instead of jumping to it. Try the structured form and read its diff first. NEW CLASS: gcc-2.96 here does NOT cross-jump identical call tails differing only in a pooled constant, where the original build did. OvlFunc_common1_4cc and OvlFunc_971_2008e10 both park on it -- three separate calls is two lines too many, an id assigned per arm with one shared call gets IF-CONVERTED to four lines too few, and gotos collapse identically. Deserves a flag sweep. pickable.py now reads park file CONTENTS (a class park is named for the class and lists members inside; message_base_register.c parks two functions whose addresses appear in no filename) and rejects three-or-more `neg`, the -1 triple |
| [batch-150](reports/batch-150.md) | 5 | ready to port — THE ASSEMBLY NAMES THE TYPE. thumb has `ldrh rD, [rB, #imm]` but NO `ldrsh` with an immediate, so a signed halfword field must build its offset in a register: an immediate-offset `ldrh` is an UNSIGNED field, a register-offset `ldrsh` is a SIGNED one. Declaring a node flag `short` instead of `unsigned short` cost Func_801b148 58 differing lines -- one character of the struct -- and a register-offset `ldrsh` is not a scheduling accident to hunt a lever for, it is the only encoding available. Cheapest type check in the corpus. EVERY STACK SLOT NEEDS ITS OWN LOCAL, AND IT IS ALL-OR-NOTHING. OvlFunc_888_20084e8 makes an ELEVEN-argument call (four registers, SEVEN stack words); written as literals gcc reuses ONE register for all seven and never spends the callee-saved register the ROM pushes. Naming THREE of the seven is WORSE than naming none (28 differing against 22); all seven is 2. The register the extra locals compete for is the one that decides the prologue, so a partial naming leaves gcc short of exactly one and it reshuffles everything. OvlFunc_918_2008f58 then matched on the first screen with six six-argument calls decided entirely by the batch-149 `str`-operand reading. THE SCREEN IS NOT A VERDICT, AND THE FAILURE MODE IS SUBTLER THAN TRUSTING AN OK. OvlFunc_935_2008aa0 screened at three differing, one of which really was only the __umodsi3/_umodsi3_RAM linker alias (five elevated files link through that route). I installed it on "the only difference left is the alias" and make compare rejected the overlay: the reasoning about that ONE line was right, and I had stopped reading the other two. They were a statement swap -- `i = 0` must precede `t = 0xff << 16` because the ROM interleaves them, the counter's init landing inside the constant's two-instruction build. Discounting a line correctly and then not reading the rest is its own error. PARKS ESTABLISH TWO THINGS. The register coin flip has NO RULE TO ENCODE: OvlFunc_946_200a16c and 2009c84 are the same thirteen-line swap, thirteen spellings screened across the pair, and they DISAGREE about which value the ROM favours -- first-computed gets r5 on one and r6 on the other, and we produce the opposite of the ROM in both. And the shared-call-tail class has a third instance (2009d2c) plus a BOUNDARY: 200a16c has four arms branching into one call and is NOT an instance, because the arms set two argument registers rather than one value gcc can hoist above the compare. solved_twins.py is now exhausted (zero remaining functions have a solved twin). A detector for the shared-call-tail class was built and DISCARDED -- it caught one of three known instances, and a signal that weak reads "clean" on functions that are not |
| [batch-151](reports/batch-151.md) | 5 | ready to port — THE COMPILER IS NOT DETERMINISTIC. gcc-2.96's optimiser depends on the process address layout: the same file, same command, same container gave 6 divergent outputs in 120 runs (a CSE decision -- whether to reuse a register already holding a pooled constant), and 0 in 120 under `setarch -R`. This batch's clean build failed on it, two bytes in the whole ROM, and it reads EXACTLY like a fake match -- a .c committed beside a .s it does not reproduce. It was not; recompiling gave the committed output. THREE CONSEQUENCES: a `make compare` failure is not automatically a decompilation error, so rebuild the object and re-diff BEFORE investigating; `git status` after a build is a free integrity check, because every generated .s is tracked and a divergent compile shows up as a modified file naming the exact object; and any function parked or reverted on the strength of a single clean-build diff deserves re-testing. `setarch -R` fixes it completely but needs `docker run --privileged`. THE `push {r4` SWEEP WAS RUN AND IS A NEGATIVE: 16 functions push r4, ZERO are parked, so the flag explains none of the register-allocation class -- recorded in the doc so it is not run a third time. It paid off elsewhere: the 16 cluster, eight in common2, so common2 (already a non-interwork TU) was ALSO built without -fcall-used-r4 and its eight r4-pushing functions were unreachable from C by construction. COMMON2_CFLAGS now substitutes -fcall-saved-r4, VERIFIED not assumed -- all nine existing common2_c*.c compile byte-identical under both flags, so the flip is a no-op for the matched corpus. Compiling every file a rule already covers under both settings is seconds of work and is the pattern to copy before touching any flag group. CONTIGUOUS STACK LOCALS MAY BE ONE STRUCT: naming pointers to both stack objects leaves exactly one store wrong, because gcc folds the OFFSET-0 store back to `sp` while keeping the pointer for offset 4 -- unfixable by more pointer naming, since at offset 0 the pointer and sp are the same value. OvlFunc_common2_380 matched once the two locals became one struct; tell is `sub sp, #N` equal to the sum with no padding. This NARROWS the stack-vector pointer rule. A 64-BIT RESULT WANTS A UNION WRITTEN AFTER THE JOIN: assigned inside both arms gcc threads each arm to the epilogue and the join vanishes; built as `((u64)hi << 32) | lo` it costs five instructions the ROM does not spend. That took OvlFunc_common2_41c to 27 lines against 27 with identical block order and branch senses, leaving 18 lines that are ALL register renames -- the cleanest specimen of the coin flip in the corpus, since nothing else differs at all. Also: `v >> n` on a 64-bit value with variable n emits a call to __lshrdi3, so a ROM doing it inline did the word arithmetic by hand |
| [batch-152](reports/batch-152.md) | 5 | ready to port — SCREEN FOR A SHAPE FAMILY, NOT FOR ONE FUNCTION. This batch contains a round that produced NO elevation at all -- two functions worked deeply, both parked. The next round looked down pickable.py for several entries with the same size, call count and interleave score in the SAME overlay, found five OvlFunc_946_* with four in one split chunk, and solving the shape once produced FOUR elevations, three on the first screen. Better return than any amount of spelling-search on one function, and repeatable -- the tool already prints what is needed to spot a family. THE ONE THING THAT MATTERED: PUT THE CALL IN EVERY ARM. These dispatchers branch to a shared tail holding `mov r2, #0` and the `bl` (and the `neg`, where the constant is negative). That reads like one call after a join, and writing it that way is NINE INSTRUCTIONS SHORT, because gcc then materialises the common first argument once instead of per arm. It is gcc CROSS-JUMPING identical call tails -- the same mechanism as the shared-call-tail parks seen from the other side, where gcc FAILS to cross-jump where the ROM did. Either way, write the call in the arms and let the compiler decide. 66 differing of 80 to 13, and the siblings written that way matched outright. COMMON2_A IS THE SAME TU as common2_c, so the Makefile pattern widens from common2_c% to common2_%; it carries both tells, the non-interwork `pop {pc}` and a `push {r4}`. common2_28c took three shapes to land: the two 8-byte operands are SUBOBJECTS of one struct with a pointer local to EACH (batch 151's rule needed correcting -- one pointer at the whole struct gives the right addresses via the wrong instructions); pointer BIRTH ORDER decides register names, one misplaced assignment swapped r5/r6 through the entire body; and the frame is laid out by DECLARATION ORDER, LAST-DECLARED LOWEST, so read the ROM's `add rN, sp, #imm` offsets, sort, and declare in reverse. THE COIN FLIP LIVES IN THE SLACK: of four functions sharing one source shape, the three spending THREE callee-saved registers match byte for byte and the one spending TWO is a pure r5/r6 rename. Before parking a two-register rename, check whether a sibling with more live values matches from the same source. THE BASIC-BLOCK LEVER HAS A BOUNDARY AND AN INVERSE: it needs a BRANCH to cross and a call does not create one, so a straight-line cutscene script (200dca4, eleven calls, no condition) is unreachable by it -- and those scripts are exactly the shape that produces interleaves. The inverse: where the ROM builds a two-instruction constant CONTIGUOUSLY and gcc splits it, naming the value in a local in the SAME block fixes it (both StartTask sites on Anim_UnleashIntro, 6 to 2). AND A WRONG JUMP TABLE CAN LOOK LIKE A REGISTER PROBLEM: four labelled cases plus a default gives a comparison chain, not a table (59 of 80); adding the redundant `case 4:` produces the table, takes it to 6, and FIXES AN r5/r6 SWAP. Count the ROM's jump-table entries and write out that many cases before concluding anything about registers. Also: -mno-thumb-interwork cancels interwork and a later -fcall-saved-r4 overrides -fcall-used-r4, so any common2 function screens correctly from a scratch path |
| [batch-153](reports/batch-153.md) | 10 | ready to port — TWO DOCUMENTATION FAULTS THAT COST REAL FUNCTIONS, both fixed. (1) THE COMPILER IS gcc-2.96, NOT agbcc. docs/matching.md opened with "Is agbcc the right compiler? Yes." -- flatly wrong, contradicted by the README, and first in name order so it is what a reader hits. agbcc/old_agbcc build FIVE objects in the whole tree, all prebuilt Nintendo library code under src/lib/m4a and src/lib/agb_flash. docs/elevation.md now opens with a banner stating this and the rule that follows: NEVER explain an elevation blocker in terms of agbcc or old_agbcc. (2) THE BRANCH-OVER-POOL "CEILING" IS NOT A CEILING and the stale claim was still stated in two places with its refutation in a third, wrong version first. Cost this round: an agent SKIPPED StartSnow WITHOUT WRITING A LINE OF C, quoting the stale text. gcc-2.96 emits mid-function pools readily (64 in already-matching code) and the shape is `.word`, not `.pool`. Several hundred functions have been sitting in the census behind a ceiling that is not there -- SCREEN THEM. A FOUR-AGENT PASS, twelve functions, screening only and re-verified here before installing; seven matched. Levers that transfer: A SINGLE EXIT BEATS FIVE RETURNS (five `return 0;` -> one `goto out;` was 152 -> 98 differing, the batch's largest single win); ONE VARIABLE AS BOTH LOOP COUNTER AND TABLE BYTE-OFFSET (a separate offset variable costs a `mov` and misaligns the loop, 101 -> 57 on the 15-copy 20088c0 group); TABLE TYPING `int[][4]` not `unsigned char[]` with byte offsets (byte offsets make gcc fold to `=.L+4` and SUBTRACT to reach element zero); A STORE FORCING A RELOAD (the order of two stores decides whether an intervening write kills the CSE); LOOP SHAPE (subscript, not walking pointer -- the pointer form hoists its preheader above the guard, 26 differing); and `(short)` CASTS ARE NOT `int` INTERMEDIATES (`int d = (short)(x)` loses the lsl/asr #16 when the operand came from an unsigned halfword). PARKS: four more confirmed instances of the unguarded-interleave boundary (200cf44 2-of-28, 2009b1c 4-of-57, 200bdec 2-of-26, Anim_UnleashIntro) -- a function with no branch has no dominating block, stop spelling-searching them. Func_80b5864 is 56 differing by default and 4 under -fno-rerun-cse-after-loop, a THIRD candidate for CSE_CFLAGS, recorded not acted on. OvlFunc_907_2008fa0 MATCHES BYTE-EXACT but is not installed: its .s carries 21 .incbin blobs and 19 global labels the overlay references, so it needs a hand split of code from data. AND AN OPERATIONAL LESSON: I deleted that .s assuming it held only the function (my data check looked for `.word`/`.byte`; `.incbin` is neither) and broke the link. split_s.py ALREADY GUARDS THIS and refuses with an explanation -- I missed it because I piped the tool through `grep -E "^asm.*->"` for the success line. FILTERING A TOOL'S OUTPUT TO THE SHAPE YOU EXPECT THROWS AWAY THE CASE WHERE IT DISAGREES WITH YOU |
| [batch-154](reports/batch-154.md) | 11 | ready to port — TWO BLOCKER CLASSES RETIRED. (1) THE BRANCH-OVER-POOL SHAPE IS NOT A BLOCKER, settled empirically: gcc-2.96 emits the pool branches ITSELF at the ROM's positions. Decisive case is AnimEnd -- its two branch-over-pool sites were ABSENT from our output at 64 differing, then APPEARED SPONTANEOUSLY AND MATCHED once the instruction count converged. The branch is a consequence of CODE LENGTH, not a source construct; if a reference has one and you do not, you are the wrong length for another reason. SCREEN THE 516 NORMALLY. StartSnow is the cost of the old belief -- skipped last batch without a line of C, now 13 of 85. A park I wrote EARLIER IN THIS BATCH claimed the opposite (contents reproduce, placement does not, class splits by size) and is corrected in place. (2) FILE STRUCTURE IS A BLOCKER CLASS AND IS NOW MECHANICAL: 31 single-function .s files carry data as a trailing `.section .data` of .incbin blobs, and converting the file wholesale deletes it. split_s.py now splits code to _b and data to _c when no data sits AHEAD of the function; the cut at .func_end is unambiguous and the linker scripts ALREADY listed (.text) and (.data) on separate lines, so the machinery existed behind a blanket refusal. SIX of this batch's eleven came out of that class. LEVERS: a STRUCT POINTER is a register-allocation lever, not a readability choice (offset arithmetic pinned the base wrong through THIRTEEN spellings; the struct form was exact immediately -- check docs/structs.md before spending screens on offsets); the DECLARATION LEVER HAS A THIRD FORM, `void` vs `int` RETURN on the mismatching callee; THE MESSAGE BASE IS A SYMBOL, `(int)(&_MSG_xxx)` makes gcc spend a callee-saved register where a literal is constant-propagated -- this is the key to the whole message_base_register.c park class, which lists the literal forms as failed and never tried the symbol; a NARROW local flips an `orr` destination where an int local never does; `*q++ = v;` EMITS A SINGLE-REGISTER `stmia`, contradicting a park that claimed it appears nowhere in the corpus (verified by probe, worth 13 lines); and Thumb-1 gas does NOT fold `ldr rX, =imm8` into `mov`, so a pooled small constant is always a genuine symbol tell. LIKELY NEW CLASS -- ldrh/ldrsh CSE: the ROM reads one halfword twice, unsigned and signed, and gcc emits only the sign-extended load and derives the other; the merge is LEGAL so no spelling prevents it. Three instances named. Worth a find_shape.py sweep. ALSO: pickable.py's interleave count has a FALSE POSITIVE (regex binds the back-reference to r0), three of four flags in one worklist were spurious |
| [batch-155](reports/batch-155.md) | 10 | ready to port — FIRST SEVEN OUT OF THE 516-FUNCTION POOL CLASS, and it behaved exactly as batch 154 predicted: the pool shape caused trouble on NONE of them, gcc emits the `b` over the pool at the ROM's position unaided, and in two cases the ROM's pool word order came out right with no special handling. Where the pool did matter it was a readout, and the mechanism is now nameable -- OPERAND MODE CONTROLS POOL PLACEMENT VIA `pool_range`: a constant written as a BARE LITERAL into a halfword store is HImode (64-byte range) and forces an early dump, the SAME constant through an `int` local is SImode (1020-byte range) and the whole pool moves past the epilogue. A FOUR-BYTE .text DIFFERENCE WITH EVERY INSTRUCTION IDENTICAL IS A POOL-PLACEMENT DIFFERENCE -- now a recognisable signature. Reading the pool order off the hand-written .s BEFORE compiling is a cheap pre-compile test (Func_8097adc: 26 differing to zero on the first attempt). THE message_base_register PARK CLASS DID NOT EXIST: it listed `int base = 0xNNN` in every position as failed and concluded the allocator 'decides it is not worth it', but never tried `(int)(&_MSG_xxx)`, which works every time; three parks deleted, one already stale. LEVERS, each retiring a park's conclusion: DELETING a local won 80c1084 (the park tried the right form while KEEPING the extra local); `volatile` refuses a pointer fold a park called unrefusable; the offset must be declared INSIDE the loop body on 80b6e30 because move_movables puts a hoist LAST in the preheader while strength_reduce inserts closer to the loop; 8092a1c needed NO work, its parked C was already right and written off on the retired ceiling claim; and a residue can be DOWNSTREAM of a spelling elsewhere -- 2008200's 18 lines looked like a head register swap and were entirely the tail's pointer locals. THREE CORRECTIONS TO OUR OWN TOOLING: (1) tryc CANNOT BE READ AT FACE VALUE on branch-over-pool functions -- gcc emits two labels at one address, the streams misalign, and Func_80b6e30 screens as 13 DIFFERING while being BYTE-IDENTICAL; use tryc for iteration and `make compare` for truth, nothing in between. (2) An ad-hoc byte-comparison script written to work around (1) was ITSELF wrong and is deleted -- it assembled functions outside their file context and called three correct functions different. (3) scratch/ was committed by mistake in two commits using `git add -A ... scratch`, sweeping 388 files into history; both scratch/ and toDelete/ are ignored now |
| [batch-156](reports/batch-156.md) | 5 | ready to port — THE SPLIT-CONSTANT INTERLEAVE IS REACHABLE, FOR LIVE LOCALS ONLY. Splitting a two-instruction constant build across two SOURCE statements with the independent work between them (`flag = 0x80; i = 0; flag <<= 1;`) produces the ROM's interleave and landed Func_80b9a70. It then failed everywhere else for one reason: gcc REMATERIALISES ARGUMENT TEMPORARIES during argument fill and discards the source's statement structure, so the lever only reaches constants that OUTLIVE their construction. Measured on 200dca4 (residue unchanged character for character) and on 2008fec, where FOUR spellings came back BYTE IDENTICAL. This re-explains a whole park class: those parks blamed a missing basic-block boundary and concluded straight-line cutscene scripts were unreachable; the real discriminator is LIFETIME, and a branch would not have saved them. It is now PREDICTIVE -- OvlFunc_953_2008648's residue was predicted at 6 before writing any C (three sites x two lines) and came back 6 of 43, length exact; pickable.py now prints a >=2N floor. THE FILTER WAS REJECTING A CLASS THAT WORKS: its 'fewer than 5 calls' rule was written for 8-to-20 instruction bodies, but the SIZE rule already covers those, and the call rule was additionally hiding 51 LOOP-CARRYING functions in the 40-120 band -- exactly where the levers work, because a loop body is full of live locals. Four of five elevations came from that class; UpdateRespawnMap (51 insns, ZERO calls) matched on the first screen. SCREENING SIGNAL from eight worked functions: count values live across the body -- at ~three gcc's allocation is forced and matches, at four or more the ROM spends one more callee-saved register than gcc needs and no spelling argues it out (all four stalls were the same whole-function register rotation); use the ROM's push list as the proxy BEFORE writing C. A READING RULE THAT BECAME PREDICTIVE IN ONE ROUND: where the ROM COPIES a register before modifying it the original had TWO live names, in place means one -- derived on Func_80c0228 (bought 4 lines) and on its first application elsewhere it closed Func_8092980 outright, 23 differing to zero on one edit. TWO SELF-INFLICTED TOOLING FAULTS: a corpus scan returned a FALSE ZERO because .s files separate mnemonic from operands with a TAB and the regex used a literal space -- corrected, 3846 sites across 555 functions, the SECOND false zero from this cause; caught only because it contradicted a site read on screen minutes earlier. tryc renders BOTH columns through a disassembler so patterns lifted from its output exist in no file, and gcc's generated output uses tabs too (a docs claim that it was immune was wrong, disproved by one compile). AND tryc's count is unreadable when the pool sits inside the function -- Func_801fda8 screened as 6 DIFFERING while BYTE-IDENTICAL, gcc emitting two labels at one address; the artifact puts the divergence at the very END where it reads like an epilogue bug, so a residue that is only labels and only in the tail is the signature. PROCESS: four edits from one diff applied together took Func_8029274 from 12 differing to 25, reading as 'all four wrong' -- isolated, three were correct and one catastrophic. Change one thing at a time; its corollary is a lever, cast the COMPARISON not the loop when you need the ROM's signed bge |
| [batch-157](reports/batch-157.md) | 5 | ready to port — TWO FILTER REJECTS WERE DELETING CLASSES FROM VIEW. (1) r8-r11 IS NOT A WALL: of the matching TUs that use a high register, 202 are GENUINE matches and only 30 fakematches, so the reject -- generalised from three named functions -- was excluding a quarter of the corpus. Removing it took pickable.py from 5 CANDIDATES TO 125, and it explains why the duplicate-group backlog never moved: ALL TEN unparked groups use high registers, so the 72 functions following from 25 group solutions were invisible rather than hard. What actually puts a value in r8-r11 is just having more call-crossing locals than r5/r6/r7 hold. THIRD instance of a reject generalised from a handful of parks: 'fewer than 5 calls' previously hid the loop-body class that produced four elevations. Before trusting any reject, check whether MATCHING code already does the forbidden thing -- one grep answers it. (2) A HAND-WRITTEN SCAN RE-OFFERED AN ALREADY-PARKED FUNCTION, third instance: park files are named by ADDRESS and the ad-hoc filter matched the trailing component of the SYMBOL NAME, so Task_SpinCamera came back and its park was overwritten (restored; `git status` showing M not ?? caught it). 134 functions in asm/ have an address matching a park file. pickable.parked() already handles filenames AND park contents; a throwaway scan is still a candidate filter, so import it. NEW PRE-SCREEN -- UNREACHABLE COPIES: three functions stalled on a ROM `mov rX, rY` whose copy never diverges from its source, which no C local can produce because gcc coalesces it. Detect it by counting `mov rX, rY` where rY is never written again: only 17 of 51 candidates have none, it would have excluded both functions that blocked the prior round, and the first zero-call candidate it ranked matched in two screens. LEVERS: BOTH DIRECTIONS of address/offset naming exist -- naming the OFFSET restores `[base, index]` addressing, naming the ADDRESS forces a separate add, and the ROM decides which. A SINGLE `bhi` DOES NOT MAKE THE VARIABLE UNSIGNED -- declaring the parameter unsigned makes the products unsigned too, `/ 8` becomes `lsr #3`, and BOTH sign-correction sequences vanish (the whole eight-line shortfall on HeightTile_A); use `int` with the guard cast. COMMUTATIVE PAIRS COME OUT REVERSED, so rewrite the pair rather than reaching for a lever -- it closed Func_800eba0 in one edit. The offset declared INSIDE the loop body moves the preheader below the guard but does NOT stop strength_reduce creating an induction pointer -- different passes. gcc NORMALISES A DEAD-COUNTER LOOP TO COUNT DOWN, so a count-up loop whose counter is unused in the body means the counter was LIVE in the original. SIBLING LEVERS TRANSFER, ALLOCATION DOES NOT: the HeightTile family is three-for-three on register assignment with interpolation, divisions and branches all exact. THE DOMINANT BLOCKER IS REGISTER ASSIGNMENT -- eleven functions this batch -- and where an addressing-mode difference sits DOWNSTREAM of a wrong assignment, spelling the addressing mode costs lines rather than fixing it |
| [batch-158](reports/batch-158.md) | 5 | ready to port — SCREENING BEAT SAMPLING. The previous batch ended with four of seven rounds at zero or one elevation, all stalling on REGISTER ASSIGNMENT discovered after four screens; this batch predicts it from the disassembly first. THREE SCREENS, all applied before writing C: (1) PARAMETER SHUFFLING -- count `mov r<callee-saved>, r<0-3>` in the first ten instructions, every rotation-stalled function had one; (2) UNREACHABLE COPIES, now pure_copies() in tools/pickable.py -- count `mov rX, rY` where rY is NEVER WRITTEN AGAIN, a copy whose two names never diverge so gcc coalesces it and no C local can produce it (confirmed on four parked functions; only 17 of 51 low-call candidates score zero); (3) THE PUSH LIST -- if the ROM spends more callee-saved registers than a natural C version needs, addressing differences are downstream and no spelling closes them. With all three the pool is ~20 functions and FOUR OF FIVE ELEVATIONS MATCHED ON THE FIRST OR SECOND SCREEN. pure_copies() went into pickable.py rather than a scratch script, per batch 157's rule. LEVERS: NAME THE COMPLETE OFFSET, NOT A PARTIAL ONE -- the ROM folds the field displacement INTO the index, `s[i + 2]` gives the mirror image, and naming the partial offset is BYTE-IDENTICAL because gcc re-associates `s + off + 4` to `(s + off) + 4`; naming the whole displacement closed two functions, and operand order inside the offset expression does not matter. ITS BOUNDARY IS THE BASE FOLD: where the ROM holds the base across the body and pushes an extra register to keep several complete offsets live, gcc folds each base-plus-offset into a pointer and needs one register fewer -- the fold and the register spend are ONE decision, so the lever cannot ask for the more expensive form. THE OPERAND-MODE RULE HAS A THIRD GUISE: a value whose only consumer is a narrow store is canonicalised to whatever is cheapest for those bits -- seen as a pooled constant where the ROM has a mov, as a spurious pool load of zero, and now as the SIGN of a pooled word (ours =0xffffaf80 against the ROM's =0xaf80, same stored halfword, different pool word); an int local fixes all three. A `volatile` LOCAL REPRODUCES A STACK-RESIDENT VALUE -- a four-byte frame for one value re-read every iteration while r5/r6/r7 are pushed, so not register pressure; five lines short as a plain local, exact length as volatile -- STATED AS AN INFERENCE, since a spilled plain local gives the same shape. OPEN QUESTION WORTH NAMING: Func_801b9a8 and its twin Func_801b9ec sit one line short because the ROM POOL-LOADS 0x1f where the immediate form exists -- Thumb-1 gas will not fold `ldr rX, =imm8`, so it is a SYMBOL TELL; no .set in the tree evaluates to 0x1f and the form recurs in five other files, so naming that constant closes TWO functions |
| [batch-159](reports/batch-159.md) | 5 | ready to port — SIBLING TRANSFER BECAME A TOOL. Transferring a matching sibling's idiom has been the highest-yield move for several batches; tools/shapesib.py now finds the sibling. It reduces each function to its MNEMONIC SEQUENCE with operands dropped -- registers and constants are exactly what differs between siblings while the C idiom survives in the mnemonics -- and ranks by difflib ratio against the 1701 already-matching functions. OvlFunc_969_2008314 scored 0.958 and came in at 1 OF 69 ON THE FIRST SCREEN (the one difference a linker alias, not C); OvlFunc_939_2009240 scored 0.769 and came in at 1 of 40. ITS LIMIT IS MEASURED: Func_80a195c's best sibling scored 0.731 and was NOT a usable template -- near 0.7 the score stops meaning 'same idiom' and starts meaning 'same size and call pattern', so read the sibling before trusting the number. The WRONG proxy, tried first: ranking by how many matching .c files sit in the same DIRECTORY measures how well-worked a bank is, and both candidates picked that way stalled. TWO DETECTOR FAULTS while building it, both caught by a number contradicting something known: the corpus came out EMPTY because elevated functions live in gcc's OUTPUT format (`.type NAME,function`) and the parser only knew hand-written `.thumb_func_start`; then the sweep returned rows scoring 1.000 -- functions matching THEMSELVES -- because the candidate scan was reading generated assembly too. parked() WAS UNDER-EXCLUDING BY 84 ENTRIES: a park header names its function as `NAME -- 0xaddr` or `NAME -- asm/<path>.s` and only the first was matched, though 80 of 182 headers use the second; OvlFunc_common1_148 came back to the top the round after it was parked (556 -> 640 entries). THE COPY TELL HAS BOTH SIDES: reachable when the two names take DIFFERENT values later (Func_80a195c leaves the returned count in r0 through the pointer computation then copies it into the dead base's register -- one variable gives no copy, two matched outright), unreachable when it is a pure duplicate (four parks; pure_copies() in pickable.py detects it, only 17 of 51 candidates score zero). THE OPERAND-MODE FIX HAS A PLACEMENT RULE: routing a stored constant through an int local turns a pooled HImode literal into a `mov`, and the ROM decides where the assignment goes -- FIRST statement on OvlFunc_939_2009240 because the ROM's mov precedes the body, immediately before the store on Func_808fe38 where hoisting it costs 40 differences. It is not free: on OvlFunc_common1_148 every route through a local is WORSE than the pooled literal, because the value must be live across the store and there is no spare register. THE POOLED-CONSTANT SYMBOL TELL NEEDS 'SINGLE-USE': probed, three forms of `*p - 0x1f` all emit `sub r0, r0, #31`, but a REPEATED literal pools via CSE (a matching file masks with 0x1f three times and carries `.word 31`). AND READ THE DIFF LINE, NOT tryc's HINT -- it named `__modsi3`/`_modsi3_RAM` where the instructions were `__umodsi3`/`_umodsi3_RAM`, already aliased |
| [batch-160](reports/batch-160.md) | 5 | ready to port — THE HEADLINE IS A TOOLING FAULT. pickable.parked() was blind to 210 OF 467 PARK FILES. Four header conventions had been bolted onto its regex one at a time, each after a parked function was re-offered, and batch 159 recorded the fourth as closing the problem; auditing every park file at once instead of reacting to one miss showed it recognising 257 of 467. The tree also uses `NAME [path]`, `NAME -- NON-MATCHING.`, `NAME -- NOT MATCHING`, `NAME and NAME2 [path]`, headers whose first line is a bare `/*`, and class write-ups naming no function. THE COST IS MEASURED: the selection filter returned 21 candidates before the fix and 10 after, so roughly HALF of every candidate list for an unknown number of rounds was work already done -- invisible because a re-offered function looks exactly like a fresh one, and caught only because a candidate's residue turned out to be quoted verbatim in elevation.md as a previously measured attempt. FIXED BY NOT PARSING HEADERS AT ALL: parked() builds the universe of real function names from the .s corpus and looks for any of them in each park file's leading comment, plus any C function the file defines; a sixth convention cannot break it. 687 entries to 1143, audit now reports zero unrecognised. RULE: when a filter is patched repeatedly for the same class of miss, MEASURE ITS COVERAGE instead of adding another case -- one audit answered what four patches did not. SOLVED_TWINS.PY IS THE CHEAPEST ROUTE AND HAD NOT BEEN RUN FOR ROUNDS: it reported exactly two remaining functions with a solved twin and BOTH matched -- Func_80b86ec is byte-for-byte Func_80b9acc (template C compiled with nothing changed but the name, OK on the first screen), OvlFunc_881_200808c is OvlFunc_881_20080d4 with two immediates changed, two seds and one screen. Its single differing instruction was the documented overlay divide-alias FALSE NEGATIVE (`bl __divsi3` vs `_divsi3_RAM`, already aliased in overlay.ld); confirming a recorded false negative is worth as much as finding a new one, the alternative being a park for a function that already matched. UPDATEPOISON -- TWO LEVERS THAT LOOK LIKE ALTERNATIVES ARE NOT: a switch (three-way dispatch compares both cases before either body) plus building the gState offset took it 78 differing to 17; the remaining 17 read as two problems -- a register-role swap and the base materialising ABOVE the loop guard -- but were ONE cause, since a local assigned before the loop is born before the guard, lengthening its live range, and by global.c's priority formula a longer range loses the earlier register. Indexing directly and DELETING the local (`gState[(0xfc<<1)+i]`) matched outright: the index carries i so there is nothing to fold into `ldr =gState+504`, and the base is born in the preheader below the guard. So where the offset varies with an induction variable, indexing already stops the fold and the local is PURE COST; reach for the local only when the offset is constant. Negatives from the same function: assigning the base inside the loop body is much worse (79 lines to 87, reloaded every iteration), and declaration order is inert, three permutations byte-identical. CORRECTION: the 'duplicate-constant CSE' class written up earlier is NOT new -- it is the DOMINANCE rule already recorded (branch uses rebuild, a dominating use hoists); what the new parks add is that EXPENSE DOES NOT ENTER, OvlFunc_903_200843c hoisting a constant whose every use is a single pool load, saving nothing and costing four. AND THE NAMING LEVER HAS A PRECONDITION it had been stated without: the named value must not be FOLDABLE -- a runtime expression survived and closed Func_80175c0 26 to 18, while a constant and a symbol address both folded away to no effect. A link-time address counts as foldable. tools/filtered.py now carries the selection filter, which had been described in the docs, used once, and never committed |
| [batch-161](reports/batch-161.md) | 5 | ready to port — AN ELEVEN-FUNCTION PARK WAS A STALE CLAIM, and re-testing it produced the batch. ovl_7c460c/2008c74.c held eleven functions on one two-line residue with eleven failed spellings, three failed flags, and a corpus zero that a LATER BATCH HAD ALREADY FLAGGED as probably a tab-versus-space artifact and asked to be re-run with \s+; nobody had. Re-run over the 3336 SOLVED .s files the shape appears at 13 SITES IN 13 ALREADY-MATCHING FILES, and one of them is OvlFunc_965_2009030 -- A MEMBER OF THAT FAMILY'S OWN LIST, solved with the note never updated. Reading it gives the construct (name every split two-instruction build in the ENTRY BLOCK, which dominates the sites) and counting guards before the neg site settles the family: the solved member sits inside two nested guards and ALL TEN unsolved ones are straight-line. That also explains the park's one anomalous number -- naming the constant at the top scored 43 not 2 -- because with no guard to cross the local stays live and costs a register. THE LEVER WAS BEING APPLIED WITHOUT ITS PRECONDITION. So the family is not its own class; it is the straight-line half of the argument-interleave class, already sized at 98 and already known unreachable. Six of the eleven paths were also stale, so members must be resolved by NAME. RULE: a park listing members and a shared residue claims they are equivalent -- before adding a twelfth spelling, check whether any member has been solved since, and re-run any corpus result the park itself flags as doubtful. tools/guarded_interleave.py turns this into a selector (81-83 unparked functions have ONLY guarded sites) and FOUR of the five came from it, with OvlFunc_948_2008f40 and _2008fdc BOTH MATCHING ON THE FIRST SCREEN with no iteration -- their .s held exactly those two so the whole TU converted with no split. It counts mov+lsl and mov+neg alike; the filter written the round before implemented only the lsl example and let a neg case through, when the doc had already generalised the shape. TWO SHARPENINGS: a POOLED argument needs naming in the entry block too (OvlFunc_959_2008bec sat at 2 of 59 with every shifted and negated constant named, and adding e=0xe666 matched outright -- a pool load looks like it needs no help but participates in the same scheduling decision); and the per-call-site STACK-ARGUMENT rule must be PAID FOR -- on the parked OvlFunc_924_20096c4 it buys the post-loop call (16 to 13) then backfires inside the loop, one extra local free and a second forcing a spill at +5 lines, because it buys two registers at the call and the block must have them spare. A GLOBAL RE-READ ACROSS A BRANCH IS A VOLATILE TELL: OvlFunc_922_2009d78 reads iwram_3001e40 either side of its guard, gcc commons the loads, and `extern volatile int` matched outright; a pointer local for the address is inert alone AND still fails combined with volatile, a third confirmation that an address-only local is pure cost. MISDIAGNOSIS RECORDED: OvlFunc_968_2009150's read-modify-write looks like the constant-as-destination lever but the constant IS the destination in BOTH streams -- only the register differs, so it is the register-role swap; int m inert, unsigned char n inert, both together much worse. Check which operand is the destination in both streams first. That function also went 59 differing to 14 on consuming the call result instead of routing it through a named local, now three-for-three |
| [batch-162](reports/batch-162.md) | 5 | ready to port — THE GUARDED-INTERLEAVE SELECTOR IS NOW A ROUTINE. tools/guarded_interleave.py, written two batches ago out of a park correction, has produced nine elevations; four of this batch's five reached a match in at most two edits and TWO MATCHED ON THE FIRST SCREEN. The order worth trying: (1) name every constant whose uses are all AFTER a guard -- shifted builds, mov/neg builds AND pool loads -- since the entry block dominates the sites and gcc rematerialises across the guard; (2) do NOT name a flag id used by the guard itself, and screen with --no-rerun-cse from the start; (3) delete the callee's prototype when the residue is purely argument fill order; (4) delete any local that only holds an ADDRESS; (5) write the arms in the ROM's fall-through order. THE REFINEMENT THAT MADE STEP 2 PREDICTABLE: OvlFunc_923_200996c reached 7 differing with everything else exact and all seven were the flag id commoned into a callee-saved register, where NAMING IT AND THE BARE LITERAL ARE BYTE-IDENTICAL. The reason is not that flag ids are special -- the id's FIRST use is the __GetFlag call in the ENTRY BLOCK itself, so there is no guard between the assignment and the use and the entry-block mechanism has nothing to act on. THE DISCRIMINATOR IS THE POSITION OF THE FIRST USE RELATIVE TO THE GUARD, not the kind of constant: first used inside the guarded block rematerialises, first used in the entry block commons and needs CSE_CFLAGS. That prediction held on all three new functions -- each was screened with --no-rerun-cse first and each needed it: 29 differing to 2, 61 to 0, 74 to 4. NO-PROTOTYPE LEVER, THIRD CONFIRMATION: __Func_8092c40 filled r0 before r1 where the ROM does the reverse, and deleting the declaration closed OvlFunc_953_200839c. Try it the moment the residue is the SAME instructions transposed rather than different register names; it does nothing for register ASSIGNMENT. ADDRESS-ONLY LOCAL, FOURTH CONFIRMATION: OvlFunc_921_20098c4 sat at 2 differing under the flag, both `ldr r3,[r3]` against our `ldr r2,[r3]`, and deleting the char* local in favour of `((char *)iwram_3001e70)[0x17] = 0` matched outright. Four instances on four shapes -- such a local is not neutral, it costs the ordering or a register. BLOCK LAYOUT NAMES THE IF BODY, confirmed: OvlFunc_953_200839c had its arms inverted, and writing the ROM's FALL-THROUGH block as the `if` body rather than the `else` took 58 differing to 4. The layout is the answer; it does not need guessing from which condition reads more naturally. Three Makefile CSE_CFLAGS rules added, each install followed by a build from clean since a flag change does not rebuild existing objects. Splitting was needed for only one of the five -- two source files held a single function each and converted whole |
| [batch-163](reports/batch-163.md) | 5 | ready to port — THE PARK CORPUS IS A CANDIDATE POOL THAT REFILLS. THREE of the five came out of src/non_matching/ rather than the candidate scanners, which changes where a round should start: a park is a snapshot of what was known the day it was written, and the lever inventory grows every batch. tools/park_retry.py ranks parks that are close (<=20 differing) and whose notes never mention a lever whose residue shape they carry; 112 qualify. OvlFunc_946_20092b4 and OvlFunc_903_2008d68 were both at 2 differing on the ORR-DESTINATION residue and both closed with `unsigned char m = K;`. The second is the instructive one: its park concluded 'it is register allocation, not operand order, and nothing in docs/elevation.md reaches it', listed 'the constant as a named local' among four attempts, and DID NOT RECORD THE TYPE -- it was an int, and the doc's own sharpening is that an int local here is folded and is NO LEVER AT ALL. OPERATIONAL RULE: when a park says a named local was tried, check whether it records the TYPE; if not, the lever has not been tried. Actor_SetAnimAndSpeed was parked at 2 on argument fill order with the park itself observing that 'the order tracks the CALLEE, not the argument expressions' -- the no-prototype lever's exact signature, never tried; deleting Sprite_SetAnim's declaration matched. THE BOUNDARY: OvlFunc_947_200a1ac carries a residue that LOOKS IDENTICAL and unsigned char is MUCH WORSE there (47 differing against 9 for the int) -- in the two that closed the constant is the ORR destination, in 947 it must stay LIVE across two flag updates. Same-looking residue, opposite remedy. And a thorough park stays parked: OvlFunc_947_2009fd4 has tried the narrow local, at 18, with five other spellings. A TOOL BUG WAS HIDING MATCHES: protolever.py scored a MATCH as the worst possible result, because tryc prints '  OK <name>' INDENTED and the tool did out.strip() then tested for ' OK ' WITH A LEADING SPACE. It printed two OK rows for Actor_SetAnimAndSpeed and reported 'best: 4 differing, as written'. Fixed and unit-tested. GENERALISE: a tool that RANKS results can hide the thing it was built to find and fails silently, because the rows it prints stay correct -- any sweep whose summary disagrees with its own output is reporting a bug, not a result. TWO LEVERS FROM FRESH CANDIDATES: (1) ONE REGISTER FOR TWO UNRELATED VALUES MEANS ONE VARIABLE -- the inverse of the disjoint-live-range rule; it closed OvlFunc_954_2008490 (38 differing to exact) and took the previous round's OvlFunc_896_200c260 park from 78 to 6 after it had been written off as an unreachable rotation. Before writing off a rotation park, check whether the ROM REUSES a register: it turns an allocator question into a variable-count question. (2) THE ENTRY-BLOCK NAMING LEVER HAS A BUDGET -- six named constants on OvlFunc_968_2008b98 were all held live and cost high registers (98 lines against 85, 89 differing); naming ONLY the constant whose build the ROM actually splits gave 85 and 4. The failure shows in the PROLOGUE before any diff is read. ALSO: twelve STALE parks swept (they name only functions since elevated, including one for Func_80b86ec that I elevated two batches earlier and left behind; OvlFunc_919_2008200's park cost real effort before I noticed the function was done), moved to toDelete/stale_parks/. And a process negative: Func_80170c4's park already recorded the shared-exit result I then re-derived -- park_retry ranks on what prose does NOT mention, so read the park BEFORE screening |
| [batch-164](reports/batch-164.md) | 6 | ready to port — THE `multi` POPULATION WAS NEVER WORKED AND IT IS ORDINARY WORK. Five of six came from it. census.py classifies 692 remaining functions as `multi` -- they share a .s so their blocker is UNKNOWN until a split -- and excludes them from the `open` worklist, which reads as though a split were an obstacle. It is not: the label costs a split_s.py run and the split is BYTE-NEUTRAL BY CONSTRUCTION. Selecting with the criteria that have been paying (loop-free, no r8-r11, no repeated expensive constant, 20-90 instructions) leaves 33; six tried, five matched, three at exact length on the FIRST screen and one with no edits at all. This came after four consecutive thin rounds on the worked-over pools. THE MERGE LEVER IS NOW THE MOST RELIABLE THING FOR REGISTER ROTATION -- 'one register for two unrelated values means ONE variable', the inverse of the disjoint-live-range rule, is three-for-three: OvlFunc_954_2008490 (38 to exact), OvlFunc_896_200c260 (78 to 6, a park written off the round before as unreachable), and the OvlFunc_899 twins (17 to exact, where `ax = (ax - x) >> 16` is right and a separate `dx` is not). Before recording a rotation park as unreachable, check whether the ROM REUSES a register for two values the obvious C keeps apart -- it turns an allocator question into a variable-count question. THREE SMALLER READINGS: a ROM bhi/bls on an ldrb value is a tell about the LOCAL'S TYPE (a byte load promotes to int and gcc picks SIGNED unless the destination says otherwise -- GiveDjinni's branch was bgt until the count was declared unsigned int); NAME THE TWO MULTIPLIES before summing them, since gcc otherwise fuses each multiply with the subtraction feeding it while the ROM computes both differences first (36 lines to 34); and `pop {r1} / bx r1` in a void-looking function wants `int` with NO RETURN STATEMENT -- measured three ways, void is 4 differing with a wrong epilogue, int with explicit returns is 74 and two lines long, int with no return is 2 and exact, because explicit returns make gcc materialise a value the ROM never produces. THE ALIASING TELL closed Func_80935d4 (54 differing to 4) and tools/aliastell.py makes it searchable, but two rounds of false positives narrowed it twice: a CALL between the store and the re-read means nothing (gcc reloads across a call regardless), and a store through a CHARACTER type does not qualify (a character type aliases everything). 48 candidates became 5. ITS BOUNDARY IS MEASURED: on OvlFunc_926_2008e94 the flag helps 82 to 65 but the LENGTH IS WRONG IN BOTH DIRECTIONS (99 against 101 without, 102 with) -- some re-reads survive and some do not, so the source distinguishes them by TYPE and a whole-TU flag cannot. When the flag moves the line count the wrong way it is the wrong class. PARKS WORTH RETURNING TO: Func_808d828 at 4 of 94 and GiveDjinni at ONE instruction; OvlFunc_947_2009938 at 36 of 91 with its whole guard chain exact and a twin. And Func_80ae99c bounds the block-layout reading -- gcc IF-CONVERTS a two-arm constant selection the ROM keeps as a real branch, and neither ordering the arms to the fall-through nor explicit gotos change it; that reading tells you which arm becomes the fall-through WHEN GCC EMITS A BRANCH and says nothing when it declines to |
| [batch-165](reports/batch-165.md) | 5 | ready to port — ALL FIVE from the `multi` population batch 164 opened; eleven functions in two batches against four thin rounds immediately before, with three at exact length on the FIRST screen and one matching with no edits. INITIALISE THE RESULT BEFORE THE VALUE THE CONDITIONS READ: GetMoveDisplayEffect is a chain of independent ifs each overwriting a result, and written the obvious way (compute key, zero result, test) gcc IF-CONVERTS the first test into branchless eor/neg/orr/lsr and the function diverges at 37 of 36, two lines long; moving `r = 0` ABOVE the key computation is 14 differing with nothing else changed. When the ROM has a run of cmp/bne/mov and we emit branchless arithmetic, hoist the result's initialiser above everything the conditions read -- this is NOT the assignment-position lever for register choice, it changes the CONTROL FLOW gcc emits. VOLATILE AT THE USE SITE REACHES A REDUNDANT LOAD THAT -fno-gcse DOES NOT: the same function reads m[3] twice with no store between; two separate int locals, passing the loaded local, and -fno-gcse are ALL INERT, and `*((volatile unsigned char *)m + 3)` matches outright. The flag and the cast are not interchangeable; the cast is the narrower instrument for a redundant load at one site. THE ALIASING CLASS HAS A SECOND FORM -- A LOAD THAT SANK, NOT ONE THAT VANISHED. Func_8096d2c sat at 4 of 41 because gcc SANK an int load PAST a halfword store, legal only under strict aliasing; ALIAS_CFLAGS matched outright and three source orderings were inert. Same cause, opposite symptom. tools/aliastell.py CANNOT find this form and its docstring now says so: the ROM's order is the natural one so nothing in the listing is anomalous, and it shows only in the DIFF. Try the flag whenever a small residue is a load on the wrong side of a store of a different width, detector or no. A STACK ARGUMENT EQUAL TO A REGISTER ARGUMENT IS ONE LOCAL USED TWICE: OvlFunc_916_2008150's first arm builds 4 once for both the fourth argument and the stack slot -- two literals give two materialisations and 23 differing, `v = 4; f(0,0,1,v,v,9)` matches, and the other arm needs them distinct and gets two locals. Check whether the ROM's stack value IS a register argument before giving it its own local. THE POOLED HALFWORD ZERO, FOURTH INSTANCE: Func_801999c came in at 3 differing and matched on it -- a bare 0 stored to a halfword compiles to `ldr r3, =0x0` where the ROM has `mov r3, #0`, and an int local fixes it. When a screen is small and the diff shows a pool load of a value that would fit an immediate, check this first |
| [batch-166](reports/batch-166.md) | 5 | ready to port — AN ADDRESS LOCAL'S BIRTH STATEMENT DECIDES WHETHER IT GETS ITS OWN REGISTER. OvlFunc_886_20090c0 sat at 4 of 60 with everything aligned except one scratch register: the ROM builds 0xc0 << 13 in r2 and then REUSES r2 for a byte pointer, ours built the constant in r1 because r2 was already claimed. Assigning `q = a + 0x64;` before the +0xc update gives 4 differing, after both int stores gives 10, and BETWEEN THE TWO INT STORES -- exactly where the ROM has `mov r2, r5` -- gives 1, which is the linker alias. This is finer than the recorded birth-ORDER rule, which is about several pointers relative to each other; here there is ONE pointer and the STATEMENT GAP is the whole lever. When a diff is 'one scratch register is wrong and nothing else is', move the address local one statement at a time before calling it allocation. A THIRD SIGNATURE FOR THE ALIASING CLASS: the two on record are a reload that VANISHED and a load that SANK; here nothing is missing. With strict aliasing gcc knows the later halfword stores cannot touch the int fields and HOISTS `add r2, #0x64` above them where the ROM keeps it below; -fno-strict-aliasing reinstates the dependence, 7 differing to 4, with no source change, and no scheduler or CSE flag touches it. So the tell is also an ADDRESS COMPUTATION scheduled across stores of a different width -- invisible to aliastell.py for the same reason the sunk load is, there is no re-read to key on. THE INTERLEAVE IS NARROWER THAN 'THIS ROM LIKES r0 EARLY': OvlFunc_927_200a1b0 comes out at the ROM's exact 108 lines with six differing, all one shape, but the SAME ROM puts `mov r0, #0x12` LAST on its four FOUR-argument calls and they all match -- only the two- and three-argument calls interleave. That rules out a scheduler difference and is consistent with six flags leaving it at 6 (-fno-schedule-insns2 takes it to 52). It also confirms the guarded/straight-line split from the other side: no conditional branch means the naming lever has nothing to rematerialise across and is exactly inert. tools/filtered.py NOW REPORTS A BRANCH COUNT and flags candidates with no guard -- it offered this one, counting calls and instructions and saying nothing about guards. LEVERS THAT LANDED AS DOCUMENTED, found by looking the residue up rather than by experiment: `pop {r1} / bx r1` means `int` with no return statement; the slot initialiser assigned AFTER the modulo call; an UNSIGNED switch value or the tree compares `bgt` where the ROM has `bhi`; int intermediates for halfword literals; an int mask against width narrowing (`q[9] & ~0xc` stored into a byte lets gcc truncate to `mov r3, #0xf3`, one instruction SHORT of the ROM's mov/neg pair); and the merge lever again, 106 lines to 101. POOL STATUS, which the next round needs: solved_twins.py returns 0 across 0 templates and filtered.py at its documented thresholds now returns 0. Relaxing to 25-110 instructions, >=4 calls and at least one conditional branch returned 3. Expect to widen further or to work the park corpus, which park_retry.py still ranks 112 deep. PARKED: OvlFunc_921_20095b4 at 101 of 102 on the register-role swap with FOUR levers landed and recorded (aliasing flag, int mask, derived second byte address, merge lever) -- a good specimen because nothing else is outstanding; and OvlFunc_927_200a1b0, not a candidate |
| [batch-167](reports/batch-167.md) | 7 | ready to port — SELECTION IS THE LEVER, and this batch cost two ZERO-ELEVATION ROUNDS to learn it. Rounds three and four picked candidates the way the tooling has always ranked them (instruction count, call count, branch count) and screened EIGHT functions; every one reached exact length and NONE matched, all stopping on register allocation or post-reload scheduling. The rounds that did produce elevations had a solved NEIGHBOUR instead -- DeleteActor matched on the first screen because its own .s already held the solved Actor_SetAnimAndSpeed with the identical opening. FIRST, the exact-skeleton tools are now MEASURED as exhausted: match_shapes.py reports 0 leads and --near 1, 2 and 3 all report 0, and solved_twins.py reports 0 across 0 templates -- every remaining constants-only variant of an elevated function has been taken, so do not re-run them hopefully. SECOND, 'no exact twin' is not 'no usable template': tools/fuzzy_solved.py (new) ranks every remaining function by the best difflib ratio between its skeleton and any solved function's, bucketed by length with a mnemonic-overlap prefilter, and reports 26 leads at ratio >= 0.80. The top three were taken in one round and ALL THREE MATCHED ON THE FIRST SCREEN: TextBox 0.986 against DialogueBox in the same .s stem, OvlFunc_924_2009bf0 0.977 against OvlFunc_924_2009420 in the same overlay, OvlFunc_926_2008484 0.971 against OvlFunc_939_2008764 in another. Size and call count predict how hard a function is to READ; similarity to a solved function predicts whether the answer already EXISTS. RUN fuzzy_solved.py BEFORE ANY OTHER SCANNER -- 23 leads remain above 0.80. CODEGEN LEVERS FROM THE EARLIER ROUNDS: a symbol-relative load needs BOTH halves named -- naming the complete offset stops gcc reassociating `Lb41ac + off + 0x30` to `(Lb41ac + 0x30) + off` and pooling the FOLDED symbol (37 lines/27 differing to 39/8), and naming the BASE as well puts the symbol in the addressing base position rather than the offset one (39/2). `do { } while (i != N)` stops gcc REVERSING a counted loop whose index is dead in the body -- indexing the array with i does not, only the != exit test does. THE MERGE LEVER CHAINS: Func_8005810 runs one register through four roles and merging them one at a time goes 35 differing to 21 to 13 to an exact match, but naming the modulo index in its own local -- the ROM's `mov r5, r0` read literally -- is 26, WORSE than not naming it. A named base pointer for a STACK array costs a callee-saved register, the opposite of the heap and global case, because sp is free to rematerialise. TWO PROCESS FINDINGS: a screening ref built from a LINE RANGE can be short and lies quietly (sed -n '225,265p' truncated a function and tryc reported a five-instruction excess that does not exist -- use awk on the .thumb_func_start/.func_end markers); and TWO DEFECTS CAN CANCEL, so the wrong three-instruction bit extraction gave the ROM's exact length and a BETTER differing count than the correct two-shift spelling, because its extra instruction cancelled a `mov` gcc never emits. SIX PARKS, all with full measurement tables including flag probes; rom_8a000/8095fcc.c is the one to look at first at 3 of 54 -- the gState offset build alone took it from 53 differing to 3, and four spellings plus -fno-schedule-insns all sit at exactly 3 |
| [batch-168](reports/batch-168.md) | 6 | ready to port — ALL SIX came off tools/fuzzy_solved.py, two with no iteration at all; the leads are down to 17 above 0.80 from 26, and batches 167-168 took nine of them at one to three screens each. Func_80aac84 SETTLES THE goto-LOOP DISCRIMINATOR that batch 167 left open (the rewrite took Func_8090584 from 95 differing to 3 and Func_80bac6c from 34 to 51). It screened at 74 lines against 77 with 71 differing because gcc had both REVERSED the inner loop AND STRENGTH-REDUCED the address, where the ROM counts up, keeps the base in r14 and recomputes base+j and idx*2 every iteration; the rewrite gave 77 lines and 2 differing and a statement-order swap closed it. THE RULE: the rewrite pays when gcc applied a loop TRANSFORMATION -- reversal or strength reduction -- not merely a hoist, because a transformation restructures the whole body while one hoisted constant is cheaper to live with than the rewrite's overhead. Corollaries held: with hoisting off the ROM's `mov r7, #0x1f` before the outer loop is an explicit `int mask = 31;`, and the order of the two loop initialisers becomes observable. A RECORDED WARNING CORRECTED: -fno-schedule-insns2 is on record as 'an actively misleading probe' from a round where it pushed the first difference to instruction 1 and multiplied the count every time; Func_8078320 is the opposite and the warning as written would have STOPPED the elevation -- three lines of residue (a strh and the ldrsh after it in the wrong order) closed outright with nothing else disturbed. So it is a SIGNATURE not a verdict: count multiplies and the diff jumps to the top, put it down; residue closes with nothing else moving, it is the answer. Its sibling Func_80782a0 shows the other half -- the same flag took 5 differing to 4 and did NOT close it, and the real fix was a spelling: the ROM widens a just-stored halfword IN PLACE, and `x <<= 16; x >>= 2;` matches where `(short)x << 14` makes gcc compute into a second register and hoist above the store. THE PER-USE-SITE-LOCALS LEVER NEEDS A DOMINATING BOUNDARY: OvlFunc_948_200938c and _200949c (parked together) each call MapActor_SetSpeed twice with the same pooled pair, gcc hoists into r5/r6 (visible in the prologue), and copying the recorded remedy is EXACTLY INERT -- byte for byte identical to plain literals, with three CSE flags inert too. The worked instance has three guarded blocks BETWEEN the assignments and the uses; these have their only guard AFTER both calls. So that lever is the dominating-block mechanism under another name, alongside the argument interleave and constant CSE. BUILD DISCIPLINE: split_s.py refusing on local labels is a TWO-STEP -- export, verify compare on the export ALONE (a .global emits no bytes), then split; and AGBCC_DIR=/opt/agbcc is REQUIRED in the container for a non-obvious reason -- it selects no compiler for any elevated function (all gcc-2.96) and feeds five old_agbcc library objects only, but the Makefile default tools/agbcc resolves to the checked-in macOS Mach-O binary, which the container cannot exec and which fails with `Syntax error: "(" unexpected` -- reading like a corrupted toolchain rather than a wrong path |
| [batch-169](reports/batch-169.md) | 5 | ready to port — NAMING A REPEATED CONSTANT CAN BE WRONG, which inverts a standing lever. OvlFunc_927_200a078 passes 0xc0 << 11 to two calls and the ROM builds it once into r8 and holds it across the intervening work -- the shape that normally means a named local, and its own solved exemplar names it. Naming it here is 2 differing at exact length on ARGUMENT-SETUP ORDER (rom `mov r2,#0x86 / mov r3,#0xc0 / lsl r3,#11` against ours `mov r3,#0xc0 / mov r2,#0x86 / lsl r3,#11`); writing the literal at BOTH sites matches outright, because gcc's own CSE produces the single build by itself and the named local additionally fixes WHEN it happens, wrongly. Moving the assignment earlier is 125 lines and 122 differing, both placements tried. So a constant held in a callee-saved register across calls is NOT by itself evidence for a named local -- name it only when the plain spelling fails. That is the exact inverse of this batch's three parks, where gcc's hoist is what must be prevented; same mechanism, and the ROM decides which. THREE PARKS ON ONE CLASS: OvlFunc_948_200938c and _200949c (one file, same cutscene over different slots), OvlFunc_959_200a1c4 and _200a06c, all duplicate-constant hoist. The recorded remedy -- separate locals per use site -- is EXACTLY INERT, byte for byte identical to plain literals, with three CSE flags inert too. The reason is guard placement: the worked instance that lever comes from has three if-blocks BETWEEN the assignments and the uses, and all four of these are straight-line across the repeats. So that remedy is the dominating-block mechanism under another name, alongside the argument interleave and constant CSE. 200a06c adds a presentation the earlier notes would not catch -- gcc commons the PRE-SHIFT eight-bit base underneath two DIFFERENT shifted results, so the repeated thing need not be an argument or even a value the ROM writes twice. TWO MORE READINGS: `(unsigned short)(v - 1) <= 1` is not `unsigned short w = v - 1; w <= 1` -- the ROM subtracts as a full word and narrows only at the comparison, and the narrow TYPE makes gcc materialise the wrap with `ldr r2,=0xffff / add r3,r2` (a narrow type asks for a narrow VALUE, a cast inside a comparison only asks for a narrow COMPARISON, same family as batch 168's `x <<= 16; x >>= 2;`); and the gState offset build cost 135 differing on one function purely by folding to a pooled symbol, so it is now the highest-yield first check when a differing count approaches the line count. TOOLING: fuzzy_solved.py now warns BEFORE any C is written -- FAKEMATCH exemplars (files that match only by pinning locals with inline asm; copying one WORKS, which is the problem) and DUP-CONST targets (reusing filtered.py's expensive_constants verbatim, reading the real instruction text because match_shapes collapses every immediate to one letter and its skeleton cannot see repeated constants at all). Of eleven leads, two carried a fakematch exemplar and three were flagged; this batch worked the clean six. Eight leads remain above 0.80, five of them clean; fuzzy_solved.py has produced fourteen elevations across batches 167-169 |
| [batch-170](reports/batch-170.md) | 5 | ready to port — A GLOBAL READ MUST BE NAMED TO KEEP ITS LOAD ABOVE A FOLLOWING BRANCH. Func_80a1cb0 selects a constant with an `if` then walks a list based at a global; the ROM loads AND DEREFERENCES the global before the branch and keeps the value across it. Written with the global used only after the branch, gcc sinks the whole load below it -- 12 differing at exact length. Reading it into a named local first and forming the pointer afterwards matches: the name pins the LOAD above the branch while the `+ 0x48` still happens after it. This is the READ counterpart of the address-local birth rule -- for a store the question is where the address is COMPUTED, for a global read it is where the value is FETCHED, and a branch between fetch and use is what makes it observable. LOOP INITIALISER ORDER CONFIRMED OUTSIDE A goto LOOP: batch 168 found it on Func_80aac84 inside a rewrite; Func_80a9b94 is a plain do/while and swapping `i = 0;` before `q = ...;` is the whole difference between 2 differing and an exact match. When a diff is two lines and both are loop setup, try the other order first. TWO FUNCTIONS, ONE EXEMPLAR, TWO-LINE RESIDUES POINTING OPPOSITE WAYS: OvlFunc_928_2008d0c and OvlFunc_957_2008de8 are both variants of the solved OvlFunc_946_2009a44, both at the ROM's exact length and both at exactly 2 differing on the placement of ONE constant build -- and 2008d0c wants it EARLIER than gcc puts it while 2008de8 wants it LATER. Six spellings measured between them, every one inert or worse in both. THE PAIRING IS THE FINDING: if a source construct controlled that placement, one of the two would have yielded. So it is the scheduler, not a missing lever, and a two-line residue of this shape is worth one probe and then a park. -fno-schedule-insns2 on 2008d0c shows batch 168's 'destroying the evidence' signature exactly. Note also that 2008d0c's exemplar contains the IDENTICAL expression and matched with it -- the spelling is not the variable, the surrounding register pressure is. WHAT THE LEAD TAGS FILTER: at ratio >= 0.70 there are 26 leads and exactly ONE is fully clean, the rest carrying DUP-CONST or FAKEMATCH or both. That is the tags earning their keep, not the pool running dry -- dropping to 0.66 surfaced five more clean leads and one is elevated here. WHEN THE CLEAN LIST EMPTIES, LOWER THE RATIO BEFORE CHANGING METHOD: a clean 0.70 lead has outproduced a flagged 0.90 one. FOUR PARKS with full measurement tables; the last two (ovl_77dd1c/2009498.c at 47 of 53, rom_b5000/80c1014.c at 31 of 33) are the same wall stated twice -- the C has no way to demand a particular callee-saved register, or a redundant copy into one |
| [batch-171](reports/batch-171.md) | 5 | ready to port — ELEVATIONS COMPOUND: three of the five matched with NO iteration, and all three came from exemplars elevated in the previous two rounds. Func_80a68a8 was elevated one round and became the EXEMPLAR for Func_80a3d24 the next; Func_80a9b94 (batch 170) was the exemplar for Func_80a9cf8 here. The solved corpus grows every round, so a ratio computed today is not the ratio computed next round -- functions below threshold RISE as their nearest neighbour gets solved. RE-RUN THE RANKING EVERY ROUND rather than working from a saved list. INITIALISER ORDER IS THREE FOR THREE and is now a FIRST check: Func_80aac84 (goto loop), Func_80a9b94 (plain do/while, counter before pointer) and Func_80a9cf8 (counter before a held constant). Not a goto-loop phenomenon and not specific to pointers -- when a small residue sits in a loop's setup block, permute the initialisers before reaching for any lever. Func_80a9cf8 also needed the global read NAMED before the pointer was derived from it, 9 differing to 2, the batch-170 rule. THE goto-LOOP LEVER, FOURTH INSTANCE, fits its recorded discriminator exactly: Func_80a9d3c's parallel flags access was STRENGTH-REDUCED by gcc into its own walking pointer where the ROM keeps the counter and indexes with it, and the rewrite recovered it -- the rewrite pays for a loop TRANSFORMATION, not a mere hoist. That function also POOLED a halfword store of 8 where the ROM has `mov r3, #0x8`, a third exception to the narrowed HImode rule, so check the halfword exception on ANY small constant through a short *, not just 0. SCRATCH-REGISTER SELECTION IS A DISTINCT WALL, THREE DEEP: OvlFunc_882_20090a4 (8 of 80), OvlFunc_968_200c968 (22 of 90) and SystemMsgBox (23 of 79) each reach the ROM's exact LENGTH and exact instruction SEQUENCE and differ only in which of r1/r2/r3 carries a value. Distinct from the register-ROLE swap (callee-saved, visible in the prologue) and from scheduling (moves instructions). Inert on all three: naming a value, naming a global's address, naming a struct pointer, reordering the operand statements, operand order, and every flag group. WHEN A SCREEN IS AT EXACT LENGTH WITH THE SEQUENCE ALIGNED AND THE DIFF IS A COLUMN OF `mov rN` AGAINST `mov rM`, ONE PROBE THEN PARK -- these cost four to seven screens each to reach the same place. A REASSIGNED LOCAL SOMETIMES HAS TO BE SPLIT, NOT MERGED: OvlFunc_882_20090a4's ROM runs r5 through 0x35 then 0x36, the merge lever's usual shape, but one reassigned local is 64 differing and two separate locals is 8, because gcc coalesces the single variable's live ranges and then needs one FEWER callee-saved register than the ROM. The PUSH LIST is the discriminator -- a push the ROM has and we lack says split, a push we have and the ROM lacks says merge. It also needed -ffixed-r7. AND CHANGING THREE THINGS AT ONCE CANCELS SIGNAL: Func_80df9d0 at 5 differing with three visible fixes scored 9 applied together, worse than baseline; individually the named loop bound is 11 and WRONG, offset-first pointer 4, split load/store 3, the two good ones 2. A help and a hurt CANCEL, so a batch scoring worse than baseline is not evidence all of it is wrong |
| [batch-172](reports/batch-172.md) | 8 | ready to port — THE SELECTION FILTERS DRAINED, AND THE POOL THEY DRAINED WAS NOT THE CORPUS. At the top of this round fuzzy_solved returned 4 leads at >=0.80 and ALL FOUR were flagged DUP-CONST or FAKEMATCH; match_shapes, solved_twins and --near 1/2/3 returned 0 as they have for three rounds; and pickable returned 99 candidates of which EVERY ONE uses r8-r11. That last number is the sharp one -- not that the filter found little but that its survivors are now HOMOGENEOUS, every remaining admission carrying the one wall it was never built to score. The filters are tuned for 40-120 instructions and >=5 calls and that band is worked out, so the move was a scan BELOW it: 14-44 instructions, no r8-r11, no repeated expensive constant, call count unconstrained. 19 candidates; three tried, THREE MATCHED, at 1, 4 and 2 screens. pickable rejects under 40 instructions because a tiny function gives the allocator nothing to act on -- sound reasoning about which LEVERS apply, and a false statement about which functions MATCH. A FILTER TUNED TO PREDICT WHICH FUNCTIONS NEED DOCUMENTED WORK WILL SYSTEMATICALLY HIDE THE ONES THAT NEED NONE. THE EPILOGUE TELL, REFINED -- AND THIS ENTRY IS A CORRECTION: batch 172 first reported the epilogue tell as new and it is NOT, docs/elevation.md having recorded it twice already (batch 46's `pop {r1}` entry and the OvlFunc_971_20091bc one). What Func_80b6378 adds is a case neither covers. Both earlier entries explain the r1 epilogue as the function returning THE VALUE THE LAST CALL LEFT IN r0, and both fix it by writing an explicit `return f(...)`. Func_80b6378 has no return statement, no trailing call and no value to return -- its body ends in a store inside a loop -- and changing `void` to `int` alone matched. So the mechanism is one step earlier than those entries state: gcc marks r0 live at exit FROM THE DECLARED RETURN TYPE ALONE, whether or not a value ever reaches it. The tell reads `pop {r0}/bx r0` = declared void, `pop {r1}/bx r1` = declared non-void -- not 'returns something' -- and when the earlier fix has no candidate call to apply to, the declaration alone is still the whole lever. Their other advice stands: this mimics scratch-register selection, so CHECK THE EPILOGUE PAIR BEFORE ADDING A FUNCTION TO THAT PARK CLASS. AN INDEX INTO A POINTER MUST BE NAMED TO STAY AN INDEX, which CORRECTS the batch-170/171 statement of the offset-first rule. Those entries describe the lever as OPERAND ORDER; operand order is inert, because + is commutative and gcc reassociates before selecting addressing modes -- `*(char*)(buf[i]+0x48+p)` with an int base is byte-identical to `*(p+buf[i]+0x48)` with a char* base, both one instruction long. What works is `k = buf[i]+0x48; p[k] = v;`. A NAME IS A MATERIALISATION POINT, NOT A PLACEMENT HINT: it does not tell gcc where to put a value, it tells gcc the value must EXIST. That unifies this with the batch-170 global-read rule (a name pins a load above a branch) and explains why the constant-folding levers fail -- there the value existed already. A REASSIGNED ACCUMULATOR MAY NEED A SECOND VARIABLE: Func_8098184 was 21 against the ROM's 22, the extra instruction a bare `mov r2, r3` inside the loop; writing `do { w = v + step; v = w; } while (w <= lim);` with the post-loop stores reading `w` matched. This is batch 171's split-versus-merge discriminator in a loop body, and its tell is DIFFERENT -- 171's tell is the push list, which is silent here since both registers are caller-saved. THE LOOP-BODY TELL IS A BARE `mov rN, rM` BETWEEN THE UPDATE AND THE TEST. AND A PROCESS FINDING THAT COULD HAVE CORRUPTED THE TREE: A STALE OBJECT CAN MAKE A WRONG ELEVATION LOOK GREEN. The linker script names asm/<path>.o; when a hand-written asm/<path>.s is replaced by src/<path>.c the old .o is still present and make has no reason to rebuild it. TWO of this batch's three elevations built green incrementally while their .o was still the one assembled from the hand-written asm that had just been deleted -- the C had never been compiled. Caught only by an asymmetry in which generated .s files existed. The existing 'check every address against the linked ELF' rule would NOT have caught it, because the symbol is at the right address either way. THE PROOF IS THE `.gcc2_compiled.` LOCAL SYMBOL AT THE FUNCTION'S ADDRESS after a make clean build, which is what distinguishes a compiled translation unit from an assembled one; that marker is now part of the check. Method note: seven screens for three matches against 171's four-to-seven PER FUNCTION. Sprite_DeleteLayerIndex matched on screen one because its solved sibling Sprite_DeleteLayer in the same .s family is the same routine reached by pointer instead of index and its entire second half transferred verbatim -- and fuzzy_solved never surfaced that pair, because the two differ across their whole first half. SIBLINGS INSIDE ONE .s FAMILY ARE WORTH CHECKING BY HAND EVEN WHEN NO RANKING OFFERS THEM; the tree's _a/_b/_c splits keep related routines adjacent and the rankings are blind to that adjacency. No parks |
| [batch-173](reports/batch-173.md) | 5 | ready to port — FOURTEEN SCREENS FOR FIVE MATCHES, two of them on the first screen, all from the small-and-medium bands batch 172 opened after the standing filters went homogeneous. NO PARKS. FIRST, A CORRECTION TO BATCH 172: it reported the epilogue return-type tell as a finding and it is NOT new -- docs/elevation.md already carried it twice, in batch 46 and again for OvlFunc_971_20091bc. The batch-172 report, its HANDOFF entry and the elevation.md section are all rewritten. What Func_80b6378 genuinely adds is that both earlier entries explain the r1 epilogue as returning THE VALUE THE LAST CALL LEFT IN r0 and both fix it with an explicit `return f(...)`, while that function has no return, no trailing call and no value -- so r0 is live at exit FROM THE DECLARED TYPE ALONE and `pop {r1}` means DECLARED non-void, not 'returns something'. The process failure is worth more than the fix: both entries were found by one grep, run only because an unrelated park note mentioned the tell in passing. A FINDING THAT FEELS NEW IS WORTH ONE GREP OF elevation.md BEFORE IT IS WRITTEN UP AS NEW; an 11,000-line notebook is past the size where memory indexes it. AN ACCUMULATOR'S BIRTH POSITION DECIDES ITS REGISTER BANK, AND THE ASSEMBLY ARGUES AGAINST THE FIX. Func_800be20 was at the ROM's exact 43 lines with five differing, all one allocation: the ROM keeps the running total in r7 and pays a push, ours used caller-saved r1. Moving `total = 0;` above the _GetSpriteInfo call matched. The ROM's listing puts `mov r7, #0x0` AFTER the bl, which reads as an initialisation written after the call -- it is not. gcc picks the bank from the SOURCE-LEVEL live range, which crosses the call, then sinks the constant's materialisation below it because rematerialising a literal is free. A CONSTANT INITIALISER APPEARING AFTER A CALL IN THE ROM SAYS NOTHING ABOUT WHERE IT WAS WRITTEN; the callee-saved register it lands in says the live range crossed the call. Third reading of the batch-171 push-list discriminator -- so far 'a push the ROM has and we lack' meant SPLIT A MERGED LOCAL, here it means THE VALUE WAS BORN EARLIER THAN WE WROTE IT, and both are the same question because the push list is a direct readout of which live ranges cross calls. A PURE-PLACEMENT RESIDUE HAS TWO CAUSES AND ALIASING IS THE ONE NO SPELLING REACHES. OvlFunc_969_20083a0 screened at the ROM's exact 45 lines with five differing, two of them a linker alias its own overlay.ld already resolves; the real residue was three transposed lines, gcc hoisting `ldr r1, [r5, #0x50]` three instructions early. That shape is the recorded one-probe-then-park signature and THREE source probes were spent before any flag: moving the +0x4c statement later 47/45, naming the subtraction inert, storing between the +0x18 and +0x1c updates 49/47. -fno-schedule-insns2 gave 22 against 5 -- the recorded 'destroying the evidence' signature, which should have been read as ruling out THE SCHEDULER rather than just that flag, since a residue the scheduler is not causing must come from an assumption it was handed. -fno-strict-aliasing gives 2 differing, both the alias: at -O2 gcc may assume the pointer loaded from +0x50 cannot alias the int struct it came from, freeing that load to move up. IF THE INSTRUCTION THAT MOVED IS A LOAD THROUGH A POINTER THAT COULD ALIAS ITS SURROUNDINGS, TRY THE ALIASING FLAG FIRST -- source order cannot express an aliasing assumption, so no spelling will ever reach it. Reserve one-probe-then-park for placements among values gcc can already see do not alias. THE SIBLING LESSON PAID TWICE MORE, and Func_809b364 is the strongest case yet: 51 lines against 55 with `gState + 0x1da` folded to one pooled ldr, then 54 with a named base and one instruction still missing -- the ROM's `ldr r3, =0x1` against our `cmp r3, #0x1`, a pooled 1 no spelling of the literal reaches. A solved function IN THE SAME .s FAMILY, src/rom_8a000/rom_9ad70_c_c_a.c, answers both at once with `g = gState; m = *(short *)(g + (0xed << 1)); if (m == (int)&_CONST_1)`. Both levers were already documented; what the sibling supplied was the INSTANTIATION -- which symbol name, which of several near-equivalent base spellings, and confirmation that this exact halfword is compared against a pooled constant. A GENERAL RULE TELLS YOU A LEVER EXISTS; A SIBLING TELLS YOU WHAT TO TYPE. Also: split_s.py refused an overlay split until three .L data labels were exported with .global, and those labels belonged to the OTHER two parts, not the target -- do the export and verify it byte-neutral as its own step so a layout mistake and a bad export cannot be confused |
| [batch-174](reports/batch-174.md) | 5 | ready to port — THE ROUND'S RESULT IS A TOOL. Batches 172 and 173 each ended, by accident both times, with the thing that closed the function sitting in an already-solved .c in the target's own _a/_b/_c family. fuzzy_solved.py structurally cannot find these: it scores whole-skeleton similarity, so a sibling sharing ONE idiom while differing everywhere else ranks near zero, yet the tree's splits keep genuinely related routines adjacent. tools/family_siblings.py ranks remaining functions by how much of their own family is elevated and -- the part that matters -- prints WHICH SYMBOLS THE TARGET'S ASSEMBLY NAMES THAT A SIBLING ALREADY SPELLS. A family with sixty solved siblings is worth nothing alone; one with a single sibling that spells the exact global the target loads is worth a lot. Four of five came off it, two on the first screen. WHAT A SIBLING SUPPLIES IS NOT A LEVER -- elevation.md already had every lever these needed -- IT IS THE INSTANTIATION: which symbol name, which of several near-equivalent spellings, whether a field is reached one way or another. `ldmia rN!, {rM}` FOR ONE WORD OUTSIDE A LOOP IS AN EXPLICIT `*p++`: a single-register ldmia is not a block move but a load with writeback, and gcc emits it when the source walks a pointer. Indexing the same two slots gives 46 lines with 2 differing AND THE LOADS IN THE OPPOSITE ORDER; `q = p; a = *q++; base = *q;` matches. The less obvious half is the declaration -- the two adjacent globals had to be reached through ONE `extern unsigned char *iwram_3001e6c[];`, because gcc cannot know two independent externs are four bytes apart and pools both, putting the ldmia out of reach no matter how the accesses are spelled. ADJACENT GLOBALS THE ROM REACHES FROM A SINGLE POOL ENTRY ARE ONE ARRAY. AN OFFSET BELONGS IN THE LOAD, NOT THE ADDRESS: Func_80058ac has `mov r3, sp / ldrh r3, [r3, #0x8]` where ours had `add r3, sp, #0x8 / ldrh r3, [r3, #0x0]`; declaring the stack buffer as the type it is actually READ as, `unsigned short buf[8]`, and writing `buf[4]` matches. Same materialisation question as the named-index rule pointed the other way, and the discriminator is readable off the ROM -- A BARE BASE WITH THE CONSTANT IN THE LOAD MEANS A TYPED ARRAY; THE CONSTANT ADDED INTO THE REGISTER FIRST MEANS BYTE POINTER ARITHMETIC. A CALLER AND A CALLEE CAN DISAGREE ABOUT THE PROTOTYPE: Func_80058ac masks its argument to 16 bits at entry so its parameter is a u16, and its caller Func_8005a78 passes it UNMASKED. `unsigned short` at the call site adds two instructions the ROM lacks; `int` matches. A CALLEE'S PARAMETER TYPE IS A PER-CALL-SITE FACT, NOT A GLOBAL ONE -- do not propagate a definition's narrow parameter into the extern at a call site unless the caller's assembly actually narrows. The tell is a mask at the callee's entry and none at the caller's call site. TWO PARKS THAT AGREE WITH EACH OTHER: rom_8a000/8091eb0.c (43 of 41, a branch to the immediately-following label gcc will not delete plus a store where the ROM builds the VALUE first and advances the base in place) and rom_77000/807961c.c (AddPartyMember, 34 of 37 WITH THE LOOP BODY BYTE-EXACT, the whole residue being that the ROM puts the guard before the loop's invariant setup and rematerialises `ldr r0, =gState` on the not-taken path, making the store block a join and costing two more branches). Between them TWELVE source spellings and TEN flag-group runs, every one landing on exactly the baseline 43/13 and 34/23. -fno-gcse inert on AddPartyMember says the hoist is the LOOP OPTIMISER'S, not global CSE's. And the ROM's AddPartyMember is three instructions LONGER than ours -- not gcc missing an optimisation but gcc declining to rematerialise a pool load the original build did rematerialise, the same allocator difference the scratch-register and callee-saved-copy parks record, in a third place. Two smaller facts from 8091eb0: its derived offsets (0x1d6 as 0x17c + 0x5a, 0x1f4 as 0x19e + 0x56) COME FREE from writing the plain offsets and need no special spelling; and its pooled `ldr r2, =0x21` is NOT a _CONST_ symbol -- gcc pools that literal itself, so CHECK THAT A PLAIN LITERAL DOES NOT ALREADY POOL BEFORE REACHING FOR const.sym |
| [batch-175](reports/batch-175.md) | 5 | ready to port — ALL FIVE CAME OFF tools/family_siblings.py, and two in its strongest form. Func_809b3d8 is a near-twin of Func_809b364, elevated last batch from the same rom_9ad70 family: THREE EDITS to the solved file (`a->0x1c = -t` for `= t`, `- d * 5` for `+ d * 5`, `0x80 << 13` for `0x90 << 12`) and it matched at the ROM's exact 56 lines on the first screen, with the two things that were hard the first time -- the named gState base and _CONST_1 for a pooled comparison constant -- coming across for free. LoadItemIcon is the same one step removed: its sibling LoadItemIconID had already established the Blk struct, the `extern int L29ee4[] __asm__(".L29ee4")` declaration and LoadIcon's signature, so writing it was transcription. ELEVATE ONE MEMBER OF A FAMILY AND ITS NEIGHBOURS GET CHEAP -- RE-RUN THE RANKING AFTER EVERY ROUND, because a family's score changes the moment one member lands. TWO SMALL LEVERS, BOTH ABOUT WHERE A THING IS WRITTEN. (a) THE LOOP-ORDER LEVER APPLIES TO THE INCREMENT: OvlFunc_899_200c704 at exact length with 2 differing, both in the increment block; `for (i = 0; i <= 0x24; i++) { ...; t += 0x10; }` puts the bump first, `for (i = 0; i <= 0x24; i++, t += 0x10)` matches. A bump in the body and the same bump in the increment clause are not the same program to gcc. (b) A COMPOUND ASSIGNMENT READS ITS DESTINATION FIRST IF YOU WRITE IT THAT WAY: OvlFunc_936_200b6f8 needed `a->8 += a->0x24;` FOLLOWED BY `vx = a->0x24;` rather than the obvious `vx = a->0x24; a->8 += vx;` -- gcc CSEs the second load away and keeps the destination read first. NAMING A VALUE YOU NEED LATER DOES NOT HAVE TO HAPPEN AT ITS FIRST USE. That function also had `*(unsigned short *)q -= 1;` coming out as `ldr r2, =0xffff / add r3, r2`, a -1 that becomes +0xffff in an unsigned 16-bit type; `*(short *)q -= 1;` gives the ROM's `sub r3, #0x1`. THREE OF THE FOUR PARKS ARE ONE WALL, and having three at once made the shared fact visible: ovl_7fa4ec/20092ac.c (one 0x100 across three calls -> r8, +5 lines), rom_15000/801965c.c (a zero twice through halfword stores, +1), ovl_7e7574/200a69c.c (two constants twice across one call -> r5/r6, +1). -fno-gcse IS INERT ON ALL THREE, so the pass is cse.c's LOCAL constant CSE and no flag reaches it -- four runs across three functions to establish, recorded once so it need not be re-established, because -fno-gcse is the obvious reach and it is the wrong one. THE CLASS HAS A PREDICTABLE VICTIM: 200a69c is a cutscene script, thirteen calls in a row with no branches and no loops, unreachable because two speed constants appear at two call sites with a call between them -- A FUNCTION CAN LOOK COMPLETELY TRIVIAL AND STILL BE BLOCKED, so run the repeated-constant check before reading the shape. 20092ac also killed the one probe with a mechanism behind it, different parameter modes so the two uses are different RTL: gcc folds the narrowing before CSE runs. THE FOURTH PARK, rom_b5000/80c0130.c (32 of 32, three differing, gcc hoisting a DMA source's `+ 0x22` above a volatile strh; -fno-schedule-insns2 doubles the count, the destroying-the-evidence signature) is worth reading for what gcc got RIGHT unaided: TWO ADJACENT GLOBALS FROM ONE POOL ENTRY AT A NEGATIVE OFFSET (`*(unsigned char **)(iwram_3001f00 - 0x88)` gives the ROM's `mov r3, r2 / sub r3, #0x88`, so batch 174's one-array rule works backwards); the DMA3 base derived as `add r3, #0x24` off &REG_DMA0SAD from plainly writing DMA0_SET then DMA3_SET; and `add r1, #0x14` off &REG_BG2CNT from writing &REG_BG2PA. AND THE POOLED-SMALL-CONSTANT TELL HAS AN EXCEPTION, now three specimens deep (=0x21, =0x30, and a pooled zero): all are eight-bit values a mov could build, all stored through a HALFWORD pointer, and in every case gcc pools the plain literal by itself. CHECK THAT A PLAIN LITERAL DOES NOT ALREADY POOL BEFORE ADDING TO const.sym; Func_809b364 is where the symbol IS needed and there the constant feeds a cmp, not a strh. Also from 801965c: the ROM's `strh / lsl #0x10 / cmp #0x0` is a 16-bit zero test on a value from a halfword LOAD, which gcc would not narrow -- writing the copy and the test as ONE expression, `if ((out[i] = src[i]) == 0)`, makes the tested value the ASSIGNMENT's value whose type is the unsigned short lvalue's, and that is what produces the lsl. Two separate statements do not |
| [batch-176](reports/batch-176.md) | 5 | ready to port — THE ARG-INTERLEAVE WALL IS REACHABLE, which retires a class parked on repeatedly. OvlFunc_921_2008384 hit the ROM's exact 133 lines with eight differing, all four copies of `rom: mov r1,#0x80 / mov r0,#0x8 / lsl r1,#0x1` against `ours: mov r1,#0x80 / lsl r1,#0x1 / mov r0,#0x8`. Batch 42 read local-alloc.c and worked out that update_equiv_regs declines to keep a constant equivalence when the pseudo is live in more than one basic block, so assigning the constant in a DOMINATING block forces the split -- but every function parked on this class since has been straight-line, and ovl_7cb2c0/200bdec.c says plainly that a call does not create the block the lever needs. THIS function has two early-return ifs before the affected calls, so the entry block genuinely dominates them: four locals assigned at the top, eight differing to zero. THE WALL IS NOT THE INTERLEAVE, IT IS THE ABSENCE OF A DOMINATING BRANCH -- read the control-flow graph before parking on this class. WHEN A BAND EMPTIES, RAISE THE CEILING BEFORE CHANGING METHOD. Two rounds returned one elevation then none, and the cause was measurable: of 1,332 unelevated functions, 465 sit in 20-120 instructions, 137 of those avoid r8-r12/r14, and ONLY 8 of the 137 avoid a repeated expensive constant -- four of them m4a. Raising the ceiling to 260 returned THIRTEEN fresh clean candidates, mostly zero-callee-saved cutscene scripts, and the first tried matched. A 130-instruction script with 41 calls is 41 easy transcriptions and one or two real questions; WORK SCALES WITH DISTINCT RESIDUES, NOT SIZE. A CHAIN OF ARMS THAT EACH STORE AND RETURN IS NOT ONE THAT ASSIGNS AND JOINS. OvlFunc_880_20082f4 maps a character code through fifteen arms; written with a join variable gcc comes out at 58 LINES AGAINST 86, because it speculates each arm's computation above its test -- legal, an assignment has no side effect. Putting the store inside each arm matches exactly: a store cannot be speculated above a test, and cross-jumping merges the fifteen identical strb/pop/bx tails into the one the ROM shows. A ROM whose arms all branch to a single store does NOT mean the source had a join variable; the discriminator is whether the arm's COMPUTATION sits before or after its test. An explicit goto chain is byte-identical to the join form (gcc canonicalises it), so this is reachable only by giving each arm a side effect. The follow-up matters: OvlFunc_945_2009894's chain is the same shape but each arm CALLS __MessageID, so the plain if/else is already correct -- AN ARM WITH A SIDE EFFECT NEEDS NO REWRITING. TWO PASSES SHARE THE -fno-gcse FLAG NAME AND ONLY ONE YIELDS. Batch 175's three constant parks had it inert; Func_807a550 MATCHES with it. A constant materialised once and kept in a register is cse.c's LOCAL sharing and does not yield; a redundant LOAD sunk onto the only path needing it is the global pass and does. A THIRD direction turned up in Func_8011164: a pool load HOISTED OUT of a loop the ROM reloads in, where -fno-gcse is inert because that motion is loop.c's invariant hoisting and gcc-2.96 has no switch for it. The recorded GCSE case rom_f0254_a_b.c is the mirror image, a load SUNK INTO a loop, and there the flag works. POOL SCAFFOLDING, AND A WAY TO REMOVE IT. Func_80f6148 is two lines SHORT because the ROM has two `b` instructions to the immediately following label, jumping over a mid-function literal pool; Func_801bd98 is one line LONG for the same reason with the sign flipped. A one-line difference in EITHER direction next to a `b` whose target is the next label is pool placement. AND IT CAN BE FIXED INDIRECTLY: batch 175 recorded that gcc pools a small literal stored through a halfword pointer, but OvlFunc_921_2008384's ROM does not -- `*(short *)a = 0xa;` gives `ldr r3, =0xa` at 135 of 133 with 66 differing, `unsigned short` is identical, and `n = 0xa; *(short *)a = n;` gives `mov r3, #0xa` at 133 with 10. The two-line gain is NOT the mov/ldr swap: removing the pool ENTRY removed a whole pool and with it the skip jump. A POOLED CONSTANT COSTS MORE THAN ITS OWN INSTRUCTION. Also: NAMING THE SCALED BYTE OFFSET flips a reg+reg load's operands (`k = idx*4; *(int *)((char *)base + k)` puts the pointer in Rn where `base[idx]` puts the scaled index -- the only differing line in an otherwise exact 42); NAMING IS A FLOOR NOT A CEILING (Func_801d014 needs a destination address moved LATER and no name can do that -- naming the loaded byte costs eight lines, naming the source address five); AN ACCUMULATOR'S INITIALISER WANTS TO BE AS EARLY AS THE SOURCE ALLOWS, twice more and for two different reasons; A RUN OF SEPARATE CONSTANT ANDs ON ONE LOADED BYTE IS THE BITFIELD TELL (four `&=` statements fold to `& 3`, four bitfield assignments do not, and gcc uses the smallest load covering each field which is why one struct gives a mix of byte and halfword accesses); and THE ADDRESSING FORMS ARE A PREFERENCE NOT A GUARANTEE (Func_8011f54 needs a named pointer for `add / ldrb`, TestCollision uses the identical spelling on the SAME tables and gcc folds it back, because pressure is higher). A CORRECTION: batch 175's three constant parks all had callee-saved registers committed, suggesting gcc shares a repeated constant only when one is spare. ovl_7d95dc/200a904.c was screened to test that -- ten calls, `push {lr}` alone -- and GCC ADDS r5, ITS PUSH AND ITS POP to avoid rebuilding a two-instruction constant three times, at identical length. A zero-pressure push list is no protection. TOOLS: tools/lowpressure.py is new -- every other ranking scores shape, adjacency or lever-applicability, none scores REGISTER PRESSURE, which is what the surviving walls are made of. It also rejects bodies that read r4-r7 before writing them and never push, which is the asm/rom_f9000 m4a engine: private calling convention, can never be C, and every scan kept offering them. family_siblings.py gained the DUP-CONST hard skip, which disqualified eight of its top eleven on the first run. NINE PARKS, of which rom_f6000/80f6148.c is worth singling out -- NO CALLS AT ALL, so nothing forces callee-saved registers, and all 71 differing lines are the same three values allocated r0/r4/r5 by gcc and r5/r6/r7 by the original build. Cleanest specimen of the REG_ALLOC_ORDER hypothesis yet |
| [batch-177](reports/batch-177.md) | 6 | ready to port -- TWO STANDING REJECTS WERE WRONG AND BETWEEN THEM HELD 610 FUNCTIONS OUT OF EVERY RANKING. BRANCH-OVER-POOL (501 functions): the premise "the compiler never emits a mid-body pool, so one proves hand-written asm" is true OF old_agbcc; this tree builds with gcc-2.96, which emits them freely. The compiler name was in poolblocked.py's own docstring and was never read against the rule it was supporting. R8-R11 (109 functions): -fcall-used-r4 takes r4 out of the callee-saved set, so a function with four values live across a call has only r5/r6/r7 and the fourth goes to r8, with exactly the `mov r7, r8 / push {r7}` prologue the reject read as a wall. THE FALSIFYING EVIDENCE WAS OUR OWN OUTPUT -- our screens have been emitting r8 and r10 for rounds and the reject was never compared against it. Relaxing it to a reported column left 122 candidates where the full reject left twelve, and three of the first four tried matched WITH NO LEVER AT ALL. r12/r14 holding a value remain real walls and the reject is kept for those two only. THE GENERAL RULE: a heuristic that was never wrong is not a certainty; both of these began as sound triage ("avoid a fight you cannot win") and hardened into "this cannot be done". RETESTING A REJECT COSTS ONE SCREEN AND RETURNED 610 FUNCTIONS. THE CALLEE'S RETURN TYPE FIXES EVERY CALL SITE AT ONCE. OvlFunc_974_20088c4 is a 232-line debug script with 53 calls, each three constant arguments; it screened at the ROM's EXACT LENGTH WITH 159 OF 232 DIFFERING, every call the same way, the ROM setting r0 LAST and gcc first. Declaring the two callees `int` instead of `void` -- nothing else changed, neither result used -- matched all 53. Withholding the prototype entirely does nothing. gcc reserves r0 for a value-returning call and evaluates that argument last; for a void call r0 is just another argument register and goes first. The recorded lever is about the PRESENCE of a prototype; the RETURN TYPE is a separate and stronger knob, and the same fact as the epilogue tell read from the other side. When every call in a function has its arguments in the wrong order, read the return types before anything else. A PARK IS A RECORD OF WHAT WORKED, NOT ONLY OF WHAT FAILED. GetEquippedItem and Func_807882c both matched on the pointer-typed-operand lever and NEITHER has an elevated sibling that shows it -- the lever was written down in src/non_matching/rom_77000/8078588.c, a PARK in the same address range that found the register-offset form, used it, and then failed on an unrelated allocation residue. family_siblings.py ranked only by ELEVATED siblings and was blind to half the family's record; it now prints a `parks near` column. Before screening, read the parks nearest the target's address. TWO NEW LEVERS. (1) TWO PLAIN LOCAL INITS ARE EMITTED IN SOURCE ORDER -- swapping `off = 0xd8; i = 0;` to `i = 0; off = 0xd8;` took Func_807882c from 4 differing of 35 to 2. This is NOT the argument-block rule, which says the OPPOSITE for pooled loads inside a call's argument list; same words, opposite answers, different scopes. (2) A BYTE-SIZED ZERO CAN BE SERVED BY A REGISTER WHOSE LOW BYTE IS ALREADY ZERO -- Func_809a3c4 holds 0xc0<<10 for two word stores, gcc reused it for `*(char *)(p+0x5a) = 0` and emitted nothing, leaving the body ONE LINE SHORT of the ROM, which reads as a missing statement and is not. A named local restored the separate mov and took it to exact length. Read a one-line-short body at a strb/strh as possible SUBWORD REUSE before hunting a missing statement; word stores cannot do this, byte and halfword stores can. THE LEVER IS THE STATEMENT BOUNDARY, NOT THE NAME. Func_80a1814: `r[0xf]=0xfe; t[0xf]=0xfe-0xff;` gives 43 of 44 with 16 differing; naming it (`n=0xfe; ...; t[0xf]=n-0xff;`) gives 44 of 44 with ONE; `n-=0xff;` as its own statement MATCHES. The folded subtraction is materialised with a mov, the standalone one survives as a sub on the register the previous store just used. THE BASIC-BLOCK LEVER IS BOUNDED BY THE REGISTER FILE: batch 176 broke the arg-interleave wall with FOUR constants in a dominating block; OvlFunc_932_200a6c0 needs EIGHT and gcc spills all of them, 136/28 becoming 144/142. Apply it to the constants the diff names, never to every constant in the function. TWIN PAIRS ARE STILL THE CHEAPEST THING IN THE TREE: Func_800d924 and Func_800d98c are identical but for the return value (-1 vs the pointer) and both matched on the FIRST screen. 22 PARKS. Unbroken classes: duplicate-constant CSE (13 flags ruled out, cse.c local sharing, no flag reaches it), stack-argument materialisation (the ROM builds both values before storing either; the one site where the two COINCIDE matches), allocation-order rotation (8020150, 80b6a60, 8078870 -- the high register comes out right, which of the rest gets it does not), copy-into-a-register (20092a4 is a second specimen of a recorded negative: the named-intermediate lever needs both values SIMULTANEOUSLY LIVE, which a shift's input and output are not), and loop-invariant-constant-not-hoisted (809a3c4 -- gcc prices mov+lsl below a pool load for 0x2000, the ROM priced it the other way; the dominating-block lever is inert in both placements). OvlFunc_917_20092f4 parks at 230 of 230 with FOUR differing and is worth reading for what came free: two jump tables from two plain switches and every cross-jumped tail including two fall-throughs, PROVIDED THE CASES ARE ORDERED BY THEIR LABEL ADDRESSES IN THE ROM RATHER THAN NUMERICALLY. Its four differing are two identical calls in one switch arm with their argument movs transposed, while the identical call in a DIFFERENT ARM OF THE SAME SWITCH matches -- the ROM emits both orders itself and no source property distinguishes them. 1,923 functions remain in asm/ |
| [batch-178](reports/batch-178.md) | 10 | ready to port -- TEN FUNCTIONS, ZERO NEW PARKS, SEVEN OLD ONES RETIRED. asm/rom_b5000/rom_bbb0c_a_c_c_a_a.s is GONE IN ITS ENTIRETY: ten TickStatusCounter routines at 0x080bf250..0x080bf524 in one .c, no split, linker script unchanged. Nine of the ten took ONE SCREEN. A "REDUNDANT" REGISTER COPY IS USUALLY A SECOND READ GCC HAS CSEd. All ten open with `ldrb r2,[r5] / mov r3,r2 / cmp r3,#0` and r2 is dead immediately after -- the next read of that byte is a fresh ldrb -- so the mov was filed under the copy-into-a-register class (recorded as a wall three times) and SIX OF THE TEN WERE PARKED ON IT. The park went further and recorded a negative: splitting the value across two named locals does not produce the copy because gcc coalesces them. All true, all beside the point. THE COPY IS A SECOND READ OF *p. With a local there is ONE read -- `n = *p; if (n == 0) goto fail; n += 0xff; *p = n;` gives 31 instructions vs the ROM's 32 with 26 differing. Written entirely through the pointer there are TWO, the second is CSEd into the mov, and it matches on the first screen: `if (*p == 0) goto fail; *p = *p + 0xff;` gives 32 vs 32. THE DIAGNOSTIC: a body ONE INSTRUCTION SHORT with a `mov rA, rB` occupying the missing line in the ROM is a CSEd RELOAD, not an allocation artifact. Count the reads in your C against the reads the ROM makes before its store; if the ROM loads once and copies, the source read twice. THE GENERAL FORM, which had never been written down: NAMING A VALUE IS NOT FREE. Every lever in the notebook that says "give it a name" buys a statement boundary and pays for it by collapsing repeated reads into one. WHEN THE ROM SHOWS MORE TRAFFIC THAN YOUR VERSION RATHER THAN LESS, TRY REMOVING A NAME BEFORE ADDING ANOTHER. THIRD REJECT IN FOUR BATCHES FOUND WRONG BY RETESTING: branch-over-pool (501 functions), r8-r11 (109), and now this. All three are the same shape -- a correct observation (the compiler does not emit mid-body pools; high registers mean an allocation fight; that copy does nothing useful) promoted into a conclusion about what the source COULD NOT HAVE BEEN. None needed new technique; all three needed one retest. THE PARKS-NEAR DISCIPLINE PAID OFF ON ITS FIRST USE. Func_80bf484 was the UNPARKED member of a twin set; its two PARKED twins carried the 31-against-32 measurement that made the reading obvious, plus two levers still needed and expensive to rediscover -- three `return 0` paths sharing one block via `goto fail` (three plain returns make gcc hoist `mov r0,#0` to the top and emit the exits reversed), and `*p = *p + 0xff` rather than `(*p)--`, which on an unsigned char wraps the store in lsl/lsr #24. The park was worth more than either elevated sibling would have been. ONE NEW LEVER: in the three routines that also clear a companion signed byte, THE NAMED ZERO GOES BETWEEN THE STORE AND THE TEST -- the ROM materialises `mov r7,#0` after `lsl r3,#0x18`, and assigning it any earlier costs six differing lines. The batch-176 live-range rule at single-statement resolution. ONE CORRECTION TO THE VERIFICATION RULE: .gcc2_compiled. IS A PER-TU SYMBOL, not a per-function one. The standing check (every elevated address must carry a .gcc2_compiled. local at the same address) quietly assumed one function per file, which had been true until now. A ten-function TU emits it ONCE, at the first function's address; the remaining symbols at their exact ROM addresses prove the layout. Do not read its absence on functions 2..N as a failure. WHY TEN AND NOT FIVE: the file is atomic -- ten interleaved functions, all one routine. Splitting it ten ways to pace the count would have added ten linker entries and ten files to describe one lever. NEXT: SEVENTEEN PARKS describe themselves as ONE SHORT or ONE LINE SHORT. That phrase is now a specific diagnosis rather than a symptom, and re-reading those seventeen against it is the cheapest work visible in the tree; five name the copy-into-a-register class outright. 1,913 functions remain in asm/. FIRST BATCH IN THIS PROJECT'S LOG WHERE src/non_matching/ GOT SMALLER |
| [batch-179](reports/batch-179.md) | 5 | ready to port -- THE READ-COUNT LEVER RUNS IN BOTH DIRECTIONS, which is the batch's result and a correction to how batch 178 stated it. That batch said: a body ONE INSTRUCTION SHORT with a `mov rA, rB` in the ROM's version of the missing line is a CSEd reload, so REMOVE A NAME. Right, and it landed Func_80b06ec and Func_80bf54c here on the first screen. Func_808ddb8 is the same rule with the sign flipped. It walks a -1 terminated key/value table and the ROM loads each entry ONCE and uses it twice -- `ldrsh r3,[r2,r4]` for the sentinel test then `cmp r0,r3` for the key. Written with two reads through the pointer gcc cannot prove the second names the same object and RELOADS IT THROUGH A RECOMPUTED ADDRESS (`sub r3,r2,#0x2 / mov r4,#0x0 / ldrsh r3,[r3,r4]`), 29 lines against 26. One read into a named local, tested twice, matches on the first screen. THE DIAGNOSTIC IS WHICH WAY THE EXTRA INSTRUCTION POINTS: an extra COPY in the ROM means the source read twice and we named it -- remove the name; an extra LOAD in OURS means the source read once and we did not -- add one. Batch 178's "naming a value is not free" stands; this is its other half. The question is never whether to name but HOW MANY TIMES THE ROM TOUCHES MEMORY. A NAMED ZERO CAN CSE WITH A CALL ARGUMENT. Func_80a7440 stores 0 through a halfword pointer and gets it POOLED (`ldr r3,=0x0` against the ROM's `mov r3,#0x0`) -- the recorded halfword-store pooling rule, whose recorded fix is to name the value, and that fix MATCHES Func_801ce48 in this same batch. Here it backfires: two lines later the function calls Func_80a77a4(0), and once the stored zero has a name gcc CSEs the two zeroes into one register and drops two instructions, 21 lines against 23. Two separate names do not help -- gcc CSEs on the value, not the spelling. BEFORE NAMING A STORED CONSTANT, LOOK FOR THE SAME CONSTANT IN A NEARBY ARGUMENT LIST. THE SENTINEL'S LIVE RANGE DECIDES WHETHER IT GETS r0. Func_8092ba8 returns -1 or a looked-up value; with `v = -1` beside its use gcc keeps the result in r0 throughout and returns it directly, one instruction SHORT of the ROM's `mov r0, r1` at the join. Moving that one assignment ABOVE the base-pointer load overlaps the live ranges so they cannot share r0 and the copy reappears -- that was the match. Renaming the variable and declaring it first does NOT do it (24 lines, 6 differing, unchanged): it is the ASSIGNMENT POINT that matters, not the declaration. ALSO: THE OFFSET BELONGS IN ONE REGISTER -- `*(u8**)(base + ((i<<2) + 0x14))` gives `add r2,r3 / ldr r2,[r2,#0x14]` where the ROM folds the 0x14 into the index and uses a register offset; naming the offset in an int local produces the ROM's form. And Func_801ce48 needed BOTH halves of the read-count rule at once: the counter read twice through the pointer AND the reload value 2 named to stop halfword-store pooling. PARKS: Func_80a7440 (23/23, 16 differing, the named-zero collision above) and MapActor_WaitAnim (26 against 29, THREE SHORT) -- the latter is the mirror of batch 177's branch-over-pool correction: there a `b` to the next label was ours and not the ROM's, here it is the ROM's and not ours. The ROM tests the frame counter BEFORE the body, reaching the test with a branch over the increment; gcc rotates the loop so the guard sits at the bottom and the branch disappears. Three spellings tried, all 26 lines. 1,906 functions remain in asm/. tools/cse_reload.py still lists 89 parked functions carrying the load-then-copy shape, and the two-directional reading means the ones where OUR version is LONG are now candidates too |
| [batch-180](reports/batch-180.md) | 10 | ready to port -- TEN FUNCTIONS, NINE OF THEM PARKED, and every one of those parks had concluded "not reachable from source". ONE OF THEM WAS MINE, from batch 179, and its central claim was wrong: it recorded that Func_801cee0's branch polarity was "source-inert across the plain break, an explicit goto inc, and an explicit goto out". All three keep the RETURN as the if's then-body, which is the thing that decides the polarity. `if (*p <= N) break; return;` MATCHES where `if (*p > N) return; break;` does not -- gcc expands if(C)X as invert-C-and-jump-past then threads the one-jump block, so WHICH SIDE OF THE IF CARRIES THE EXIT picks the condition, and nothing else moves. Three negative results that all share a hidden constant are one negative result. FIVE NEW LEVERS. (1) A NARROWER TYPE ON THE COMPARED COPY, NOT A SECOND READ: where the ROM has `ldrb rA / mov rB, rA` with the copy BEFORE the cmp, every int-typed spelling lost the mov or put it after the branch; declaring the compared copy `unsigned char` while the stored value stays wide makes the QImode/SImode pair non-coalescable, the copy lands right, and a three-way register rotation resolves for free -- 45 differing to 1. THIS IS THE MIRROR OF THE READ-COUNT LEVER, NOT AN INSTANCE OF IT: a genuine second read there is byte-identical to one read because CSE folds it away. (2) A SECOND, GIV-ONLY INDEX VARIABLE relocates induction updates past a store -- with -fcall-used-r4 the counter is spilled around the call and sched2 cannot disambiguate a char store from the spill slot, so order is frozen at RTL order; one index surviving only in the loop compare plus one appearing only in address expressions (so biv elimination deletes it and keeps its givs) puts the updates where the ROM has them. (3) ASSIGNMENT ORDER OF AN ADDRESS-HOLDING POINTER fixes register birth order where declaration order is inert, and `volatile` must be on the POINTEE as well as the pointer or gcc keeps the p==sp+0 equivalence and the single non-loop store folds to `str r3,[sp]`. (4) THE RETURN TYPE IS AN ORACLE IN BOTH DIRECTIONS -- a ROM epilogue popping into r0 is POSITIVE EVIDENCE OF A void FUNCTION, a cheap check to run before writing anything; and the lever applies to the function's OWN signature (declaring Func_80aaf58 `int` with no return gives the ROM's `pop {r1}/bx r1`). (5) A REPEATED POOLED CONSTANT MAY BE AN OFFSET RE-READ, NOT A VALUE REBUILT -- Func_80ae714 was flagged duplicate-constant CSE but the two `ldr =0x219` are the loop bound re-read across a call; re-reads yield to spelling, rebuilds do not, and caching the bound costs 51 differing. TWO NEGATIVES WORTH AS MUCH AS THE MATCHES. `goto fail` IS A NEGATIVE when gcc already cross-jumps the returns: with two plain `return -1` it merges them into the ROM's shared block and places it BEFORE the success tail, and an explicit goto to a trailing label inverts the two -- the recorded shared-exit lever is for when our output is MISSING the shared block (GiveDjinni). And Func_80a3d9c is now re-parked WITH ITS MECHANISM NAMED: three of its four missing instructions are a 0xf800 mask that combine folds, because the value is single-set from an `ldrh` so nonzero_bits <= 0xffff and simplify_shift_const rewrites mask-then-shift into shift-then-mask and drops the mask; if the width were unknown the rewrite would leave an AND with no Thumb insn and no valid split and combine would REJECT the combination, which is exactly the ROM's four lines. Only two states are reachable from source (31 lines constant-hoisted, 28 folded) and the ROM is neither; it closes by itself if the Func_801f730 fold is ever defeated. Thirty spellings and six flags measured. FIRST BATCH SCREENED BY PARALLEL SUBAGENTS, at the user's request -- nine of ten. Each was sandboxed to its own scratch directory, forbidden to run make, and required to report (lines, differing) for EVERY spelling rather than only the winner; those measurement tables are most of the value, because the negatives are what identify a mechanism. Every match was re-verified by make compare, which remains the authority. One agent found a real bug in tools/tryc.py: the helper-alias hint searched ("divsi3","udivsi3","modsi3","umodsi3") in order and "modsi3" is a SUBSTRING of "umodsi3", so it named the WRONG alias -- a line someone would have pasted into an overlay.ld. Fixed longest-first, and it now says to check whether the alias is already present. 1,896 functions remain in asm/. The read-count rule has unparked ELEVEN functions across batches 178-180, and in SIX of those the park had specifically concluded a register difference was unreachable. A REGISTER ROTATION BESIDE A MISSING OR MISPLACED COPY IS A SYMPTOM OFTEN ENOUGH THAT IT SHOULD NOT BE RECORDED AS A BLOCKER CLASS ON ITS OWN |
| [batch-181](reports/batch-181.md) | 8 | ready to port -- THE PARKED SET HAS BEEN RANKED ON THE WRONG NUMBER for most of this project's life, and re-measuring it CHANGES VERDICTS, not just numbers. A POSITIONAL diff compares instruction i to instruction i, so the moment one side has an extra instruction everything after it reports as differing; tryc.py --align reports disagreeing REGIONS. tools/realign.py (new) re-measures the parked set on that metric. HeightTile_4 recorded 22 of 28 measures 3; OvlFunc_939_20092a4 recorded 38 of 55 measures 2 AND THEN MATCHED; OvlFunc_952_2008264 recorded 33 measures 6. It also INVERTED A RECORDED FLAG COMPARISON: that park measured default at 33 and -fno-rerun-cse-after-loop at 57 and called the flag a loss; aligned the flag is BETTER, 8 against 9, and makes all three of its pool loads exact. A PARK OF MINE FROM BATCH 177 WAS WRONG. I read OvlFunc_939_20092a4's `lsl r3,#6 / mov r5,r3 / add r5,#0xe6` as a shift-in-place plus a copy and filed it under the named-intermediate lever's recorded failure mode. THE COPY IS NOT ABOUT THE SHIFT: Thumb has no three-operand add for an immediate above 7, so `+ 0xe6` must be destructive and the two values must be separate pseudos. `ang = X << 6; ang += 0xe6;` is ONE pseudo -- the += reassigns -- and gcc folds the copy away; `ang = (X << 6) + 0xe6;` as one expression emits the ROM's three instructions. WHERE THE ROM HAS <op> rX,#n / mov rY,rX / add rY,#K WITH K > 7, WRITE IT AS ONE EXPRESSION, NOT TWO STATEMENTS -- the mirror of the recorded derivation-as-its-own-statement lever, which wants the boundary. A FIX FOR A CLASS THE NOTEBOOK LISTS AS UNSOLVED: storing a literal ZERO to a `short` lvalue is ALWAYS a halfword pool load. Seven spellings measured (short*, unsigned short*, struct field, int h:16 bitfield, &= 0) and every one gives `ldrh r3,.Ln / .word 0`; only a REGISTER-ALLOCATED LOCAL escapes -- `short zero = 0; *p = zero;` gives `mov r3,#0 / strh`, because the movhi expander force_const_mem's a CONST_INT when the destination is memory. The same root cause produced a second apparently unrelated difference: the ldrh needed its pool in range so arm_reorg dumped the pool mid-function and branched over it. A STRAY JUMP-TO-NEXT-LABEL IN THUMB OUTPUT IS USUALLY A POOL DUMP, NOT A JUMP-OPTIMISATION FAILURE. DELETE THE LOCAL closed three of the five unparked functions, each against its park's recorded conclusion. OvlFunc_931_2008d08 held an `int k` because the ROM shared one `mov r2,#0x14` across two stores; six spellings of that local (four types, both declaration orders) all measured EXACTLY 7, and the notebook's own rule says identical counts across unrelated spellings indict the variable itself. A REGISTER SHARED BETWEEN TWO STORES IS NOT EVIDENCE OF A SOURCE VARIABLE. OvlFunc_931_200807c carried two address-only locals; OvlFunc_964_20094ac carried the read-modify-write through a named value. PROMOTE_MODE MAKES EVERY HImode LOCAL SIGN-EXTEND: arm.h sets UNSIGNEDP = TARGET_MMU_TRAPS != 0, which is 0 here, so no type on a halfword local can produce `lsr` -- OvlFunc_960_2008ce4's park was right that unsigned is not enough, and the fix is on the READ side, assigning into an unsigned int before the store. Also: a dead four-byte stack local survives -O2 as a `char` array but NOT as a struct of four bytes, and `mov rX, sp` before a `strb` is the byte-granular address-taken tell; and when gcc reuses a compared-against-zero register as a stored constant and the ROM does not, OVERWRITE THAT VARIABLE BEFORE THE STORES. A MISTAKE AND WHAT CAUGHT IT: converting asm/rom_8a000/rom_92950_c_c_c_c.s WHOLE dropped a `.section .rodata` block exporting .L9ed80 which stage1.ld pulls on its own line and another TU references. The link failed with an undefined reference -- CAUGHT BY make compare, NOT BY THE SCREEN, which cannot see data other objects need. Hand-split, because split_s.py cuts at FUNCTION boundaries and would have kept trailing data with the function it follows. GREP A .s FOR .section, .global AND .incrom BEFORE CONVERTING IT WHOLE. TWO TOOL BUGS, BOTH MINE, BOTH FIXED. tryc.py's batch-180 pool-entry guard exists because an agent found a candidate that DUPLICATED a pool word and screened OK -- four bytes larger than the ROM, and make compare would have failed; the existing .text size check should have caught it and silently did not, because text_size(reference) returns None whenever the reference will not assemble standalone, which is most overlay references. That guard then had a false positive of its own: a reference may spell a pool word as a bare `.word` under a local label rather than `=value`, and counting only the `=` form under-counts it -- Func_8011644 is exactly that shape and was failed wrongly. Both fixed, and the OK line now SAYS when the size check did not run, because silence there reads as verification. realign.py was blind to overlay parks (no address comment on their .thumb_func_start), so 33 of the first 60 scanned read as stale; ~2000 parked functions are overlays and none could be re-ranked until it indexed the symbol name too. PARKS: OvlFunc_952_20083b0 at 2 of 88 with its tie-break characterised from sched2's READY LISTS rather than inferred -- the lsl always has a lower LUID because the constant is force_reg'd in precompute_register_parameters, and it wins the tie only because an intervening CALL gives it a fourth dependent; confirmed by construction, swapping that call for a two-register-argument one reproduces the ROM exactly. Nineteen flags and ~20 source spellings measured. One park DELETED as genuinely stale (OvlFunc_898_2008acc, elevated in 54498ab2, park never removed). 1,888 functions remain. Nine subagents contributed screens; every match re-verified by make compare, and one of them turned out to depend on a TOOL BUG rather than a real match, which is exactly why |
| [batch-182](reports/batch-182.md) | 6 | ready to port -- THE BATCH'S THEME IS LIVE RANGE AS THE THING YOU ARE ACTUALLY WRITING. Four of six turned on where a value's first definition sits rather than on how it is spelled, and both new levers are statements about live length rather than about types or operators. A RE-MATERIALISED CONSTANT AFTER A JOIN IS TWO LOCALS -- the read-count rule's mirror. OvlFunc_941_2008210 has a guarded prologue and a join and both halves pass 0x15 as a stack argument; one shared local for it is the obvious reading and costs EIGHTEEN instructions, because the shared pseudo's live range spans the whole function, allocno_compare ranks it below the short-lived pair locals, gcc gives it r10, and all eight stack-argument sites pay `mov r3,r10 / str r3,[sp]` where the ROM pays one `str`. The reference says so directly: it emits `mov r5,#0x15` inside the if body and AGAIN after the join, on a path where r5 already holds it, and GCC DOES NOT RE-MATERIALISE A VALUE IT KEPT LIVE ACROSS A BRANCH. A second `mov rN,#imm` where rN already holds imm is a SECOND VARIABLE being born. Stated as a pair with the read-count rule: a load followed by `mov rB,rA` is a CSEd second read of ONE object, and a constant materialised twice across a join is TWO objects -- LOADS COLLAPSE TOWARD ONE VARIABLE, CONSTANTS SPLIT INTO SEVERAL, and in both cases the number of materialisations in the ROM is the number of source-level things. AN INITIALISER IS NOT AN ASSIGNMENT, and this is the first source-level control the notebook has for LOWERING an allocno's priority rather than raising it. `int redraw; redraw = 1;` and `int redraw = 1;` emit the SAME INSTRUCTION AT THE SAME INDEX and allocate differently: the initialiser makes the first def function entry, lengthening the live range, lowering n_refs/live_length in allocno_compare, demoting the flag from r5 to r7 and letting the CSE-hoisted &gKeyPress take r5, which is the ROM. 13 aligned regions to 3, for one character. Every previously recorded birth-order lever works by RAISING a priority; Blocker 2 now has both directions. The last region closed on statement placement: SCHED2 SINKS A STORE FREELY INTO AN ARGUMENT BLOCK BUT WILL NOT HOIST IT BACK OVER A CALL, so `redraw = 0;` anywhere in the guarded block except after the final bl gives the ROM. SIX MESSAGE IDS WERE SYMBOLS (_MSG_810.._MSG_813, _MSG_ad0, _MSG_d1c, _MSG_c20) -- half the batch on one recorded tell, each a shiftable byte gcc would have built with mov+lsl and each given a whole pool word by the ROM. Two of them extended the tell: GCC WILL SPEND A CALLEE-SAVED REGISTER ON A SYMBOL ADDRESS AND NOT ON AN INTEGER IT CAN REMATERIALISE (Func_80b3284 holds 0xd1c high and reaches 0xd1d..0xd20 with add #K; as an integer each site gets its own ldr and the push list changes), and the knock-on is not the two instructions of the constant -- once the id is in a register cse's related_value stops deriving 0xc21/0xc22 from the base, the ROM's `add r0,r5,#1 / add r5,#2` pair goes and the r5/r6/r7 assignment rotates, 27 aligned to 13. MEASURE WHAT CSE CAN DERIVE FROM A POOL SLOT, NOT THE COST OF THE CONSTANT. BLOCKER 1b IS PARTLY REACHED: the entry said no formulation reaches the ROM's word-sized pool load for a halfword store, and the entry's own reasoning explains why exactly two shapes can. The load width follows the width of the eventual store, so anything keeping the value inside the store's expression narrows; what cannot narrow is a value that must already exist in a register before the block that stores it. `int inval; inval = 0xffff;` as the FIRST statement, stored in a guarded arm, gives `ldr rN,=0xffff / strh`; assigned inside the arm gcc folds it back to a HImode const_int -1, commons it with the `mov #1 / neg` from the `!= -1` test and comes out three instructions short. IT IS THE DOMINATING BLOCK THAT DOES THE WORK, NOT THE int TYPE. Worth 27 aligned to 9. The corpus template was already in the tree and unnoticed (src/rom_9000/rom_ea54_c_b.c); the blocker entry read as a dead end so nobody grepped for the shape. 1b is now 'the value must live in a register the store cannot narrow into', which also covers batch 181's constant-zero escape at a different width. TWO PLACEMENT LEVERS on the inn, neither a type nor an operator change: delete the halfword pointer and write its address expression at all four use sites, or the offset build hoists above the actor lookup; and name the gState base INSIDE the else arm, since above the branch gcc takes a fourth callee-saved register and rotates the other three -- the local's SCOPE, not its existence, was the variable. PARK: FieldMove_NoTarget at SIX aligned of 137 with instructions 12-136 identical including every register, the wall quantified from global.c:allocno_compare rather than inferred -- the kind variable needs live length 18 to beat the gState slot pointer's priority of 1600, which needs it assigned LAST, but the ROM's emission order needs it assigned FIRST or the pooled base dies before its load and the anti-dependency reorders the two bases. Mutually exclusive at every statement order tried; gap exactly one RTL instruction, measured on both sides. THE .s SPLIT HAZARD IN ITS CHEAP FORM: rom_b0070_c_c_c.s holds five functions and a trailing .rodata block, and taking the split at the FIRST function leaves the data with the four that stay. The expensive case is when the target is LAST, because then the data travels with it into the .c -- check which end the target is on before choosing the split. The overlay split was verified byte-neutral with make compare green and no .c present, which is what separates a layout mistake from a bad decompilation. 1,884 functions remain in assembly: 653 unparked and 286 parked in the main ROM, 625 unparked and 320 parked across the overlays |
| [batch-183](reports/batch-183.md) | 7 | ready to port -- THE ROUND BEGAN WITH THE CANDIDATE PIPELINE EMPTY. filtered.py returned zero and a relaxed sweep at >=4 calls also returned zero, which made the remaining 1,278 unparked functions look out of reach. They were not: THE FILTER WAS REJECTING THE SHAPE BATCH 182 HAD JUST SOLVED. Its duplicate-constant rule rejected any function repeating an expensive constant ANYWHERE, which is exactly the join shape -- OvlFunc_941_2008210, batch 182's own specimen, would have been rejected by the tool meant to find it. Teaching the rule to ask WHERE the repeats sit turned zero candidates into 26, and FIVE OF THIS BATCH'S SEVEN CAME FROM A LIST THAT DID NOT EXIST AT THE START OF THE ROUND. Then the new marker turned out to conflate THREE shapes whose fixes are inert or harmful on each other, and it took two corrections to separate them. First: a repeated `mov rN,#imm8` into a STACK-ARGUMENT slot is two source VARIABLES and splits; a repeated POOLED id consumed as r0 by a `bl` is one LITERAL that rerun-CSE commons and needs CSE_CFLAGS, unreachable from source because constant propagation folds any name back to the same const_int -- four spellings of OvlFunc_920_2008304's flag id all measured identically. Second, and worse: the test asked 'reaches a bl' against 'everything else', so STRUCT OFFSETS -- reaching neither a stack slot nor a call -- read as the split shape. SEVENTEEN OF THE TWENTY-SIX WERE LABELLED [split] ON THAT BASIS AND EXACTLY ONE ACTUALLY IS. OvlFunc_939_2009668 has no stack frame at all, so there was nothing for the lever to act on, and giving each block its own offset local went 33 aligned to 38: two locals holding one constant fold to a single const_int and stay live across the branch, where the ROM rebuilds per block, and gcc-2.96 Thumb already builds a shiftable constant with mov/lsl from a bare literal. Now three-way, [split]/[cse]/[offset]. And [cse] is a HINT NOT A VERDICT -- OvlFunc_932_200a934 matched on its first candidate with no flag, because its two sites are in MUTUALLY EXCLUSIVE ARMS and rerun-CSE does not common across those; the flag is for the guard/set shape where one use DOMINATES the other. BLOCKER 1b NOW HAS A DIRECTION SWITCH AND THE ROM SETS IT. OvlFunc_933_2008cd0 shows 1b reaches down to THE VALUE 1: `*(short *)(p+K) = 1;` narrows to a HImode const_int and, although thumb.md's *movhi_insn has an I alternative that would give `mov r3,#1`, the value arrives through force_reg from the movhi expander and comes out of the LITERAL POOL -- a pool word holding 1, absurd enough to be misread as a scheduling artefact. OvlFunc_939_2009668 wants the same mechanism from the other end: the ROM has `mov r3,#0xa / strh`, a literal at the store gives the pooled ldrh, and `int t = 0xa;` in the ENTRY block is worth 19 of 123. Set beside FieldMove_NoTarget's park, where an int local crossing a block boundary is what FORCES the unwanted `ldr rN,=0xffff`, all three are one rule: THE LOAD WIDTH FOLLOWS THE EVENTUAL STORE, SO A VALUE THAT MUST EXIST IN A REGISTER BEFORE THE STORING BLOCK CANNOT NARROW. When the ROM pools, keep the value inside the store's expression; when the ROM has mov/strh, hoist it into a dominating block. Also: a long jump where the ROM has a conditional branch was a SYMPTOM of 1b, the pool words having pushed the epilogue out of range, not a polarity problem. THE JOIN-SPLIT LEVER BOUNDED FROM BOTH SIDES, which is most of the batch's value because it is cheap to try and all three failure modes look like near-misses. It has a SWITCH-ARM FORM (FieldMove_Target: folded inline gives one pool word where the ROM has two and an add, 11; one shared local spans the switch and moves the diff into the prologue, 13; ONE LOCAL PER ARM defeats the fold and keeps the range short, exact -- and the cost runs BACKWARDS from the usual dominance rule, naming the value ADDS the instruction pair the ROM has). It is INERT when both uses already left the assignment's block (OvlFunc_898_20084a0: one local and a split pair compile byte-identically; split only when the shared range spans the join and forces a high register, and CHECK THE PUSH LIST FIRST). And it is POWERLESS against two variables holding the same constant (Func_8021390: five assignment positions, two named zeros and five types all measured exactly 36, and proved by construction -- change ONLY the pre-call argument from 0 to 9 and gcc's whole allocation flips to the ROM's shape. The split is about VARIABLES, the fold is about VALUES, and the fold wins). A DOMINATING-BLOCK LOCAL CAN BEAT THE FLAG: OvlFunc_898_20084a0 runs 12 aligned with literals, 6 with -fno-rerun-cse-after-loop, and 4 with the constant named in the dominating block and NO flag, closing to exact after two more applications -- the flag removes the CSE hoist but leaves three argument-scheduling residues it cannot touch, while a rematerialised pseudo has low rtx_cost and drops out of precompute_register_parameters, which is what lets `mov r0,#2` land between the two pool loads. WHEN UNRELATED SPELLINGS TIE EXACTLY, CHECK A SIGNATURE: four spellings of OvlFunc_901_20088a8 all measured EXACTLY 2 and the residue was in none of them -- __Func_8092c40 RETURNS A VALUE, and marking the callee int makes r0 live out of the previous call, changing what precompute_register_parameters may reorder across. The ladder is now delete the variable, then check every callee's RETURN TYPE, then check the flags. THE CALL-SAVED ALLOCATION ORDER IS NOT MONOTONIC: every register-birth-order note here read as if registers are handed out numerically, so an r8/r9 diff looked adjacent. REG_ALLOC_ORDER's call-saved run is 4, 5, 6, 7, 8, 10, 9, 11 -- r10 BEFORE r9 -- so an r8/r9 diff is allocnos four and six trading places and an r9/r10 difference is a TWO-PLACE move. Settled first by probe and then by reading the source, WHICH TURNED UP THE LARGER CORRECTION: THE COMPILER SOURCE IS IN THE BUILD IMAGE and this notebook has been treating it as unavailable. /opt/camelot-gcc/gcc-2.96/gcc/ and /opt/camelot-gcc/agbcc/gcc/ hold arm.h, global.c, local-alloc.c, combine.c and thumb.md; several notes citing arm.h:989, global.c:allocno_compare and calls.c:805 were carried on trust from a fingerprint list and can now simply be read. OTHER MECHANISMS: a NEGATIVE multiplier is what selects the shift chain, because expand_mult calls synth_mult on the ABSOLUTE value and negates after with the cost budget taken from the negative MULT -- x*6553 gives ldr+mul and only x*-6553 gives the seven-instruction chain, so READ THE SIGN OFF THE ROM, and note that C - x*k and x*-k + C are indistinguishable in an ISOLATED probe and separate only under real register pressure. A `mov rLow,rHigh` before a store based on that high register is a SECOND RELOAD and is evidence about CONTROL FLOW, not statement order -- gcc emits it only when a call sits between the pointer's definition and its use. A SPILLED PARAMETER IS A STATEMENT ABOUT ANOTHER PSEUDO'S LIVE RANGE. And DO NOT REUSE ONE LOCAL FOR TWO ROLES: splitting one two-def pseudo into two variables, same instructions and same values, went 54 to 26. FOUR PARKS. Func_8021390 at 36 of 97, the dominance contradiction's third instance and the first where the constant is ZERO and the hoist is provably not CSE or PRE -- twelve flags byte-identical. OvlFunc_964_20090c4 at 17 of 116 of which FOUR ARE PHANTOM (__umodsi3 vs _umodsi3_RAM, already aliased in the overlay's own linker script), so 13 real, with instructions 0-35 and the whole loop body byte-for-byte; ~100 spellings and 8 flags leave the floor unmoved. Func_8021228 at 15 of 126, whose _MSG_980 was DELIBERATELY NOT ADDED to message.sym because it closes nothing while the function is parked. And Func_80a5b94 at 32 of 118, all of it one allocator decision, with the mechanism READ OUT OF local-alloc.c:868-871 rather than inferred: reg_equiv_replace is set only when a pseudo is set once, used once AND referenced in more than one basic block, so UPDATE_EQUIV_REGS NEEDS A BASIC-BLOCK BOUNDARY and a pooled constant's twin is rematerialised only ACROSS A BRANCH. Its `1` pair straddles an if and reproduces the ROM; its `0` pair sits wholly in the tail block, is not moved, stays live across five calls and rotates four registers. Proved by construction -- change only that zero to a 5 and it drops to 21 of 118 with the prologue and the whole head matching. That is a 118-instruction instance of the class this notebook calls its single most valuable open question, and a far better probe target than the 250-instruction script band. It also found that ALLOCNO PRIORITY IS COMPUTABLE AND THE COMPILER WILL PRINT IT: -da plus the .18.greg dump gives the allocation order directly and .17.lreg gives what local-alloc took first, which should be the first move on any register-swap park. WHAT THE ROUND SAYS ABOUT METHOD: three corrections were to things this notebook already said, none found by looking for errors -- each was a note written once, from one function, and never measured against a second. A FINDING STATED WITHOUT ITS TABLE IS A FINDING THAT CANNOT BE CORRECTED. 1,877 functions remain in assembly: 647 unparked and 291 parked in the main ROM, 618 unparked and 321 parked across the overlays |

**[Fakematch worklist](reports/fakematch-worklist.md)** — seven functions we
matched with inline asm rather than with a construct, all previously parked. The
debt is listed rather than buried, with twenty ruled-out formulations and the one
positive lead: `volatile` gives the right ordering in plain C and fails only on
the stack slot it also forces.

**[Cracking arg-interleave](reports/arg-interleave.md)** retires two blocker
classes that stood for 36 batches, and unblocks 516 functions. The lever is one
line of C: assign an argument's constant to a local in a DIFFERENT BASIC BLOCK
from the call. Read it before attempting anything with a displaced argument --
and read how it was found, because the method (search gcc's own output rather
than generating variants) applies to the classes that are still open.

**[Do large functions break the method?](reports/large-functions.md)** is not a
batch -- it is an experiment, and it is the most important document here for
anyone deciding what to work on next. Short version: length is not the problem,
blocker DENSITY is. 99% of functions over 400 instructions contain at least one
shape we cannot solve at any size, against 23% of those under 20. The binding
constraint is the number of unretired blocker classes, not the number of
unelevated functions.

Every batch is verified the same way, from a clean build:

    docker build -t goldensun-build -f tools/Dockerfile .
    docker run --rm -v "$PWD:/work" -w /work goldensun-build \
        sh -c 'make clean && make compare'

- **Fourteen `.global` lines have been added to eight existing `.s` files** (four
  in batch 09, three in batch 34, one in batch 35, one in batch 44, one in batch
  50, three in batch 52, one in batch 54) so that functions could be split out
  from the `.incbin` tables and script tables they select between. Every one of those
  files already exported sibling labels the same way, and a `.global` emits no
  bytes -- but it is the only change in these batches that edits assembly rather
  than replacing it. Reverts cleanly if you would rather it did not happen.

## Naming

`docs/names.md` is generated by `tools/name_evidence.py` and carries a proposed
name plus **the basis for it** for every function elevated. The rename pass is
deliberately deferred — names do not survive into the ROM, so a bulk rename is
byte-neutral — but the evidence is captured as it is produced.

Read the `Basis` column before trusting a name: `docs/attribution.md` records
that the inherited annotation corpus gets mechanism right and purpose wrong
often enough to matter.

## Standing items for review

**36 functions are NOT compiler output and are excluded from candidate lists.**
`asm/rom_f9000/rom_f95e0.s` and `asm/rom_f9ef8_a.s` are the MP2K sound driver,
hand-written assembly: `mov r12, lr` / `bx r12` return conventions, an `adr`/`bx`
switch into ARM mode to reach `umull`, and a `bl` to a label inside another
function. `tools/not_c.py` holds the list and the evidence;
`asmfacts.functions()` filters them out by default, so every tool built on it
skips them. They are byte-exact today and should be counted as DONE, not as
remaining work. Corroboration is already in this tree: `src/lib/m4a/m4a0.s` is a
hand-written assembly copy of the same driver, carried in from Coaltergeist's
tree and never built -- upstream ships this driver as assembly.

**Do not clear the smallest size band first.** See
`src/non_matching/tiny_reg_order.c`. Below twenty instructions there is no
structure left for C to express and everything remaining is register birth order
and address-load placement -- the residue that parks larger functions, except it
is the whole function. The 21-40 band is where the hit rate is.


**26 parked files are PARTLY STALE** — they name a group of functions, some of
which have since been elevated, and their notes now describe a mix of solved and
unsolved work. `python3 tools/stale_parks.py` lists them. They need editing, not
deleting, and that has not been done. Sixteen FULLY stale files were deleted in
batch 71; the park count went 180 → 164. Treat any park census taken before that
as counting solved problems.


Things surfaced across batches that need a decision from someone who knows the
codebase better than we do. Listed once here rather than repeated per batch.

- **`struct Actor` was 16 bytes short** and is corrected in batch 01, with
  `include/entity.h` folded into it. The five offsets that still have two
  competing readings are documented in the header itself.
- **The annotation corpus gets mechanism right and purpose wrong** often enough
  to matter -- see `docs/attribution.md`. Any annotation ported into this tree
  should be treated as a starting point, not a finding. One was corrected in
  batch 01 (`Func_80b7e7c` does not take the arguments it was documented with).
- **What are the id namespaces?** No longer blocking — answered mechanically
  in batch 07 — but still worth a real answer.

  Where the ROM pools a constant that would fit in an eight-bit `mov`
  (`ldr r0, =1`, `ldr r2, =0xf`, `ldr r3, =0x1d`), the operand was a **symbol
  reference** in the original source — gcc never pools what it can `mov`, and
  always pools a symbol address. Verified by assembling both forms.

  This was carried as the top blocker for twelve rounds, at a **measured 103
  of 395 overlay candidates**, on the reasoning that naming a namespace we
  could not identify was a bad trade. (It was reported as "three functions" in
  batches 04 and 05, which was what happened to be in front of me rather than
  the real number.)

  **That reasoning was wrong.** Matching needs the operand to be a *symbol*,
  not an *identified* one. Defining it by value in `area.sym` emits no
  bytes and asserts nothing — exactly what `message.sym` already does, per its
  own comment: *"named by value; pending semantic names."*

  So what remains is a naming question, not a blocker: `_AREA_4d` works, and a
  real name would be better. See `docs/elevation.md`, "Tell: the ROM pools a
  SMALL constant".

  **What the values themselves say.** There are **122 ids**, and **121 of them
  are compared in exactly one overlay** — which is what "each area's code lives
  in one place" looks like. That result is structural, read straight out of the
  ROM, and it is the strongest single argument for calling this an area id.
  `0x6a` is the lone exception, compared in two.

  The tables selected are named `MapEntrance_ARRAY_*`, `Events_TolbiSpring`,
  `Events_GameBuildings` — per-location data — and a ROM annotation reads
  *"area 0x13 -> .L1d04"*.

  **What that evidence is not.** The `MapEntrance`/`Events` names arrived with
  the upstream tree and are **not the maintainer's** — they are an earlier
  contributor's inference, so they corroborate nothing on their own. Earlier
  batches counted them as independent confirmation; that was wrong. The
  annotation corpus is documented in `docs/attribution.md` as getting mechanism
  right and purpose wrong often enough to matter. The genuinely independent
  support is the overlay-exclusivity result, and that alone.

  Oddities a correct account should explain: ids `0x00`–`0x0f` never appear,
  and the space is **70% dense over `0x10`–`0xbd`** — 52 unused values inside
  the range, with 11 of 23 overlays showing gaps and two having clusters 56
  apart.

  The names were adopted deliberately on that basis. If it turns out wrong the
  fix is a rename: the values are what matter to the link and they do not
  change. Confirming what `MapEntrance` means would settle it.

  *(Two earlier claims here have been corrected: that the ids came in PAIRS,
  and that they formed "dense, contiguous per-area runs" spanning ~190 values.
  Both were repeated before being checked.)*

- **Two clean-build bugs in the Makefile are fixed in batch 07**, both
  predating our work and both reachable from a fresh clone of your tree: three
  `orig.bin` dependencies naming files that have since been split, and
  `as -MD` recording a directory-less `.c` from a generated `.s`'s `.file`
  directive. Until these, every clean build here needed manual recovery.

- **A third clean-build bug in the Makefile is fixed in batch 31**, also
  predating our work and also reachable from a fresh clone: the dependency
  generator for the ELFs greps linker scripts for `.o` names, so the `.sym`
  files a script `INCLUDE`s are never dependencies. Editing `message.sym` left
  `stage1.o` stale and every overlay referencing the new symbol failed to link
  with `undefined reference`, which reads like a typo in the C. Silent until
  someone adds a symbol.

- **Twenty-three TUs are built with `-fno-rerun-cse-after-loop`**, covering thirty
  functions (first two in batch 25),
  and as of batch 42 one of them is MAIN-ROM code rather than an overlay, which
  weakens the reading that this is an overlay-only property. It
  this needs a decision from someone who knows the original toolchain. Both load
  a save-flag id twice around a call; at -O2 gcc-2.96 hoists it into a
  callee-saved register, spending a push, a pop and two moves to save one pool
  load, and one of them comes out LONGER than the ROM as a result.

  That flag is specifically the pass responsible — `-fno-gcse`,
  `-fno-cse-follow-jumps`, `-fno-cse-skip-blocks` and
  `-fno-expensive-optimizations` all leave the hoist in place.

  **The evidence is thin and is stated as such.** Sweeping all 85 parked files
  with the flag matched only the first two, so it is not a general lever for the
  constant-CSE class -- the nine added since were each found by recognising the
  shape on a fresh candidate, not by the sweep.

  **THE COUNT IS NOW THE ARGUMENT.** Batch 51 searched the whole corpus for the
  shape mechanically -- a pooled flag id loaded for two or more flag calls in
  one function -- and found **19 unelevated functions** carrying it; the worklist is now
  exhausted, 18 elevated and one already parked. Twenty per-file rules is no longer comfortably read as "the original
  build used this flag on these particular files". It reads more like
  **gcc-2.96 running a pass the original compiler did not**, in which case the
  right fix is a compiler-level one and all fourteen rules should eventually be
  dropped together. Flagged here because that is a maintainer's call, not ours,
  and because every batch that adds another rule makes it more pressing.

  **Batch 50 turned the recognition into a rule worth stating: a flag id READ
  IN A GUARD AND WRITTEN IN THE BODY is constant-CSE.** gcc hoists it into a
  callee-saved register across the call, spending a push and a pop to save one
  pool load, where the ROM simply loads it twice. That shape found three of the
  eleven directly. It may instead mean gcc-2.96 runs a pass the original
  compiler did not, in which case the right fix is a compiler difference and
  these two rules should be dropped. See `CSE_CFLAGS` in the Makefile.

- **Four functions are left as assembly that an inline-asm fakematch would
  match** (batch 32, the pool-load-first class). Your tree already contains one,
  `OvlFunc_883_2008fbc`, with that exact shape. We chose assembly over four more
  fakematches; if you would rather have the bytes, the park note lists every
  member and the change is mechanical.

- **Per-file flag rules written as `%` patterns can describe the wrong
  translation unit** (found in batch 45). The `_a`/`_b`/`_c` split chain is a
  POSITIONAL carve of one overlay's assembly, not a TU boundary, so a rule
  anchored on a name prefix can spread one TU's `-O1` choice onto a neighbour
  that merely shares that prefix. Two functions were compiled at the wrong `-O`
  this way, and the symptom was a clean four-line argument-fill diff
  indistinguishable from a real blocker.

  One rule is narrowed in batch 45. Two others in `rom_7ed0a0` still span more
  than one parent; **both build green today**, so they were left alone, but a
  future split under either stem would inherit `-O1` silently.
  `tools/tryc.py` now warns when a mismatch was screened under wildcard-sourced
  flags. Worth a decision from someone who knows which stems are really one TU.

- **Seven sources in your tree are compiled but never linked**, found by
  `tools/asmfacts.py --unlinked` (added batch 59). All seven arrived with the
  base commit, so this predates our work and is reported rather than changed:

      src/rom_b0000/dummy.c
      src/rom_c0/rom_447c_a_b.c
      src/rom_f9000/rom_f9ef8_b.c, _c_a_b.c, _c_a_c_b.c, _c_b.c, _c_c_b.c

  The `rom_f9ef8_*` ones look like an abandoned pass at the m4a engine: the
  linker script takes `src/lib/m4a/m4a.o(.text)` for that region instead, and
  the matching `.s` files are gone.

  **Why it is worth knowing rather than tidy-up trivia:** a `.c` whose `.o` no
  linker script references still compiles, still leaves `make compare` green,
  and still reports clean from `--orphans`. Anyone who elevates a function into
  one of these files gets a passing build and no effect on the ROM. That is
  exactly the failure this check was written for, after it happened once here.

- **SIXTY OF THE PARKED SET ARE WITHIN SIX INSTRUCTIONS OF MATCHING**, measured
  in batch 58 with `tools/near_parks.py`. That number is the most useful one
  here for deciding what to work on, because it separates two populations the
  word "parked" hides: a park at 2 of 24 is a compiler difference nobody has
  cracked, and a park at 30 of 34 is a function whose C is probably wrong. Only
  the second is worth re-reading from scratch.

  **Close does not mean reachable.** Many of the sixty sit on named, settled
  classes where the residual is a FLOOR rather than a gap -- the signed
  lower-bound canonicalisation (batch 55), the pre-header load merge, multiply
  operand canonicalisation.
  Each park note says which. Read it before spending a round.

  What the number DOES say is that the remaining difficulty is concentrated in a
  small number of compiler behaviours rather than spread across the corpus. If
  any one of those classes is ever cracked at the compiler level, it takes a
  large block of these with it.

- **Narrow constant materialisation** gates 34 functions and is half solved: a
  named `int` mask reproduces the ROM's 32-bit constant, but the instruction
  ordering resists seven attempts. `src/non_matching/overlays/narrow_constant.c`
  has the detail.

### `make clean` cannot be recovered inside Docker

`tools/agbcc/bin/{agbcc,agbcc_arm,old_agbcc}` are **Mach-O x86_64** binaries --
macOS host executables. The Linux container runs them as shell scripts and
fails with `Syntax error: "(" unexpected`. Five objects need them:
`src/lib/m4a/{m4a,m4a_tables}.o` and
`src/lib/agb_flash/{agb_flash,agb_flash_mx,agb_flash_at}.o`.

After any `make clean`, rebuild those five ON THE macOS HOST before returning to
the container. The exact commands are in
[reports/batch-61.md](reports/batch-61.md); the `m4a` pair must be run by hand
because macOS make picks the generic `%.o: %.c` rule for them and reaches for
`tools/gcc296/xgcc`, which exists only inside the container.

`make compare` is byte-exact against `baserom.gba`, so a passing compare after
this recovery is proof the host-built objects are correct -- nothing is taken on
trust. But the report gate's "clean `make clean && make compare`" is a
five-minute recovery, not a no-op. Do not run `make clean` casually mid-round.

### Argument precompute: DIAGNOSED, and it is a compiler difference

Eleven screened functions, every one within six instructions, are held by where
gcc materialises a cheap `mov rN, #imm` among the other argument registers. The
cause is now read out of the compiler source in the build image, not guessed:

    calls.c:805   precompute_register_parameters() copies any argument whose
                  rtx_cost > 2 into a pseudo BEFORE any hard register is loaded,
                  guarded by SMALL_REGISTER_CLASSES && reg_parm_seen.
                  reg_parm_seen is set for argument i before argument i is
                  tested, so it is 1 on the first register argument already.
    arm.h:1061    SMALL_REGISTER_CLASSES is TARGET_THUMB -- always 1 here.
    arm.c:2042    In Thumb, ASHIFT/PLUS/MINUS/NEG/NOT/COMPARE cost 4; pool
                  loads and synthesised constants also exceed 2.
    calls.c:1684  load_register_parameters() then loops FORWARD;
                  LOAD_ARGS_REVERSED is not defined anywhere in this tree.

Expensive arguments get hoisted ahead of the register loads; a cheap constant is
emitted afterwards, and lands last. The ROM's compiler did not precompute -- its
stream is plain forward load order with constant synthesis left in place.

**The predictive rule, tested:** a call misorders when its argument list mixes
cheap constants with TWO OR MORE expensive values and a cheap one is not last.
A call whose arguments are all cheap constants matches. Confirmed on
`OvlFunc_921_20099bc`, which has one call of each kind -- both predictions held
on the first screen. It also explains the matches: `OvlFunc_946_2009624` and
`OvlFunc_932_200aa10` pass only cheap constants.

**This is not fixable from C.** Eight source spellings and eight flags are
byte-identical to the default. These parks should not be retried as source
problems; they need a compiler change, and they sit with the
`-fno-rerun-cse-after-loop` count as the second concrete piece of evidence that
the reference toolchain differs from Camelot's.

**Correction, and it invalidates a test I reported earlier:** `-fno-schedule-insns`
was never a real experiment. arm.c:634 force-disables `flag_schedule_insns`
whenever TARGET_THUMB is set, silently, "since it's on by default in -O2". The
first scheduler NEVER RUNS for any file in this project. Only
`-fno-schedule-insns2` does anything. Any past note claiming sched1 was ruled
out by flag should be read as ruled out by construction.

Affected functions:
`OvlFunc_883_2008dc0/e54/e84/f5c/f8c`, `OvlFunc_884_200881c/20088ac` (2 of 16
each), `OvlFunc_930_2008870` (2 of 24), `OvlFunc_930_20088a8` (5 of 24),
`OvlFunc_909_2009958` (6 of 18), `OvlFunc_921_20099bc` (2 of 15),
`OvlFunc_899_2008428` (2 of 14), `OvlFunc_966_200810c` (5 of 27),
`OvlFunc_926_200a68c` (3 of 30).

The last three were re-derived from scratch in later rounds, twice with an
invented "a callee-saved register must already be committed" theory that this
diagnosis already covers and contradicts. Their park files now open by pointing
here. **Check this section before screening anything whose argument list mixes
a shifted or pool-loaded value with a cheap constant** -- that is the whole
population, and none of it is reachable from the C.

Full derivation:
[src/non_matching/ovl_780898/2008dc0.c](src/non_matching/ovl_780898/2008dc0.c).

### Register-pressure residue: a category, not a set of one-offs

Six parks now reach the same conclusion independently, and it is worth stating
once rather than rediscovering per file. **What registers the ROM uses is a
consequence of pressure in the original translation unit, not of how the C is
written.** Three shapes recur:

- **An elided copy.** The ROM loads a value into one register and copies it
  before use; gcc loads straight into the destination and skips the copy.
  `Func_80bf54c` (4 of 19), `OvlFunc_969_200d9f0` (9 of 27).
- **A dead callee-saved register.** The ROM reserves a register, sets it, and
  never reads it. `OvlFunc_935_2008704` (6 of 24) -- six instructions of
  prologue/epilogue bookkeeping for a value with no consumer.
- **A constant hoisted or not hoisted out of a loop.** `Func_80a9d84`
  (14 of 30) -- gcc hoists three loop-invariant constants, the ROM hoists two
  and materialises the third inside the loop.

**The diagnostic that settles it:** find the function's near-twin that DOES
match. `Func_80a9cbc` matches and `Func_80a9d84`, identical but for a third
constant, does not -- so the third constant is the whole cause.

**CORRECTION (batch 64).** This entry previously also claimed that the
two-named-locals spelling produces the elided copy in `Func_80bf574` but not in
`Func_80bf54c`, and attributed the difference to pressure from a second store.
That was wrong and was never checked: screening `Func_80bf574` shows it emits
`ldrb r3` with no copy, exactly like `Func_80bf54c`. A third sibling,
`Func_80bf3bc`, has strictly MORE pressure -- a parameter held across a
three-argument call -- and elides it too.

So for THAT shape the copy is a plain codegen difference, not a pressure effect.
Pressure remains the right reading for the dead callee-saved register and the
loop-invariant hoist; it is not established for elided copies, and the
near-twin test is what distinguishes them. Run the test before invoking the
category.

**What not to spend rounds on:** the declaration lever, statement reordering,
extra named locals, and the derived-initialiser lever have all been tried across
these six and are byte-identical to the default in every case where the twin
comparison shows pressure is the cause. A copy cannot be requested from C when
nothing competes for the register.

**What might actually move them:** more of the surrounding TU being elevated, so
that the register pressure the original had is reproduced. These are the parks
most likely to fall out for free later rather than to a targeted fix, and they
should be re-screened after their file's neighbours are elevated -- not before.

### The pool tell: 209 functions need NO naming at all -- correcting batch 63

Batch 63 measured the pool tell at 271 unelevated functions and concluded they
were "blocked on naming... a maintainer's call". **The second half was wrong.**

`area.sym`, `message.sym`, `file_table.sym` and `wram.sym` already define **611
symbols**. Cross-referencing them against the pool-tell sites:

    unelevated functions with the pool tell    270
      EVERY value already has a symbol         209   <- elevatable NOW
      some values have symbols                  13
      no values have symbols                    48   <- genuinely need naming

So 209 functions are not blocked on a decision by anyone. They are blocked on
nobody having looked in `area.sym`. Two were elevated the day this was found:
`OvlFunc_952_2008070` (parked at 7 of 32, exact once `_AREA_8b` was used) and
`OvlFunc_926_200a574`.

**THE TECHNIQUE**, already established in this tree and documented at the top of
`area.sym`:

    extern int _AREA_3c;                        /* declare  */
    if (v == (int)(&_AREA_3c)) ...              /* compare against its ADDRESS */

gcc-2.96 always pools a symbol's address and never pools a constant it can build
with an eight-bit `mov`, which is exactly the asymmetry the pool tell describes.
An absolute symbol definition emits no bytes, so the link is byte-identical and
`make compare` proves it.

**`OvlFunc_926_200a574` is the clean demonstration**: it compares against
`_AREA_3c` (pooled) AND against a literal `3` (`cmp r3, #0x3`) in the same
function, so the tell distinguishes symbols from literals rather than being an
artifact of the disassembly.

**What the remaining 48 need** is genuine naming, and `area.sym`'s own header is
the model: it records why the namespace is called an area id, what the evidence
is, and explicitly what the evidence is NOT. The values still unnamed are
`00 02 05 0c 15 18 19 1a 1b 1f 28 2d 32 3a 3b 3d 69 7e 80 86 8d 8f 90 92 a9 b6
bb f0` -- and note that several of those (0x00 appearing in 54 functions, 0x02 in
21) are almost certainly NOT area ids, since the area space is documented as
never using 0x00-0x0f. They belong to some other namespace that has not been
identified yet.
### Dead end: load-then-copy is NOT a blocker signature

`ldrb rA, [..]` immediately followed by `mov rB, rA` is the shape behind the
four-function `Func_80bf*` family, where gcc loads straight into the destination
and drops the copy. It looked like it might be a large blocker class, and the
raw numbers encouraged that:

    unelevated   508 of 2702  (18.8%)
    elevated      72 of 2915  ( 2.5%)

**That 7.5x enrichment is a SIZE ARTIFACT.** Elevated functions are small,
unelevated ones are large, and a longer function has more chances to contain any
given pattern. Controlling for length removes it:

    size band     elevated      unelevated
    12-25         19/980  (2%)  10/149  (7%)
    26-40         29/485  (6%)  28/351  (8%)
    41-70          4/94   (4%)  67/666 (10%)
    71-200         2/45   (4%) 191/988 (19%)

In the 26-40 band -- where both populations are well represented -- the rates are
6% and 8%. gcc emits load-then-copy routinely in functions that match byte for
byte, so its presence says nothing about whether a function is reachable.

**Do not build a candidate filter on this.** The `Func_80bf*` family is blocked
by a specific instance of the pattern, not by the pattern itself. The only two
measured, real blocker counts remain argument precompute (12 functions,
mechanism traced to compiler source) and the pool tell (271, upper bound).

### What the park corpus actually looks like, measured

A claim was made in batch 64 that the remaining small-function pool is
"dominated by cases where gcc's output is shorter than the ROM's". **That was an
overstatement.** Measured across the 53 park files that record a length
comparison:

    ours SHORTER than ROM    21  (40%)
    same length              28  (53%)
    ours LONGER               4  ( 8%)

**The majority are same-length**, which means register allocation and
instruction ordering -- differences the levers are built for and which have been
cracked repeatedly. The shorter-than-ROM group is real and substantial at 40%,
and it is the genuinely hard one: where the optimiser proved something (a value
the guard pinned, a provably-dead instruction, a mask the store makes
redundant), no source spelling puts the longer form back.

**Caveat on the denominator:** only 53 of 159 park files record a length
comparison at all. The newer park format includes it; older notes do not. The
proportions are from the files that have it, and newer parks are biased toward
the near-misses that got the most attention, so the shorter-than-ROM share is
probably an over-estimate for the corpus as a whole.

**What follows for planning:** the same-length majority is still worth working,
and the biggest shortfalls are listed below so they are not re-attempted as
spelling problems:

    -6  src/non_matching/ovl_7bf5a8/2008704.c   dead callee-saved register
    -5  src/non_matching/rom_b5000/80c23c0.c    branchless bit extract
    -3  src/non_matching/rom_c0/8006384.c       register-register AND
    -3  src/non_matching/rom_b5000/80c2410.c    provably dead mov

### Retired: "dma.h register binding" was not a blocker class

Batches 54-55 named `include/dma.h` register binding as a blocker and five parks
were filed under it. Re-screening all five in batch 65 shows the label was
wrong, and no park is actually held by it:

- **`Func_80170c4`** matches the ROM's `stmia r3!, {r0, r1, r2} / sub r3, #0xc`
  EXACTLY through `DMA3_SET(&buf, d, cnt)`, with the halfword staged at `sp+2`
  by a plain local. Its residue is an unrelated copy elided at a shared exit.
- **`Func_80a22f4`** has the binding as ONE of two defects, and its own note
  records a helper variant (`"+l" (_dst)`) that removes it -- the spill goes
  away and the length drops from 13 to 12. What remains is gcc strength-reducing
  the second transfer's constants off the first, which is about constants and
  has nothing to do with registers.
- **`OvlFunc_914_2008c0c`** needs gcc's partial tail merge; the count of base
  loads is the count of calls, which no helper shape reaches.
- `Func_80198dc`, `Func_80bd7a4` are held by other classes.

**Practical consequence:** `include/dma.h` works. Reach for `DMA3_SET` /
`DMA3_COPY16` when a function does a DMA transfer, and do not treat the header
as a known-lost cause. If a DMA function does not match, the reason will be
somewhere else, and the park note should name that reason rather than the
header.

### The remaining corpus is duplicated, but mostly by SHAPE not by bytes

`tools/twin_finder.py` reports clusters of UNELEVATED twins. **The first version
of this entry (batch 65) reported 102 functions in 12 clusters and implied one
solution would port to all of them. That was wrong, and the correction matters
more than the original claim.**

A signature match is the same OPCODE sequence; operands may differ. Measured
across the 118 shape-matched functions in clusters of four or more:

    EXACT, operand-identical -- one .c ports verbatim      15 functions, 3 shapes
    SHAPE only, constants differ                          103 functions

    18 x 172 insn   shape only, no two identical
    17 x 139        shape only
    17 x 132        shape only
     7 x 220        shape only
     7 x  97        shape only
     7 x  27        EXACT -- all seven identical
     6 x  16        shape only
     5 x  46        shape only, 4 of the 5 identical
     5 x  39        shape only, 4 of the 5 identical

So the free-work figure is **15, not 102**. Reporting the shape count alone
overstates it by nearly 8x. The tool now prints both and labels each cluster.

**Shape-only clusters are still worth real money**, just not for free: one `.c`
ports with the constants substituted. That is exactly how `OvlFunc_916_200836c`
was elevated from `OvlFunc_947_2009578` in batch 63 -- same code, different
VCOUNT bound and tables, three constants changed, matched on the first screen.
A 172-instruction shape with 18 members is still the largest single lever in the
corpus if its shape can be cracked once.

**The one EXACT cluster is the highest-value park.** 7 x 27 instructions, a
shared routine in the main ROM and six overlays, blocked by a SINGLE hoisted
load. It was parked twice independently before the duplication was noticed
(`rom_8a000/rom_9a44c.c` and `ovl_7ced6c/2008ab0.c`, now cross-linked).
`-fno-schedule-insns2` fixes that exact load and breaks four earlier pairs
instead.

The 6 x 16 shape is the `OvlFunc_883/884` family, blocked by argument precompute
(`calls.c:805`) -- a compiler difference, not fixable from C.

### Where the blocked work is concentrated, by function count

Three blockers now account for more blocked functions than everything else
combined, because each holds a whole duplicated cluster:

    4 functions   one register naming (was 8; the 4 x 39 cluster is now
                  ELEVATED -- basic-block placement was solved by inverting the
                  guard, see docs/elevation.md)
                  src/non_matching/ovl_7ced6c/2008a4c.c
    7 functions   one hoisted load, scheduler-related
                  src/non_matching/rom_8a000/rom_9a44c.c (highest value: the
                  seven are operand-identical, so one fix ports verbatim)
    12 functions  argument precompute, calls.c:805 -- a compiler difference,
                  NOT fixable from C

So 11 functions sit behind two behaviours that are still open, and 12 behind one
that is closed. That is a better guide to where a round is worth spending than
the raw park count of 158.

**All three clusters were found by tools/twin_finder.py or by the cluster census
in this file, not by reading the candidate list.** Duplicate-aware selection is
what makes a single screen worth four to seven functions; per-function selection
had been picking singletons for many rounds.

### Naming a symbol is sometimes the elevation -- eight added on stated evidence

Batch 67 added eight ids to `area.sym`: `_AREA_7e 86 8d 8f 90 92 a9 b6`. Each is
**compared against the halfword at gState+0x1C0**, which is area.sym's own
stated criterion for the namespace. Eleven functions were blocked on them.

**Each was previously defined only in `file_table.sym`.** That is not a
contradiction: a file id and an area id may share a number, and 95 small values
already collide across the four `.sym` files. The CONSUMER distinguishes them,
which is why `tools/sym_candidates.py` refuses to classify from the value.

**What the addition is and is not.** It asserts the namespace, not the meaning --
the same "named by value, pending semantic names" convention `message.sym`
already uses. It does not claim to know which place `0x8f` is.

**Weak corroboration, stated as weak:** all eight fall inside 0x10-0xbd, so they
shrink the gap the area.sym header records as an oddity. The gaps shrink; they
do not close.

**An unresolved discrepancy, recorded rather than smoothed over.** The area.sym
header states "52 unused values inside the range". A direct count of the
definitions gave 57 before this change and 49 after. The two may be counting
different things -- defined ids versus ids some function actually compares -- or
one may be stale. Reconcile before citing either as evidence.

### `__SetDestMap` is a second area-id consumer -- resolved, not assumed

Batch 68 recorded the `__SetDestMap` lead as INCONCLUSIVE, naming the check that
would settle it: match each pooled value's destination register against the
callee's signature. Running that check settles it three ways:

- The signature is known from elevated code:
  `extern void __SetDestMap(int map, int entrance);`
- At **all 16 call sites** the pooled value goes into **r0** -- the `map`
  argument -- with r1 set separately as the entrance.
- An elevated file **already** writes `__SetDestMap((int)(&_AREA_01), 1)`. The
  convention predates the investigation.

Seven ids added on that evidence: `_AREA_00 02 04 2d 3a bb be`. `area.sym` now
defines 133.

**A header claim was wrong and is corrected.** It said *"ids 0x00-0x0f never
appear"* — flatly contradicted by `_AREA_01`, which the file itself defines. The
accurate form is that they never appear **in comparisons**; they do appear as
`__SetDestMap` destinations. The comparison census still shows nothing below
0x7e among the ids it was missing, so the underlying observation survives; only
its scope was overstated.

**The consumer rule now has two branches for area:**

    compared against gState+0x1C0     -> area id  (strong)
    passed as arg 0 to __SetDestMap   -> area id  (strong)
    used in arithmetic with the above -> area id  (weaker, read it)

**Caveat on immediate value:** every `__SetDestMap` caller is 69+ instructions
and they are cutscene functions, so these seven symbols unblock nothing on their
own today. They remove a blocker that would otherwise be hit later.

### What the 177 parks are blocked on, roughly

A keyword pass over the park notes, first-match-wins:

     37  register allocation      (naming, elided copies, elided saves)
     35  scheduling               (hoists, load ordering)
     19  constant-CSE
     12  optimiser proved something and removed it
      7  argument precompute
      4  pool tell / naming
      4  basic-block placement
      3  cross-jumping / CSE of two loads
     56  unclassified

**Read the numbers with two caveats.** The classifier takes the FIRST class
whose keywords appear, so a park describing two causes is counted once -- the
argument-precompute figure reads 7 here against a hand count of 12, because
several of those notes mention register allocation earlier in the text. And the
56 unclassified are mostly older parks written before these class names
settled; they are not a separate phenomenon.

**What survives the caveats** is the shape: register allocation and scheduling
together are the largest group by a wide margin, and both are compiler-policy
differences rather than things C can state. The recurring detail is that the
ROM's allocator reaches for callee-saved registers sooner than gcc-2.96 does --
several parks show the ROM using r4-r6 where gcc uses r0-r3, and a few show the
ROM using r4 without saving it at all.

**Planning consequence:** the small-function frontier is now mostly these two
classes. Further progress at this size is likely to need a compiler-level
answer rather than more source spellings, which is the same conclusion the
argument-precompute investigation reached from a different direction.

### A named mechanism for the register-allocation parks: `REG_ALLOC_ORDER`

37 parks are blocked on register allocation, and the recurring detail is that
the ROM reaches for r4-r6 where gcc-2.96 uses r0-r3. That has a named cause in
the compiler source.

`config/arm/arm.h:989` defines the allocation order, with its own rationale in
the comment above it:

    /* ... Allocate r0 through r3 in reverse order since r3 is
       least likely to contain a function parameter; in addition results are
       returned in r0.  */
    #define REG_ALLOC_ORDER  { 3, 2, 1, 0, 12, 14, 4, 5, 6, 7, 8, 10, ... }

So gcc tries **r3, r2, r1, r0 first** -- caller-saved before callee-saved -- then
r12 and r14, then r4 onward. **There is no Thumb-specific override**; the same
order applies, with the high registers simply unusable.

**What this explains.** In `Func_800fa8c` the ROM keeps four loop values in
r4/r5/r6 and saves three of them; gcc puts two in r0/r1 and two in r4/r5. With
no call in the loop, gcc's choice is cheaper and legitimate -- it is not a bug,
it is a different starting point in the same list.

**What it does NOT establish.** That the original toolchain had a different
`REG_ALLOC_ORDER` is the most economical explanation, not a proven one.
Different register pressure, a different pass order, or a different
`CALL_USED_REGISTERS` would produce similar symptoms. Nothing here was tested by
rebuilding a compiler.

**The testable next step**, for anyone who can rebuild gcc-2.96: change
`REG_ALLOC_ORDER` to start at 4 and re-screen the register-allocation parks. If
a meaningful fraction resolve, that is the answer for the largest blocked class
in the corpus. If none do, the class needs a different explanation and this
entry should be struck.


- `docs/structs.md` -- every struct the tree declares, by name / by layout / by file.
  Generated by `tools/structmap.py`. Read it before inventing a struct; see
  "Check docs/structs.md before inventing a struct" in docs/elevation.md.

